# config/settings.py
import os
from pathlib import Path

# ==========================================
# 基础配置 (Base Settings)
# ==========================================
APP_NAME = "Jian - 专业交易复盘系统"
# 版本演进（1.4.1 及以前，属"回测总集"语义编号，已废弃）：
#          v1.1 交易时间契约重构 → v1.2 交割单字段补全 → v1.2.1 微观测算修正
#          → v1.3 范围聚焦 + 持仓时长三级口径回归
#          → v1.4.0 回测能力总集（多函数段共享池 / 条件组 Gate / 风控离场器 / 指数 regime 门控）
#                   + 单股回测 UX 硬化 + 年度面板净额口径统一 + Dashboard 每日净额日历
#          → v1.4.1 数据管理页（数据湖删除/清点/增量/批量预下载）+ 行情同步统一收敛(MarketSyncService)
# ★ v6.16 起版本号**统一跟随 git**（用户 2026-09-15 拍板）：版本号 == 最近一次 push 的
#   **commit message 首词**（形如 `1.22`），每次 push 递增 **0.01**（1.22 → 1.23 → …）。
#   三处必须同步（§9-A）：① git commit message 首词；② 本常量；③ 仓库根 `version.json`。
#   1.21 = §7-B5「回测成交真实性」（三档成交时点 + T+1 硬约束 + 用户指引）。
#   1.22 = 回测页 UI 重构（摘要条 + 右侧遮罩抽屉 + 结果区吃满）+ §9-L 模块拆分收官。
#   1.23 = 行情工作台版式收口（§7-B6：顶栏两行 + 图标轨/可折起左栏 + 常驻读数条
#          + 最近使用的 chips + 分钟档位 1/5/15/30/60）+ trading_desk 拆分（1375 → 341）。
#   1.24 = 侧边栏重设计（§7-B8：手风琴卡片 + 自选分组 + 拖拽排序 + 配方库收编内置指标）。
#   1.25 = 画线工具重做（§7-B9：在图上点出来 + 平行/回归通道语义修正 + 32 种全可画
#          + 拖动/点选交互修复 + 扇形改延伸射线）。
#   1.26 = §9-U 复盘页版式收口 + §9-L `review`/`data_feed` 拆分收官。
#   1.27 = 文档分层重构（主文件瘦身 + `docs/` 三册，零业务代码）。
#   1.28 = §7-B1/B2 M2「全市场筛选」页落地（内核 / 会话缓存 / 后台线程 / M2 页，STEP 0–4）。
#   1.29 = §7-B1/B2 M3「广度统计」页落地（广度折线 + 指数副图联动 + ⚡增量 + ⟳全量重算，STEP 5）。
#   1.30 = §7-B1/B2 STEP 6 联动收尾（就绪度体检 / ⬇补齐缺失 / 成分股人话诊断 / 扫描就绪预设）。
#   1.31 = 修复 M2/M3 指数成分股代码格式（sh000300 → 接口要 000300；normalize_cons_code 边界规范化）。
#   1.32 = 修复成分股名称列全空（回包漏装配 _names）+ 内核警告 UI 出口（此前被整段吞掉）。
#   1.33 = M2/M3 体验修复轮：M3 图表可读性根治（bar 序号轴/双视觉/指数四图形/分界把手/轴对齐）
#          + M2 基准日先选后扫与缺数据全链路诚实化（闸门/就近落位/missing 进总数）
#          + 成分股换中证官网权威源（修「指数代码」选列回归与缺斤短两）+ 表格性能三件套。
#   1.34 = §7-B8 R7 副图换序收尾（⬆⬇ + 拖拽：复用 DragHandleListWidget 三道闸 + layer_model
#          可持久化 sub_order + 换序只改格位不改 target）+ 测试偏好隔离补正
#          （新键 sub_order 防泄真实库 / breadth 显示前置钉死），smoke 707→721 / 482→493。
#   1.35 = §7-B10 数据新鲜度全案（M1+M2/M3）：定稿守卫（is_daily_bar_settled/裁尾）+
#          真交易日历（trade_calendar/CalendarWorker，当日缓存+离线回退）+ M1 区间默认终点/滞后自动补/
#          回退回执 + M2/M3 一键「更新到最新」/滞后提示/基准日诚实化，smoke 737→742 / 508→513。
#   1.36 = §7-A2 回测结果图表导出：build_daily_series（逐日净值+买卖点共同源）+
#          CSV 末尾逐日净值数据段（Excel 选中列绘图）+ 新增 backtest_xlsx.py（openpyxl 内嵌
#          净值曲线图+买卖点 marker），smoke 745 / 514→521。
#   1.37 = §7-A4 回测历史存档：data/backtest_archive.py（不可变快照+轻量索引，save/list/load/
#          pin/delete/滞动淘汰/uuid 防注入；净值抽稀端点保底+并入成交日；读路径不写盘）+
#          ui/widgets/backtest_history_ui.py（版式/渲染）+ ui/views/backtest_history.py（薄壳，
#          第4子页“运行历史”：载入查看/复用/重跑/送行情/★重点/删）；默认自动存档可关、
#          每标的20/总500淘汰重点豁免；M1 只读回放（现场保存/恢复+禁导出+横幅）+
#          导出菜单「存为历史快照」；结果区净值曲线新增买卖点散点，smoke 782 / 554。
#   1.38 = §7-E2 代理失败“说人话” + 熔断提前：_classify_error 新增 "proxy"
#          （⚠ 必须先判文本指纹再退回 isinstance —— requests 的 ProxyError 也是 OSError 子类）
#          + looks_like_proxy_error + 三出口文案各一档（不写死本机端口）
#          + ThrottlePolicy.proxy_circuit_breaker=3 + abort_reason_text 三消费方共用
#          + bulk_download 竞态判据收编公共件 JobGuard，smoke 796 / 557。
#   1.39 = §7-E3 日线落盘列白名单：akshare_feed.DAILY_KEEP_COLUMNS（OHLCV + symbol/amount/
#          turnover/outstanding_share）+ _normalize_ohlcv 末尾 apply-if-present 裁列
#          ⇒ 源透传的中文列/期货列不再进湖；白名单 ⊇ 横截面内核所需列有交叉断言钉住；
#          存量 parquet 不动。
#   1.40 = §7-E5 下载层新鲜度对齐真交易日历：ThrottlePolicy.expected_latest（由调度层
#          ui/workers 每批注入一次「最近一个已收盘定稿的交易日」）+ _is_fresh 改判
#          last >= expected ⇒ 周末/节假日/盘中不再把全池判成"不新鲜"而白跑一遍空增量
#          （5400 只 ≈ 50 分钟换来零变化）；estimate_seconds 支持 stale_count +
#          format_duration 单一出口，修掉"永远显示约 50 分钟"的劝退式预估。
#          拿不到日历 ⇒ 原样回落旧判据（零行为变化，可安全回滚）。smoke 817 / 557。
#   1.41 = §7-B1/B2 补漏「需要动作必有入口」（用户实测：M2 换范围后体检说未下载 456，
#          整页无更新入口）：按钮名收成单出口 custom_widgets.SYNC_ACTION_LABEL
#          （core/data 不许指名 UI 按钮）+ M2/M3 摘要条常驻 btn_sync + 两页
#          _drop_stale_result 作废旧结果（ScanResult/BreadthResult.clear）+
#          "已有结果"分支绝不调 set_empty + 坏文件补「去数据管理」入口；
#          同批把 JIAN_RULES.md 从 99.1k 瘦身回 96.8k。smoke 817 / 566。
#   1.42 = §10-9 还清「数值控件宽度」：用户实测「间隔(秒) 预设 0.6 但只看得见 0」——
#          弹窗把控件钉死在 64px，原生上下箭头占掉 18~22px 后文本区只剩 ~30px。
#          收成 custom_widgets.SPIN_MIN_WIDTH(72) + double_spin()/int_spin() 唯一工厂
#          （只给 minimumWidth、绝不 setFixedWidth）；默认值/范围/步长不变。
#          附带修冒烟脚本竞态污染（异步回测写脏真实策略库与存档目录）。smoke 817 / 577。
#   1.43 = §7-B11 后台下载：任务归属从弹窗搬到主窗口（新 ui/download_hub.py，串行 K=1、
#          不自造 QThread）+ 底部下载条 / 导航角标 / 非模态队列面板三处投影；
#          批量预下载改非模态（旧版模态冻屏 + SyncWorker 挂在弹窗 ⇒ 只能守着）、
#          数据管理页不再锁整页、M2/M3 交队列、单只同步登记互斥；
#          后台完成不再弹“部分失败”模态框，原因文案收成 sync_service.failure_hint 单出口。
#          smoke 817 / 611。
#   1.43(收口轮) = §7-B11 收口（与 1.42 合成一个 commit）：
#          ① cancel(全部) 漏给**排队中**任务发 job_finished ⇒ 页面永久卡在“进行中”（§11.5-82）；
#          ② 下载条「中断」误用 cancel(None) ⇒ 静默取消排队任务，改为只停当前；
#          ③ 单只互斥 token 由 4 处手写收成唯一公共件 download_hub.SingleSyncGate；
#          ④ 去重键补 min_date/档力度；批量弹窗 parent 改主窗口；取件统一 hub_of；
#          ⑤ 补「真 SyncWorker / 真 QThread」端到端探针 15 项（假线程测不出①）。
#          smoke 817 / 627。
#   1.44 = §7-B11 后续 · 下载性能档（§11.6 待拍板项落地，A+B 一次发版）：
#          ① 批内并发池（SyncWorker 用 ThreadPoolExecutor 编排现成 refresh_one，数据层仍纯同步），
#             配 **共享全局节流阀** RateGovernor ⇒ 聚合请求率不变、只靠网络往返重叠 ≈2×（不封 IP）；
#             concurrency 字段默认 1（回滚开关），真实下载由 DownloadHub.submit 升到均衡档 3；
#          ② 全市场当日快照秒补（spot_em 1 次请求）：只补“只差当天这一根”、只 15:05 定稿后、
#             只 qfq 日线分区、不复权价仅追加当天不回填历史；缺口>1日/首次退回逐只；单位归一防 100×。
#          真实联网端到端待用户实测。smoke 841 / 637。
#   1.45 = §7-B11 后续（续）· 统一下载设置入口（用户：“预下载里调的间隔/并发，M2/M3/数据管理怎么同步？”）：
#          ① 批量下载节流参数（间隔/抖动/熍断/跳过/并发）收进**一份全局偏好** preferences.download_prefs（唯一真源）；
#          ② 新建 `ui/dialogs/download_settings.py` 统一点设置对话框（唯一编辑面），预下载弹窗/数据管理/队列面板三处入口都开它；
#          ③ **删掉**预下载弹窗与数据管理页各自的间隔/抖动/熍断/跳过旋钮（防“两套值”漂移）；M2/M3 自动跟随全局；
#          ④ `DownloadHub.submit` 无策略⇒走 `download_policy_from_prefs()`；有策略⇒完全尊重。smoke 848 / 636。
#   1.46 = §7-B12 M2/M3 结果表增强 + 配置资产化 + 接入运行历史（P1–P8 全落地）+ 三处实测修复：
#          ① 结果表：# 行号 + 表头排序（命中置顶/组内排序）+ A 层富字段（当日·当月·当年涨幅/量/流通市值）
#             + 细分行业列 + B 层估值（仅基准日==最新定稿日才显，当前值不冒充历史）+ 列拖拽自定义；
#          ② 独立「筛选方案库」data/scan_strategy_store.py（M2/M3 共享一份池，不并入回测策略库）；
#          ③ 运行历史：kind 声明式注册表（history_kinds.py）+ M2/M3 专属预览（四态占比条/条件全文可复制）
#             + 存档显式分组（scope/condition/counts）+ 范围名带指数名与代码；
#          ④ 修：M2「重跑」对指数成分必空跑（改 request_run 等名单+体检）· 动作层无 kind 守卫 ·
#             自动存档纯静默 · **「⬆ 更新到最新」在已有结果时永久卡在"更新中…"**（改：不动结果区）·
#             **「历史不足」说不清**（空态点名标的 + 说明次新股/更新补不齐/非缺陷）；
#          ⑤ 撤 M2/M3 的「送行情页」（统计口径不针对个股）；界面字体改**开源/免费商用栈优先**（§10-15）；
#             M1 净值图加样板同款白底浅边框圆角卡 + 段间距留白。smoke 855 / 682。
#   1.47 = 复权自愈 + 字体版权收口（用户实测："切前复权/不复权图表始终没变化，怀疑复权坏了"）：
#          ① **真 bug**：前复权分区被增量同步污染成"混合体"（旧行旧口径 / 新行新口径）⇒ 图上出现
#             **假跳空**（实证 300803：本地 9/18=82.00 × 9/21=56.86，源端 qfq 连续 56.55→56.86）。
#             根因 = 增量起点只取「本地末日 + 1」**零重叠**（注释却写"多留一点重叠"）⇒ 除权后
#             已落盘历史**永远不重算**。修法：`QFQ_OVERLAP_DAYS=10` 真重叠 + `_qfq_factor_drifted()`
#             （重叠日收盘差 >0.5% ⇒ 因子变了）⇒ **当场整段重拉**并回 `rescaled`（只对 qfq 分区）。
#          ② 体检补第三类：`_price_seam()`（单日 ≥25% 交易上不可能）⇒ 点名"疑似除权后历史未重算"。
#          ③ 取数失败**不再假装在取数**（`_refresh_adjust_hint(failed=True)`）；另一份口径未下载时
#             明说"差异暂时比不了"。
#          ④ §10-15 落到代码：新增 `ui_painter_font(pt)`（磅值版），清掉全仓 14 处专有字体名 +
#             源码级断言扫全 ui/ 防回归。smoke 864 / 686。
#   1.48 = 「不复权」分区的**下载入口**（用户实测："切不复权还是没数据；数据管理里 raw 只有 1 项"）：
#          ① 探针先排除嫌疑 —— 数据层与取数**都正常**（`refresh_one(kline_daily_raw)` 真能下 1665 行；
#             行情页切不复权**真线程端到端**也成功）⇒ 坏的是**入口**；
#          ② 批量预下载加「复权口径」（前复权 / 不复权 / **两个都下 = 两个任务**，任务名带口径；
#             指数预设来源禁用该项 + 说明；预估乘口径数）—— 旧版非指数来源一律只写 `kline_daily`，
#             所以"预下载几十轮"也永远补不上不复权那份；
#          ③ 数据管理页：**空分区**（表里没行可勾 ⇒ 按钮恒灰的死局）回落"过滤框里的代码"作同步目标
#             （只对同步类动作，**删除仍只认勾选**）+ 空分区把出路写进状态行；
#             顺手修 `str.isalnum()` 对中文也为 True 导致"平安"被当代码下载的坑（加 `isascii()`）。
#          smoke 864 / 699。
#   1.49 = 云端同步「一次两份」+ 复权正确性再补一刀（用户两问驱动）：
#          ① `DeskData.SYNC_ZONES=(kline_daily, kline_daily_raw)`：一次点齐前复权 + 不复权，
#             两个分区各一个 worker（`SingleSyncGate` 单占位 ⇒ 按分区各一个实例 `_gate_for`），
#             收齐才收尾（`_sync_seen` 含被丢弃回包 ⇒ 按钮不会永久禁用），只渲染当前口径那份；
#          ② **真 bug**：`_try_apply_spot`（spot 秒补）**命中即 return** ⇒ 绕过 v1.47 的"复权因子漂移
#             ⇒ 整段重拉"；除权当天恰好是它最易命中的形态（本地末日==上一交易日）⇒ 只追加当天一根、
#             历史仍按旧因子缩放 ⇒ 用户实测"当天转赠/分红的股票抓到的是不复权"。修法：快照加
#             `昨收 → prev_close`（**不进** `DAILY_KEEP_COLUMNS`，只当判据）+ `_spot_prev_close_ok()`
#             除权闸门（不等 >0.1% 或判据缺失 ⇒ 一律拒绝秒补 ⇒ 退回网络增量整段重算）。
#          ③ v6.66/1.48 的其它两件（发布物护栏 / 口径可见性）见 §11.6。smoke 869 / 714。
#   1.50 = **「切不复权图始终是前复权」修复**（用户实测 601825，数据层与程序入口都正常）：
#          `ui/widgets/custom_widgets.py` 的 `SegmentedControl._on_clicked` 曾用 `not key` 判"无效段"，
#          而「不复权」的业务值 `ADJUST_NONE` **就是空字符串** ⇒ 手点被整段吞掉（`current_adjust`
#          不变、高亮也不动）；而 `select_adjust()` 走 `set_current()`（无该判断）⇒ 程序化正常 ⇒
#          **断言全绿、真人点不动**。改**按索引**判有效（`key_at()` 越界也返回 `""`，靠返回值分不出
#          "越界"与"合法空 key"）+ 断言补**真实点击**。坑 = §11.5-96。smoke 869 / 717。
#   1.51 = **M2 行业列不再静默**（用户实测："行业一直是空的，是不是我下载没弄好？"）：诊断结论 =
#          代码与下载都没错，是 **东财 `clist/get` 端点对本机整片断连**（同域名单点报价却 HTTP 200；
#          新浪源全正常；无代理）⇒ 细分行业唯一来源就是它 ⇒ 映射永空、`industry_map.json` 从不落盘。
#          代码侧真缺陷 = `scan_flow._on_industry_ready` 拿到 None/空表时**什么都不做**（静默）⇒
#          修：失败分支回执**说话 + 给出路**（"再点一次开始扫描会重试"，缓存命中也重试）。坑 = §11.5-98。
#          smoke 869 / 719。
#   1.52 = **行业列改造**（东财源评估结案后的落地）：旧抓法"板块清单 + 逐板块成分 = 80+ 连击"正踩
#          东财**匿名频次窗**（连打几十次整段拒绝、约 30 分钟自恢复；带 Cookie 可豁免但**不做依赖**）⇒
#          改为**同端点分页直取 `f100`**：`fetch_industry_page`（单页 100、全市场 ~56 页）+ 每批 8 页
#          + 页间隔 0.5s + 进度存 `preferences['industry_fetch']`（**断点续抓**）+ `IndustryStore.merge`
#          分批入库（空值不覆盖）⇒ 分几次扫描摊平、补齐后零请求。坑 = §11.5-98/99。smoke 869 / 727。
#   1.53 = 东财额度「不再自己打光」（用户实测：行业列仍空，日志两条相隔 7 秒一起失败）：
#          真因 = 一次扫描完成**同时**起两串"全市场"请求 —— 行业分页（1.52）+ `ak.stock_zh_a_spot_em`
#          的**隐藏 ~56 页突发**（akshare 源码 pz=100 + 逐页翻）⇒ 匿名频次窗被自己打光（§11.5-100）。
#          ① 东财取数整块搬 `data/em_market.py`：分页 `clist`（行业）+ **批量报价 `ulist.np/get`**
#             （一次 100 只，估值/秒补共用；实测 f9/f20/f21/f23 + 价量一次全给）⇒ 估值与秒补**按标的取**
#             （单只 1 个请求，不再是 56 页）；② `data/em_throttle.py` 失败退避（10→20→40→60 分钟、
#             成功清零、跨重启、冷却期不联网 + 回执说"还要等多久"）；③ 行业每批 8→3 页；
#             ④ 关窗 `main_window._shutdown_background_threads()` 统一 cancel+wait 所有 QThread。
#          smoke 879 / 732。
# ⚠ 必须与仓库根目录 version.json 保持同步：自动更新以二者比对为准（见 core/updater.py）
APP_VERSION = "1.53"

# ==========================================
# 界面配置 (UI Settings)
# ==========================================
MAIN_WINDOW_WIDTH = 1500
MAIN_WINDOW_HEIGHT = 950

COLOR_PROFIT = "#4CAF50"         
COLOR_LOSS = "#F44336"           
COLOR_PROFIT_TEXT = "#2E7D32"    
COLOR_LOSS_TEXT = "#C62828"      
COLOR_TEXT_PRIMARY = "#212121"   
COLOR_BACKGROUND = "#FFFFFF"     

RGB_PROFIT = (76, 175, 80)
RGB_LOSS = (244, 67, 54)
RGB_PROFIT_FILL = (76, 175, 80, 50) 
RGB_LOSS_FILL = (244, 67, 54, 50)

# ==========================================
# 业务逻辑配置 (Business Logic Settings)
# ==========================================
DEFAULT_STRATEGY = "未分类"
# NOTE(v1.2.1): 已删除 DEFAULT_ACCOUNTS —— 手工录入的"归属账户"只应列出真实存在
# 的账户（来自已导入数据），不预置"国内长线/国内短线"这类虚构账户。

# 绩效统计的虚拟初始资金 (用于计算收益率与回撤基准)
INITIAL_CAPITAL = 1000000

# ==========================================
# 路径与存储配置 (物理隔离)
# ==========================================
USER_HOME = Path.home()
USER_DATA_DIR = os.path.join(USER_HOME, ".jian_data")

DB_PATH = os.path.join(USER_DATA_DIR, "jian_trades.db")
SCREENSHOT_DIR = os.path.join(USER_DATA_DIR, "screenshots")

# ==========================================
# 更新检测配置 (Update Settings)
# ==========================================
# 这里填写你存放在云端的 version.json 文件的直链地址
# 格式要求形如：{"version": "1.1.0", "notes": "修复了Bug", "url": "https://下载链接"}
# 测试阶段，我们先用一个安全的占位符，一会教你怎么在本地测试它
UPDATE_CHECK_URL = "https://raw.githubusercontent.com/ENDVEN/Jian/refs/heads/main/version.json"