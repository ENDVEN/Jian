# === Python 核心缓存 ===
__pycache__/
*.py[cod]
*$py.class

# === 本地运行产生的业务数据 ===
# 正式截图目录在 ~/.jian_data/screenshots（仓库外）；本条只防"误在仓库里建截图目录"
screenshots/
data/cache/
*.db
*.sqlite3

# === 临时脚本 / 调试输出（用完即删，见 §10-13 文件归置规范）===
# 会话期的一次性探针脚本与重定向输出**不允许沉淀在仓库里**，这里只是兜底
_tmp_*
*.smoke.out

# === 操作系统产生的临时文件 ===
.DS_Store
Thumbs.db

# === 常用 IDE 的配置文件 (比如你用的 VSCode 或 PyCharm) ===
.vscode/
.idea/

# === 虚拟环境目录 (如果你未来使用的话) ===
venv/
env/
.env

# === 用户私有数据/公式 (禁止误入开源仓库) ===
# 【v6.14】私有样例统一收进 samples/：整个目录默认忽略，**只放行说明文件**，
# 这样"以后随手往里丢什么"都不会误提交（原来只靠文件名模式兜，漏一个就泄露）。
samples/*
!samples/README.md
# 以下为纵深防御：即使文件被挪出 samples/ 也依然不会入库
实例函数.txt
*_函数*.txt
*策略*.txt
*实例*.txt
# 实例/真实交割单 Excel 含资金账户等隐私，禁止入库
*交割单*.xlsx

# === AI 生成的文档库 (repowiki) - 防止误 push ===
# .qoder/repowiki 是 AI 自动生成的文档库，包含 77+ 个 Markdown 文件
# 这些文档仅供本地参考，不应提交到版本控制系统
.qoder/repowiki/