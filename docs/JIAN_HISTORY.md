# Jian 历史时间线（`docs/JIAN_HISTORY.md`）

> **归档文件（v6.32 分层重构）**：这里放**时间线**类历史 —— 版本叙事与 changelog。
> ⚠ **本文件的历史行数字一律冻结**（永不回改）；**现状**一律以 `JIAN_RULES.md` 为准。
> 配套：`JIAN_RULES.md`（现状·规范）/ `JIAN_PLAYBOOK.md`（踩坑手册）/ `JIAN_ARCHIVE.md`（已完成主案 + 旧审计）。

---

## 一、版本叙事（原 `JIAN_RULES.md` 文件头 · v5.1 → v6.31 逐版记录）

> **本文档是"给未来的你看"的唯一权威记忆**。功能开发前先读：
> §11 速查表（30 秒找回手感）→ §5 数据契约/铁律 → §4 代码地图 → §6/§7 完成度清单。
> 本文档于 v5.0 全面重排：完成项按模块归类核对，未完成项按**优先级 + 依赖**重新分类。
> v5.1：记录「市场回测·阶段A」落地（多函数段共享池 + 条件组 Gate）。
> v5.2：记录「市场回测·阶段B」落地（引擎风控离场器 + exit_reason 全链路）。
> v5.3：记录「市场回测·阶段C」落地（大盘指数 regime 门控）。
> v5.4：记录「单股回测 UX 硬化」（指数预设扩充 / 防滚轮 / 角标弱化 / 编辑区滚动）。
> v5.5：**全仓逐文件通读核对产出** —— 修正 §4 目录树遗漏；新增 §9-F~L 七项实测发现的技术债
> （年度面板口径漂移、花名册半成功覆写风险、UI 边界松动等）；新增 **§11 速查表**。
> v5.6：修「风控行箭头与条件组不一致 + 上箭头只有右半边可点」（根因=Qt 复合控件半截 QSS，
> 见 §10-9 / §6.6-U5）；回测区间下沿 2018→**2016** 并新增近3个月/近6个月/近5年三档（§5.3-H）。
> v5.7：**甲方案收尾** —— 修 §9-F 年度面板净额口径 / §9-G 花名册半成功覆写 / §9-K 注释失真 /
> §9-A 版本号滞后（APP 升 **v1.4.0** 并同步 `version.json`）；
> 新增 **C1 Dashboard 每日净额日历热力图**（`ui/widgets/calendar_heatmap.py`，GitHub 风格 53×7）。
> v5.8：**§7-A3 数据管理页落地 + §9-H 架构债清账** —— 数据湖补删除/清点原语；
> 同步逻辑收敛到 `data/sync_service.py`（增量合并 + 温柔抓取 ThrottlePolicy 防封 IP）；
> UI 线程统一到 `ui/workers.py`；第 6 导航页「🗄 数据管理」+ 批量预下载弹窗。
> APP 版本升 **v1.4.1**（`config/settings.py` 与 `version.json` 已同步）。
> v5.9：**对 v5.8 的独立复查（第二双眼睛）** —— 因会话中更换过语言模型，把上一批改动
> 当作陌生代码重新审查，发现并修复 4 个隐患（见 §9-M）：①增量"时效滞后一天" ②休市空增量
> 误判失败+白重试 ③数据起点 2010→2016 的静默缩水回归 ④扫描线程/关窗线程的竞态与崩溃风险。
> 全部以冒烟测试断言固化后回写本文件。
> v5.10：**数据管理页 UX 加固（用户实测反馈驱动）** —— 修"勾选框点击后直接消失"
> （根因：勾选列 ResizeToContents 算出≈0 宽，指示器不可见/点不中）+ 过滤下单元格行错位；
> 「清空分区」从主操作行**隔离**到左侧栏底部并要求键入「清空」二次确认；按钮/文案改用户语言
> （"增量更新"→「更新到最新」、"强制全量重拉"→「重新全量下载」）；
> 同步失败**按原因分类提示**，退市股（无数据源）不再被误报为"代码不存在/网络抖动"。
> 详见 §9-N；新增 §10-10 按钮层级与文案铁律。
> v5.11：**勾选框最终重设计** —— v5.10 用"原生 indicator + cellClicked"出现**双重触发**：
> Qt 对 indicator 自动切换一次、cellClicked 又切一次 → 点一次净效果=没点（永远打不出勾），
> 且 `ItemIsSelectable`+`SelectRows` 带来蓝色行高亮。最终方案：**彻底放弃 Qt 原生 checkbox**，
> 第 0 列自绘 ☐/☑ 字符 + 整格点击=唯一切换入口 + `NoSelection`（消除行高亮）。
> 已用 QTest **真实鼠标事件**验证单点/连点/取消均稳定（§9-N1 修订）。
> v5.12：**第二次全仓通读核对（模型切换后的"陌生代码"复查）**。本次**未改任何业务代码**，
> 只做"文档 ↔ 磁盘"的逐项对账，产出 §9-O 系列 10 项发现：
> ① 口径漂移：流水/复盘的「仅盈利/仅亏损」过滤器仍用毛利（O-1）；
> ② 架构债残留：`ui/workers.py` 只收口了"行情同步类"线程，回测/成分股/导入/版本检测
>   4 个 QThread 仍散落在各自文件（O-2）；③ `sync_service` 的 docstring 把数据起点写成
>   2016，与实际的 2010 冲突，是 §9-M3 的**回归隐患**（O-3）；
> ④ 行情页「☁️ 云端同步」恒为全量重拉、与管理页双按钮体验不一致（O-4）；
> ⑤ 回测页同步结果缺竞态守卫（O-5）；⑥ §4 目录树行数/版本号全面过时（O-6）；
> ⑦ 图表样式重复从 2 处变成 3 处（O-7）；⑧ §9-J 小坑确认仍在 + 一处文档描述有误（O-8）；
> ⑨ Dashboard KPI 实为 17 项非 18 项（O-9）；⑩ 复盘页回放 K 线反复全量读 Parquet（O-10）。
> **本节最底部的 §11.6 已据此刷新"下一步推荐顺序"。**
> v5.13：**第一 / 第二梯队全部落地**（用户已批准"筛选改净额"，并明确云端同步语义
> = "本地有数据就更新、没数据就抓取"）。改动清单（全部通过 25 项冒烟断言 +
> 离屏构造主窗口/全部弹窗）：
>   · **O-3** `data/sync_service.py` docstring 起点改回 2010-01-01 并写明"数据窗≠评估窗"；
>   · **O-5** `backtest.py` `_on_synced` / `_on_index_synced` 补竞态守卫（照抄 market.py）；
>   · **O-7** 新增 **`ui/widgets/chart_style.py`**，把**四份**重复的 Pokorny 轴样式与
>     资金/净值曲线绘制收敛为 `apply_pokorny_style` + `plot_equity_curve` 两个函数；
>     （⚠ 通读时漏数了一份：`ui/views/market.py` 也有，且它传的是 **PlotItem** 不是
>     PlotWidget —— 新函数两种类型都兼容）；
>   · **O-2** 三个 UI 线程（`_BacktestRunThread` / `_ConstituentsWorker` /
>     `FuturesImportWorker`）迁入 `ui/workers.py`，改名 `BacktestRunWorker` /
>     `ConstituentsWorker` / `FuturesImportWorker`；§2 表述已改为准确版本；
>   · **O-8** 四项小坑：`review.py` 空数据也刷新时间选择器 / `days=0` 改 `continue` /
>     切年视图清空回放图 / 画廊不再静默丢弃失效截图路径（改为标注「⚠ 已丢失」）；
>   · **O-1（用户拍板）** 流水页 + 复盘页「仅盈利/仅亏损」改为 **净额** 口径，
>     两处下拉加 tooltip 说明口径；
>   · **O-4（用户拍板）** 行情页 `force_sync_cloud` → 改名 `sync_cloud`，
>     `force_full` 恒为 **False**：本地有数据=增量、没数据=全量；
>     新增 `lbl_sync_status` 回执（已是最新 / 已更新新增 N 行 / 同步失败）。
>     "重新全量下载"仍只存在于「🗄 数据管理」页（§10-10 危险动作隔离）。
>     **v5.13 修订**：O-4 新增的同步状态 QLabel 触发了 Qt 布局坑（顶部栏末尾 QLabel
>     会把富余高度全吞掉、标题被拉成 400px），已给 main_splitter 显式 `stretch=1` 根治，
>     固化为 §11.5 第 13 条。
> v5.14：**回测结果导出落地（§7-A2 半程）** —— 用户拍板"**只做导出、不做删除**"，
> 并把「结果历史存档」列为**远期方案**（见 §7-A4，留给后续会话）。
>   · 回测页「K线控制行」右侧新增「导出明细」按钮；
>   · 新增 **`_last_meta` 参数快照**：`start_backtest` 发起瞬间定格 标的/区间/策略名/
>     函数段/参数/买卖表达式/风控/指数门控，导出永远对应当前 `_last_result`，
>     不掺导出瞬间的编辑框内容；
>   · **`_compose_result_csv`**（纯函数，14 项断言全过）= 注释头 + 逐笔明细；
>     `utf-8-sig` 落盘，Excel 可直接打开。
>     ⚠ **v5.16 推翻 v5.14 的"注释头裸写"方案**：裸写会让含英文逗号的函数源码行
>     （如 `STICKLINE(A, B, C, 3, 0), COLORFF0000;`、`MA(C, 5)`）被 Excel/WPS
>     当多列拆开 → 用户实测"第三部分：图形绘制"乱码不可读。正确做法见 v5.16。
> v5.15：**导出升级 · PNG 报告图 + 共享常量上收**（用户拍板：CSV 移除净值行 /
> PNG 加离场原因饼图 / 下拉菜单入口）。
>   · **CSV 精简**：移除 200+ 行「每日净值明细」段 —— 它既是可读性低的主因、又可由
>     引擎用相同参数复现；溯源义务交给『参数 + 逐笔』，净值曲线视觉化由 PNG 承担。
>   · **新增 `ui/widgets/backtest_report.py`**（SRP，218 行，零新增第三方依赖）：
>     单页 PNG 报告图 = 标题（品种/区间/策略）+ KPI 四卡 + 净值曲线 + **离场原因饼图** +
>     参数/风控/指数门控简表；复用 `chart_style.plot_equity_curve`；QWidget 离屏
>     grab() 渲染，中文依赖系统字体（沙箱缺 CJK 字体会显方框，属环境非代码问题）。
>   · **共享常量上收到 `core/backtest.py`**：`EXIT_REASON_LABELS` / `EXIT_REASON_COLORS`
>     / `risk_summary(...)` —— 明细表着色、CSV 表头、PNG 图例与饼图色彩**三处同源**，
>     禁止任何 UI 文件再各写一份（§9-O7 教训）。
>   · **下拉菜单取代单按钮**：`📄 导出 CSV 明细…` / `🖼 导出结果图 PNG…`，
>     未来加 XLSX 只需在 `_export_menu` 新增 action。
>   · **16 项断言全过**（CSV 无净值段且数据行=逐笔数、PNG 尺寸 1120×660、菜单结构、
>     共享常量、空 equity 容错）。报告图单页 1120×660。
> v5.16：**CSV 导出兼容性修复（用户用 WPS 实测反馈）** —— "函数源码第三部分含英文
> 逗号的行在表格软件里被拆列成乱码"。
>   · 根因：v5.14/5.15 把"注释/表头行"**裸写**（不走 csv 转义），而函数源码里
>     `STICKLINE(..., DYN_INDEX, DYN_INDEX, 3, 0), COLORFF0000;` 这类行含英文逗号，
>     Excel/WPS 打开时会当成多列拆分，表现为"图像绘制段看不到内容"。
>   · 修法：`_compose_result_csv` 里**每一行都作为单格字段走 csv.writer**
>     （含逗号行自动加引号 → 表格软件还原为单格；不含逗号行保持裸写；空行用
>     `writerow([])` 产生真·空行，不再出现 `""` 假空行）。
>   · 校验：**9 项断言** —— csv.reader 严格重解析整份文件：表头区每非空行 = 1 格、
>     逐笔区 = 8 列 × 成交数、中文离场原因可解析、无 `""` 假空行。
>   · **导出文件对任何遵循 CSV 规范的软件（Excel / WPS / 编辑器）均适配**。
> v6.0（规划定稿）：**§7-B3 公式统一绘图（引擎级 · 最高优先级 P0 · 无代码变更）** ——
> 用户复盘发现"函数里 STICKLINE/DRAWICON 画出的指标从没出现过"，根因是 `program.py`
> 长期 SKIP 绘图语句（历史 §7-B3 欠账），而 CSV/PNG/回测K线都没有能画它的底层管线。
> 用户定稿：以"**引擎产出统一绘图 IR + 唯一渲染器 OverlayPainter**"为长远架构，
> 避免未来优化各图表时逐图一对一手改；M0 契约层 / M1 求值层 / M2 渲染+回测页 /
> M3 收尾 / M4 远期全图复用。完整规格与验收见 §7-B3 主案，优先级见 §11.6。
> v6.1（图表架构总纲 · 用户 2026-09-10 拍板 · **无业务代码变更**）：
> 「§7-B3 公式统一绘图」**升级为「图表架构总纲」** —— 不再只解决"公式叠层画不出来"，
> 而是把绘图底层拆成**四层**（① 引擎 IR `core/formula/draw.py`；② 图表宿主
> `ui/widgets/chart_pane.py` + `chart_host.py`；③ 渲染器 `ui/widgets/draw_overlay.py`；
> ④ 标注对象 `data/annotations.py` + `ui/widgets/annotation_layer.py`），并厘清
> **两条互不相同的管线**：**A 公式叠层**（随数据/参数重算、**不持久**）与
> **B 用户标注**（持久到 `(标的,周期)`、**逐个独立删除**）。
> 终极形态 = **通达信级行情工作台**（主图/副图用户函数 + 画线工具 + 按标的持久化）。
> **四项拍板**：① `ChartPane` 抽象**插在 M2 之前**（防"单图写死"返工）；② 首个落地验证场
> 用**回测页**（P3），**行情页放 P4**；③ 标注存储**先 JSON、按可迁 DB 设计**（API 与存储解耦）；
> ④ **内置指标与用户公式在渲染层统一为一套"图层协议"**。
> 完整四层架构 + P0–P8 路线图见 §7-B3 总纲；铁律见 §10-12；"改 X 动哪"见 §11.4。
> v6.2（**P0+P1 落地** · 用户 2026-09-10 批准开工）：
> **P0 图表宿主最小抽象** `ui/widgets/chart_pane.py`(76 行)—— `ChartPane` 把「一个窗格」=
> PlotItem + 公式叠层容器 + 用户标注容器 固定下来；渲染器从此**只认 `ChartPane`**（防单图写死）。
> **P1 引擎 IR 契约 + 求值**：新增 `core/formula/draw.py`(124 行：COLOR_TABLE + DrawSpec/DrawData
> + 属性校验)；`core/formula/program.py` 113→**265 行**，语句分类 **3 态 → 4 态**
> （ASSIGN / OUTPUT / DRAW；旧 SKIP **移除** —— 无法识别的语句/绘图函数一律报错，绝不静默）；
> 新增 `execute_programs_with_draws()`（与 `execute_programs` **同一 EvalContext、不二次求值**）。
> 新增可复跑冒烟脚本 **`smoke_chart.py`**（P2 起扩展为"图表架构"统一冒烟入口）。
> **核心回归保证**：`execute_programs` 行为逐位不变 → 条件组 Gate / 函数检测 / 指数门控零影响。
> v6.3（**P2 多窗格落地**）：新增 `ui/widgets/chart_host.py`(262 行) —— `ChartHost` =
> **主图 + N 副图**（x 轴联动 `setXLink` / 只有最下窗格显示底部轴 / 窗格增删与高度权重 /
> `pg.DateAxisItem` 日期轴 / **跨窗格同步十字光标 + 数值回执**）；`chart_style.py` 抽出
> `style_axis()` 作为"换轴后重新着色"的唯一来源，并新增 `CROSSHAIR_COLOR`。
> `smoke_chart.py` 扩到 **97 项断言全过**；全仓 compileall + 主窗口离屏构造通过。
> v6.4（**P3 渲染器 + 回测页接入落地**）：新增 `ui/widgets/draw_overlay.py`(210 行) ——
> **全 app 唯一** `OverlayPainter(pane, bars_x).render(draws)`：line→`PlotDataItem`、
> stick→自绘 `_StickItem`（TDX 语义：width≤1 细线 / >1 实体 / 第5参=空心）、icon→`ScatterPlotItem`
> （图标号映射表 + 未知号默认菱形）；附 `slice_draws()` / `overlay_extent()` 两个宿主工具。
> **新增 `chart_style.ensure_contrast()` 对比度守卫**（通达信公式按黑底写的 `COLORWHITE`
> 搬到白底会"画了看不见"—— 保留色相地压暗/提亮到 WCAG 3:1 且亮度差 ≥0.30）。
> **回测页 `🕯 K线买卖点` 接入**：`_on_data_ready` 改走 `execute_programs_with_draws`
> （不二次求值）；`_render_kline` 按 **"排序后位置→原始行号"映射**切窗口喂叠层（防漂移）；
> 新增「显示公式叠层」开关（只切可见性、不重置缩放）；y 范围纳入叠层（超 K 线高低价不被裁）；
> **函数检测阶段也走同入口** → 写错的绘图语句"检测时"就报错。
> **验收**：`smoke_chart.py` **126 项** + `smoke_backtest_overlay.py` **20 项页面级**（含
> 离屏 grab 像素差异证明"真画上去了"）全过；全仓 compileall + 主窗口离屏构造通过。
> v6.5（**事故修复：存量函数回归** · 用户 2026-09-10 实测反馈）：用户在软件里报
> "之前能正常用的保存好的函数突然跑不了"，报错 `无法识别的语句 'STICKLINE(...), COLORFF0000':`
> `公式末尾存在无法解析的内容: ','`。**根因 = v6.4(P1) 引入的回归**：`_compile_draw` 把整条
> 绘图语句（含**尾部颜色属性**）直接喂给 `parse()`，而通达信本来就允许
> `STICKLINE(条件,价1,价2,宽,0), COLORFF0000;` —— 旧版整行 SKIP 所以"能用"，新版必炸；
> 且 `start_backtest` 依赖检测成功 ⇒ 函数**彻底跑不起来**（详见 §9-Q）。
> **修三处**：① 新增 `_split_draw_attrs()`，先按**顶层逗号**把尾部属性切出去再解析
> （与输出语句属性解析同源，不引入第二套文本解析）；
> ② 已知但未渲染的绘图函数（DRAWTEXT/DRAWBAND…）**不再硬报错**，登记成 `kind='unsupported'`，
> 由检测结果给出**非阻断提示** —— 既看得见、又不掐死存量函数；
> ③ `STICKLINE(状态, P, P, w, 0)`（价1==价2）原来会退化成 0 高度矩形 → **什么都看不见**，
> 改为画**横杠**（用户那 4 根彩色状态柱正是这个写法）。
> 已用**用户真实函数**（`samples/实例函数.txt`，L1=5/L2=20）端到端复验：解析通过、27 个变量、
> 2 线 + 4 色状态柱 + 1 图标、横杠有宽度。
> 断言：`smoke_chart.py` **134 项** + `smoke_backtest_overlay.py` **25 项** 全过。
> v6.6（**P4 落地：行情页公式叠加 + 内置/用户指标统一图层**）：
> 新增 `ui/dialogs/formula_overlay.py`(144 行) 公式编辑器（复用 `FunctionSegments`，
> 自检口径与回测页**同源**）；`ui/views/market.py` 新增「🧮 自定义公式」区（开关 + 编辑 + 状态），
> 并把 **MA/BOLL 与用户公式都翻成 `DrawData`** 交给**同一个** `OverlayPainter` ——
> §7-B3 D4「像 MA 一样显示用户函数」落地为**架构事实**，而不是给用户公式单开分支
> （连开关行为都与均线/布林带一致：同一个 `render_charts`）。
> 为消除跨页重复，抽出三件**共用件**：`core.utils.parse_params_text`、
> `core.utils.synthetic_bars`、`core.formula.program.probe_missing_parameters` ——
> 回测页与公式编辑器**共用同一份**，免得同一函数"行情页能跑、回测页缺参"（§11.5-11/12）。
> `smoke_backtest_overlay.py` **更名 `smoke_pages_overlay.py`**（现在也覆盖行情页）。
> 断言：`smoke_chart.py` **146 项** + `smoke_pages_overlay.py` **36 项** 全过；
> 全仓 compileall + 主窗口离屏构造通过。
> 下一步 = **P5 副图指标（附图）**。⚠ 注意 `market.py` 已到 425 行，**P5/P8 必须新建文件**。
> v6.7（**P5 前置 + 对话框返工 + 共用组件修 Bug** · 用户实测反馈驱动）：
> ① **`ui/widgets/function_segments.py` 两个真实 Bug**（回测页因在滚动容器里没显形，
>    对话框给出大高度才暴露）：
>    · **僵尸段** —— `set_texts()` 的 `removeWidget + deleteLater()` 只把控件移出布局，
>      在事件循环真正删除前它**仍是可见子控件** ⇒ 对话框里出现两个"函数段 1"（用户截图）；
>      修法：先 `setParent(None)` 立刻脱离父级，再 `deleteLater()`。
>    · **排版塌陷** —— 编辑框 `setFixedHeight(96)`，父级分到多余高度时**无人承接** ⇒
>      段标题被顶到最上、编辑框被推到几百像素之外（"标签与框之间一大片空白"）；
>      修法：`Expanding` 纵向策略 + 最小高度，让多余高度**变成编辑面积**。
> ② **公式对话框返工**（`ui/dialogs/formula_overlay.py` → 246 行）：去重复、补引导、收拾版面 ——
>    新增**目标窗格**（主图/副图）+ 随选动态说明 + **示例模板菜单**（主图/副图各一套，
>    "用例子教"比堆说明文字有效）；编辑区放进 `QScrollArea`，段多也不撑爆。
> ③ **支持副图叠加**（P5 前置）：`market.py` 在既有窗格体系上新增「**公式副图**」窗格
>    （`setXLink` 联动主图、**y 轴独立**、数值跨 0 时给零轴参考线）——
>    副图量级的函数（MACD/RSI/成交量）从此**不会再把 K 线压扁**。
> ④ **量级引导**：新增 `ui/widgets/chart_layers.py`(65 行，图层公共件) ——
>    `builtin_indicator_layers()`（内置指标→IR，页面不再自己换算）+
>    `layer_value_range()` + `scale_mismatch_hint()`（数值与股价差 3 倍或完全错位 →
>    在面板明说"建议改用副图"，完整解释进 tooltip）。
> 断言：`smoke_chart.py` **161 项** + `smoke_pages_overlay.py` **58 项** 全过；
> 全仓 compileall + 主窗口离屏构造通过。
> 下一步 = **P5 剩余部分**（多副图 / 内置指标也可放副图）→ P6 → P7 → P8。
> v6.8（**P5 落地：多副图 + 窗格编排收编 ChartHost**）：
> ① **每段可选目标窗格**（主图 / 副图 1/2/3）—— 对话框把"目标"下放到每个函数段右侧；
>    `FunctionSegments` 新增 `accessory_factory`（每段附件控件；回测页不传 ⇒ 行为零变化）。
> ② **引擎新增 `execute_programs_with_draws_grouped`**：draws **按函数段分组**返回 ——
>    各段必须共用**同一个变量池**（后段引用前段变量），所以"每段不同目标"只能
>    **一次求值、再按段归位**，绝不能按目标分组各跑一遍（那会把共享池打断）；
>    顺带把三个执行入口收敛到唯一的 `_run_programs` 循环（消除三处重复）。
> ③ **行情页窗格编排迁移到 ChartHost**（§10-12 的违规清账）：market.py 不再自己
>    `addPlot` 拼窗格；`ChartHost` 补齐 `fixed_height`（钉死附图 150px 旧观感）、
>    `bottom_axis_mode`（hide / no_values，行情页用后者保住轴线）、`clear_sub_panes()`、
>    `clear_pane_content()`（比 `PlotItem.clear()` 安全 —— 不会把十字光标一起删掉）、`setBackground()`。
> ④ **多副图按需创建**：有目标为副图的图层才建窗格；量能/振荡指标各占一格、互不压扁。
> ⑤ **内置副图内容搬出页面**：新增 `ui/widgets/indicator_panes.py`（成交量 / MACD 内容构建）。
>    ⚠ 量柱/MACD 仍是原生 `BarGraphItem`（逐柱配色是它的强项），**尚未**并入图层协议 ——
>    这是 P5 明确保留的例外（不为架构一致性去改两张已经好用的图，P8 统一）。
> 断言：`smoke_chart.py` **161 项** + `smoke_pages_overlay.py` **63 项** 全过
> （含"跨段共享变量池仍可用""窗格顺序/x 联动/y 独立""刻度值只在最下窗格"）。
> **market.py 463→448（首次下降）**。下一步 = **P6 标注持久化** → P7 → P8。
> v6.8 追记（收工前用户反馈 · **立项 §7-B4 坐标轴自适应**）：
> 行情数据中心放大看局部时间时，横轴只剩一个无意义刻度（截图中的"A"）、纵轴量程不随可视窗口
> 重算（副图整段历史被压成一条线）。经排查这是**全 App 普遍性缺陷**（§9-S：行情/复盘全是静态
> ticks，只有回测页手写对了），已立项 **§7-B4 主案 + 实施步骤** —— **P0–P8 步骤完成后立即做**，
> 方案与验收见 §7-B4。今日进度（P4/P5 前置/P5 + §9-Q/§9-R）已全部同步至 §4/§6/§7/§8/§9/§11。
> v6.9：**第三次"陌生代码"复查 + 小债清账**（本批为纯收尾，无新功能）：
> ① **§9-P1 净额残留收尾** —— 新增 `core/utils.record_net_amount(record)`（全站唯一
>    「净额 = 平仓盈亏 − 手续费」取值口径，容忍 Series/dict/TradeRecord + NaN），
>    并把**逐笔展示层**最后 4 处毛利判定改净额：流水页盈亏列（**着色 + 显示值，
>    列名改「净盈亏」**）、复盘页当日清单、复盘页详情头、回放标记色/高亮带方向。
>    至此 §5.3-B 的"全站再无例外"**首次真正成立**。
> ② **§10-9 控件契约收口** —— 新增 `custom_widgets` 的 **复合控件完整 QSS 生成器**
>    （`combo_qss` / `date_edit_qss` + 8 个具名常量）。清理 7 处业务页面就地半截样式。
>    ⚠ **实测发现一个长期隐形 Bug**：`QComboBox/QDateEdit` 一旦被写上 `::drop-down`
>    而**没有** `::down-arrow`，Qt 切到样式化绘制路径后**下拉箭头会整个消失** ——
>    复盘页时间选择器与手工录入弹窗此前正处于这种"没有箭头"状态。现统一用
>    **border 三角**绘制箭头（纯 QSS、零图片资源）。
> ③ **NoWheel 补齐**：新增 `NoWheelDateTimeEdit`；复盘页孤儿补录日期、手工录入弹窗的
>    日期/时间/数值控件全部换入 NoWheel 族（§6.6-U2 的遗漏项）。
> ④ **行情页接线跨窗格十字光标**（P2 早已具备、此前未接），竖线贯穿主图/量能/MACD/公式副图。
> ⑤ 断言：`smoke_chart.py` 161→**178 项**（+净额口径 6 项、+样式契约 11 项，含
>    "半截 QSS 看不到箭头"的**负向对照**）；`smoke_pages_overlay.py` **63 项** 全过；
>    全仓 compileall 通过。**§4 行数已按本次实测重刷**（口径 = 非空行）。
> v6.10：**红线收编 + P6 标注持久化落地**（承接 v6.9 的小债清账）：
> ① **§9-T4-① 收编（用户拍板"红线不放宽"）** —— `MarketSyncService` 新增
>    `fetch_index_constituents()`：**联网抓取的唯一入口**。`ConstituentsWorker`
>    不再 import 行情源（`ui/` 全层**零 `AkShareFeed`**，已用源码级断言守门）；
>    失败文案按"网络 / 行情源未收录"分类（§10-10）。连带修掉 §9-J 里
>    "docstring 与实现不符"的同源问题（worker 只调度，联网细节归门面）。
> ② **P6 用户标注持久化（管线 B）全部落地** ——
>    新增 `data/annotations.py`（255 行，零 Qt：对象模型 + `(标的,周期,id)` 原子写 CRUD +
>    `DateAxis` 日期↔序号映射）与 `ui/widgets/annotation_layer.py`（222 行：绘制 + 选中 +
>    **逐个独立删除**）。**坐标存日期不存序号** —— 数据窗口前移后标注仍锚在同一天
>    （有"前置 5 根 ⇒ 序号 +5 而非漂移"的页面级断言）。`ChartPane` 补 `remove_annotation()`
>    与 `add_annotation()` 成对（防"容器与真实图元不一致"）。行情页旧的"内存趋势线"
>    （§7-A1 欠账）**被吸收升级**：三类标注（趋势线/水平线/垂直线）+ 工具下拉 +
>    删除选中 + 清空（二次确认）+ Delete/Esc 快捷键 + 面板回执。
> ③ 断言：`smoke_chart.py` 178→**212 项**、`smoke_pages_overlay.py` 63→**84 项**，全过；
>    全仓 compileall 通过。§4 行数再次按实测重刷。
> v6.11：**P7 函数资产化 + 行情↔回测互送落地**：
> ① **配方库**（新增 `data/formula_store.py` 213 行，零 Qt）：把「函数段 + 参数 +
>    **每段的目标窗格**」存成有名字的**配方**，`~/.jian_data/formula_library.json` 原子写，
>    按 `name` upsert（同名即覆盖）、`touch/last_used` 记录使用、坏数据行容忍。
>    **与 `strategy_store` 同源但不合并**：配方只有公式；策略 = 配方 + 条件 + 风控 + 区间。
> ② **配方库窗口**（新增 `ui/widgets/formula_library.py` 171 行）：列表 + 预览 + 载入/改名/删除，
>    **两个页面共用同一个对话框**（不为某页写特例）。
> ③ **互送**（`main_window` 当唯一传话筒，两个页面**互不 import**）：
>    行情页「📤 送去做回测」→ 回测页①函数区（**目标窗格不随行**，回测没有副图概念）；
>    回测页「📤 送到行情页」→ 每段默认落主图并立即渲染；两边都会**自动切页 + 自动自检**。
> ④ **"公式重启就丢"治本**：行情页开机自动恢复 `last_used` 配方（面板明说来历），
>    另有「💾 存为配方…」入口（行情页存 **target**、回测页存 **文本**，来源分别标记）。
> ⑤ 断言：`smoke_chart.py` 212→**238 项**（配方模型 26 项，含"三种入参形态"与
>    "VALID_TARGETS 必须与编辑器下拉一致"的防漂移断言）、`smoke_pages_overlay.py`
>    84→**111 项**（互送链路 25 项 + **防污染自检 2 项**）；全仓 compileall 通过。
> ⑥ 一次性事故：P6 调试期某个中间版本在用户目录留下了一个**空** `annotations.json`
>    （`items: []`，零影响），已清理；并给页面级冒烟加了**"用户真实库未被创建/修改"**的
>    收尾自检 —— 以后测试再想污染用户数据，冒烟当场红。
> v6.12：**P8 行情页整体重做（换壳不换芯）落地**：
> ① **新页面 `ui/views/trading_desk.py`**（734 行，"行情工作台"）：旧 `ui/views/market.py`
>    **已删除**（661 行越线文件到此为止）。页面**只做装配与交互**，图表能力全部来自既有组件
>    （ChartHost / OverlayPainter / chart_layers / annotation_layer / indicator_panes /
>    formula_store / formula_library / workers+sync_service）—— 没有新增一行绘图逻辑。
> ② **自选股**（新增 `data/watchlist_store.py` 143 行，零 Qt）：增删/上下移/双击切换、
>    名称随花名册刷新、坏数据与重复项容忍；`~/.jian_data/watchlist.json` 原子写。
> ③ **周期切换 日/周/月**（`core/utils.resample_ohlcv` 纯函数）：由日线**就地聚合**
>    （开=首 高=max 低=min 收=末 量=和，**date 取该周期最后一根真实交易日**），
>    列名与日线完全一致 ⇒ K 线/指标/公式/标注**零改动**；指标在聚合**之后**才算
>    （否则"周线 MA5"会变成"日线 MA5 被抽样"）。
> ④ **画线工具栏 3 类 → 5 类**：新增**斐波那契**（两端点 + 7 档水平位 + 标签，
>    拖动重算）与**文字标注**（`_ClickableText` 子类 —— `pg.TextItem` 本身不发点击信号，
>    不补这个信号就**没法单独选中/删除**，管线 B 的"逐个独立删除"直接不成立）。
> ⑤ **标注按 `(标的, 周期)` 隔离**：新增 `data/annotations.period_key()` 规范化周期键，
>    存 `daily/weekly/monthly`；日线上画的线**不会**跑到周线上。斐波那契的附属图元
>    （水平位/标签）登记进 `_extras`，删/清/重建与主图元**同步**（§11.5-15 同类风险）。
> ⑥ 断言：`smoke_chart.py` 238→**278 项**（周期重采样 9 + 自选股 13 + 斐波那契/文字 18）；
>    `smoke_pages_overlay.py` 111→**132 项**（P8 页面级 21：自选/周期/隔离/新标注类型 +
>    防污染名单加 `watchlist.json`）；全仓 compileall 通过。
>    ⚠ **本轮踩到一次"脚本看起来卡死"**：详见 §11.5-20（离屏测试三条打桩铁律）——
>    根因是"模态框阻塞 + 管道缓冲 + 非守护线程不退出"三件事叠加，**与网络无关**。
> ⑦ **P8 仍待做（明确登记，非欠账）**：**复权切换**（需新增"不复权"数据湖分区 + 抓取链路，
>    涉及数据窗红线，单独一轮做）、**分钟周期**（§7 D3 远期）、斐波那契/文字的**锚点拖动**
>    （文字本轮为放置式）。修完这些再进 **§7-B4 坐标轴自适应**。
> v6.13：**P8 收尾完成（复权切换 + 文字标注拖动）**：
> ① **复权切换（前复权 / 不复权）** —— 关键决策：**不复权数据单独一个数据湖分区**
>    `kline_daily_raw`，而不是在原分区加一列。理由：前复权会随除权**整体漂移**、不复权不会，
>    两份数据永远无法互相推导；同文件只能二选一覆盖（切一次复权重下一遍全历史）。
>    链路：`akshare_feed` 两个源都按同一个 `adjust` 拉取（**不允许多源降级时静默换复权口径**）
>    → `sync_service.zone_for_adjust()`（UI 唯一入口）→ 工作台复权下拉；
>    「数据管理」新增「日线行情 (不复权)」分区并可单独同步/删除（两份互不影响）。
>    切到不复权若本地没有 → 自动转后台取数，面板立刻给"取数中"回执（**且不谎报根数**）。
> ② **文字标注可拖动** —— pyqtgraph 覆写了 `mouseMoveEvent` 且不调父类实现，所以
>    `ItemIsMovable` **不会**让图元真的跟着走：必须自己实现 `mousePressEvent`/`mouseDragEvent`
>    （按场景位移量改坐标），并**松手才落盘**（不在拖动途中疯狂写文件）；
>    拖动后坐标按 (日期, 价格) 回写，重启读回一致。
> ③ 断言：`smoke_chart.py` 278→**290 项**（复权链路 7 + 文字拖动 5）、
>    `smoke_pages_overlay.py` 132→**139 项**（页面级复权 7）；全仓 compileall 通过；
>    临时脚本/输出文件已清理，仓库无残留。
> ④ P8 **仅剩分钟周期**未做（`kline_min` 属 §7 D3 远期，非欠账）。下一步按用户指示进
>    **§7-B4 坐标轴自适应**。
> v6.14：**全仓文件归置（零业务代码变更）** —— 用户指出"根目录存在许多未分类文件"，
> 于是按新立的 **§10-13 文件归置规范** 把根目录收敛为"门面"：
> ① 新增三个**角色目录**（判据 = "谁去调用它"，不是"它像什么"）：
>    `scripts/`（运维脚本，**人工运行、不进 app import 图**）、`tests/`（验收脚本）、
>    `samples/`（私有样例，**是数据不是代码**）；
> ② 根目录**只留** `main.py` / `requirements.txt` / `version.json` / `JIAN_RULES.md` / `.gitignore`
>    + 四个层目录：`sync_roster.py`→`scripts/`、`smoke_*.py`→`tests/`、
>    `实例函数*.txt` + `实例交割单*.xlsx`→`samples/`；
> ③ **删除**根目录空壳 `screenshots/`（§9-C 早已记为"历史遗留"）与全仓 `__pycache__`；
> ④ 移动脚本**必须同批修路径引导** —— 子目录里 `sys.path[0]` 不再是仓库根，
>    一律改 `__file__` 反推，否则 `from data...` 当场 ImportError；
>    连带改掉**用户可见文案**（"花名册为空 —— 请先运行 `scripts/sync_roster.py`"）与 docstring；
> ⑤ `.gitignore` 收紧为 `samples/*` + `!samples/README.md`（**整目录忽略、只放行说明**，
>    比"逐个文件名模式"可靠得多）+ 新增 `_tmp_*` 兜底（会话期探针脚本/输出**不留仓**）；
> ⑥ 回归：`py tests/smoke_chart.py` **290 项** + `py tests/smoke_pages_overlay.py` **139 项** 全过，
>    全仓 compileall 通过；`git ls-files` 核对**无死代码**（所有 py 都被引用，唯二"零引用"的是
>    两个独立入口脚本，属正常）；`market.py` 的删除也在同批走 `git rm`。
> ⑦ 顺带登记待办（**暂缓**）：`ui/widgets/` 17 文件平铺，可再分 `chart/` 子包 —— 但涉 51 处
>    import 且牵动 §7-B3 四层表锚点，留待单独一轮。
> v6.15：**§7-B4 坐标轴自适应全部落地（全 App 收口）** —— 治的是用户截图反馈的两个普遍缺陷：
> ① **放大后横轴只剩一个刻度**；② **纵轴不随可视区自适应**（副图被全量极值压成一条线）。
> 根因：全 app 的 x 是"bar 序号 + 日期映射"，pyqtgraph 不知道这层映射；而正确实现只在回测页
> 写对过一次、**没抽成公共件** ⇒ "改一处漏三处"（§9-O7 同款）。
> ① 新建 **`ui/widgets/adaptive_axis.py`(348)** ＝全 app 唯一的刻度/量程来源：
>   纯函数 `compute_ticks`（日期格式梯子：日内 / 同年 / 跨年）/ `compute_text_ticks`（序数轴）/
>   `slice_span`（可视极值，NaN 忽略）；信号挂接 `attach_date_axis` / `follow_y` / `attach_all`
>   （**同轴重复 attach 自动替换旧跟随器**，页面每次重渲染无脑调）；
> ② **3 处静态 ticks 全部收敛**（行情主图/量/MACD/公式副图逐窗格、复盘 K线回放、复盘资金K线），
>   回测页两处也改为调用公共纯函数（「价格轴跟随可视区间」开关语义保留）；
> ③ 页面只回答"**该窗格在可视区间内的数值范围**"（provider 契约），公共件不猜业务；
> ④ 两条新知识已固化：**`ViewBox.setYRange` 会偷加自己的 padding**（一律 `padding=0`，§11.5-21）、
>   **复盘"资金 K 线"其实是"当月第几日"的序数轴**（套 `%m-%d` 会说谎 ⇒ 增设文本刻度入口，§9-v6.15）；
> ⑤ 断言：`smoke_chart` 290→**312**、`smoke_pages_overlay` 139→**150**，全过 + 全仓 compileall。
> v6.16：**A 类文档欠账清账（零业务代码变更）** —— 用户拍板两项：
> ① **§4 行数改为"只标 ≥400 行的文件"**（逐文件维护精确数字每次大改都要返工，且 v6.15 那批
>    实测已有 10+ 处漂移；行数的唯一用途是"提醒谁快越线"，<400 行不标即不漂）；
> ② **版本号统一跟随 git**（= 最近一次 push 的 commit message 首词，**每次 push 递增 0.01**）
>    —— `APP_VERSION` 与 `version.json` 已由 `1.4.1` 迁到 **1.20**，新纪律写进 §9-A；
>    顺带修掉 2 处"指向已删除 `market.py`"的**活引用**（`formula_overlay.py` / `backtest.py`）、
>    4 处历史叙事补注、文档 §11.4 两条活引用；§11.6 标题与全文旧行数一并校正。
> ℹ **`version.json` 的 `url` 已由占位符 `https://github.com` 改为项目 Releases 列表页**
> `https://github.com/ENDVEN/Jian/releases`（用户 2026-09-15 决定：软件尚未完工，
> **暂不制作 Release**，先用它占位；正式发版时再换成具体版本直链 —— 见 §9-A 末条）。
> v6.17：**§7-B5 立项「回测成交真实性」（零业务代码变更）** —— 用户提出三个痛点（指标滞后 /
> 开盘容量限制 / 盘中已出信号却要等次日成交），拍板四项：① **T+1 修正默认开启、不给旧口径开关**；
> ② **「当日收盘」成交档**要做；③ **触发式委托（条件单）**要做；④ **P1（账户+容量+成本）与
> P2（盘中即时成交）搁置**，设计留档待将来评估。
> 立项前用合成 K 线探针**实测 7 组**（跑完即删），坐实 5 条现状事实 ——
> 其中 **F3 是危险项：止损 / 止盈 / `max_bars` 能在买入当天就离场 ⇒ 按 A股 T+1 属违规，
> 方向是"低估风险"（带止损的策略回撤比真实好看）**；**F2 是"引擎根本没有仓位"**
> （`equity` 是 1 股复利、`shares` 恒为 1），所以"大仓位影响价格"当前无处安放。
> 产出 **§7-B5 主案**：P0 定稿规格（C1 三档成交时点 / C2 T+1 闸门 + "进场当根触发则顺延到
> 最早可卖根开盘价" / C3 触发价与跳空处理**与风控同源** / C4 唯一口径 / C5 **七条验收断言含
> 负向对照**）、影响面 5 条、实施步骤 0–5、P1/P2 挂起设计（含"明确不做"清单）。
> **下一步 = 步骤 1：引擎核心（`core/backtest.py`），等用户确认口径后开工。**
> v6.18：**§7-B5 P0 落地（步骤 1–3）+ 用户指引返工**。用户先追加拍板"**同根卖出后当根不再重建仓**"，
> 随后实测反馈"**触发式条件单 / 触发跳数 完完全全看不懂**"，于是这一批做了三件事：
> ① **引擎**（`core/backtest.py` 315→**495**）：三档成交时点 + **T+1 硬约束**（进场当根触发
>    价格型风控 → 顺延到最早可卖根**开盘价**成交；`max_bars` 顺延到次根收盘；末根不建仓）
>    + **同根不重建仓**（判据 = **成交根**重合，**不是**评估根 —— 写宽了会误杀"离场后下一根
>    的合法再入场"，实现时当场自我复查修窄）；
> ② **口径连通 + UI**：策略签名（**默认口径归并为空串** ⇒ 旧存档签名一字不变）/ 旧档回落 /
>    `_last_meta` / CSV 表头与「（T+1 顺延）」标记 / PNG 报告图；回测页新增「🎯 成交模型」行；
> ③ **用户指引**（用户反馈驱动）：文案换成"什么时候、按什么价、会发生什么"，术语翻译成
>    用户量纲（`1 跳 → 0.01 元`、`当根 → 当天`、"信号作废" → "就不买也不卖"）；新增
>    **行内实时说明**与**教学弹窗** `ui/dialogs/fill_model_help.py`（一套固定价格数字三档对照）；
>    参数只在有意义的档位出现。
> **纪律固化**：§10-10 追加条款（术语不许当唯一解释 + 教学入口三条硬要求）、§11.5-22（四步模板）、
> §11.7 自检加一栏。
> ④ **步长 Bug 修复（同批用户实测反馈）**："买卖价要多等"按一次上箭头就跳到 0.2 且无法继续
>    上调、没有过渡价格。根因**不是图标**：公共工厂 `_risk_spin` 把步长写死成
>    `1 if decimals == 0 else 0.5`，对 2 位小数的新控件等于"一按顶到上限"。
>    修为**随小数位自适应**（`0→1` / `1→0.5` / `≥2→10^-decimals`）并把上限放宽到 **1.00 元**；
>    教训固化为 **§11.5-23**（公共工厂里写死的经验默认值，换量纲就会咬人）。
> **验收**：四套一次性探针 —— C5 引擎 **39** / 口径+UI **22** / 文案可懂性 **24** / 数值控件步长 **12**
> （含**正则级行话检测**与**借 `git show HEAD` 旧引擎逐位回归**）全部通过；项目冒烟
> `smoke_chart` **312** + `smoke_pages_overlay` **150** 全过 + 全仓 compileall。
> ⑤ **步骤 4 验收**：断言移植进两个冒烟脚本（`smoke_chart` 312→**356**、`smoke_pages_overlay`
>    150→**180**）+ **用户真实资产端到端复跑**（两个私有公式解析/求值通过、27/17 变量、
>    各 7 条绘图指令；真实策略存档 2 套签名不变；三档 + 风控对照 49/41/33 笔、
>    T+1 顺延 10/0/10）—— 全部符合设计预期。
> **遗留 = 步骤 5（收尾回写）+ 用户手动实测手感。**
> v6.19：**提交发版 —— app 版本升到 `1.21`（零业务代码变更）**。§7-B5 回测成交真实性的
> 实现与验收已在 v6.17（立项）/ v6.18（落地）完成，本批只做**三处版本号同批同步**
> （§9-A 新纪律的**首次实跑**）：① commit message 首词 **`1.21 回测策略更新`**；
> ② `config/settings.py:APP_VERSION` = **`1.21`**；③ 仓库根 `version.json` = **`1.21`**
> （`notes` 换成这一轮的用户可见变更：三档成交时点 / T+1 / 同根不重建仓 / 口径入签名与导出 /
> 教学弹窗 / 步长修复）。**用户手动实测手感已通过**（原话"现阶段没问题"）
> ⇒ **§7-B5 步骤 0–5 全部收官**。
> ⚠ 本次**只提交、未 push**（push 才会触发老用户的「✨ 发现新版本」提示）。
> **下一轮 = `1.22`：回测页 UI 大改**（用户已定方向，方案待讨论 —— 见 §11.6）。
> v6.20：**`1.22` 回测页 UI 大改（样板 A 落地）+ §9-L 拆分第一批** —— 用户先点选
>   "**我更喜欢 a 的设计**"（我按「使用频率 × 视线停留」出了 4 套可交互样板，
>   见 `design/1.22-backtest-ui/index.html`），随后拍板三件事：**整体迁样板 A**、
>   **顺路拆大文件**、**打开页面记住上次**。
> ① **版式**：七区块平铺（结果区只剩 ~60px）→ **摘要条 + 右侧遮罩抽屉 + 结果吃满**，
>    离屏实测结果区 **625/867 ≈ 72%**；五张编辑卡片**默认不在页面上出现**，点胶囊才由
>    抽屉浮出来（**覆盖层、不进布局** ⇒ 打开配置时结果区高度**一个像素都不变**：
>    实测抽屉开关前后都是 625px）。中途先做过"就地展开（手风琴）"，用户实测拍板
>    "**就地展开虽然直观但不美观**" → **改回遮罩抽屉**（理由已固化进 §10-14 L3）；
> ② **拆出六块**（§9-L 收官）：`ui/views/backtest.py` 1697 → 1467 → 1156 → **787（-54%）**，
>    新增 `ui/widgets/` 六个模块 —— `backtest_panes.py`(430：五张卡片 + `EditDrawer` 抽屉 +
>    三个表单构件) / `backtest_summary_bar.py`(111) / `backtest_result.py`(462：KPI + K线控制 +
>    4 结果页签 + 全部图表渲染与坐标轴自适应) / `backtest_export.py`(128) /
>    `backtest_flow.py`(304：运行流程) / `backtest_strategy.py`(194：策略库桥接)；
>    统一约定 = **状态留在页面、行为搬进模块**（`self.page.*` 读写）；页面保留控件
>    **同名别名** + 私有状态 `property` 读口，既有 356+180 条断言零改动；
> ③ **两个真实 Bug（用户截图倒逼，见 §11.5-27）**：遮罩盖不住表格（**创建顺序**问题）、
>    抽屉内大片空白（`QStackedWidget` 拉满卡片导致内部伸缩布局散架）；
> ③ **记住上次**：`preferences` 新键 `backtest_ui={pane,last}`；
> ④ **纪律固化**：**§10-14 版式分层**（L0–L4 + "新配置一律进摘要条与抽屉卡片"
>    + 明令**禁止"就地展开"**）、§11.5-25（`isVisibleTo(ancestor)` 口径）、
>    §11.5-26（覆盖层几何必须自己算，且它才是"不挤主区"的正解）；
>    **§9-U 登记"其他页面未按 §10-14 收口"为长期待办**（用户 2026-09-16 明确要求
>    "写在 rules 里留着，将来再定什么时候做"）；`design/1.22-backtest-ui/` **入库**
>    作为长期设计参考（四套交互模型 + 分层表 + 取舍分析）。
> 验收：`smoke_chart` **356** + `smoke_pages_overlay` **180** 全过 + 全仓 compileall。
> **→ 下一轮候选**：① §9-L 第二批（结果区 / 流程 / 策略库，`backtest.py` 1467 → 目标 ≤900）；
> ② §9-U 其他页面收口（点名才做）；③ 分钟周期 / §7-B1·B2 / §9-O10。
> ✅ **已提交 `1.22`**（commit `1.22 UI重构 模块拆分`）：三处版本号同批同步 + `design/` 入库；
>   ✅ **已 push**（v6.21 开工前核对：`origin/main` 已是 `e0f409a` —— 本文件里"未 push"的旧记录已过期）。
> v6.21：**`1.23` 立项 = 行情工作台版式收口（§7-B6）+ STEP 0 落地**（零业务代码变更）。
>   用户 2026-09-16 看完 4 套样板后拍板：① **结构 = 样板 A（深色专业终端）的骨架**
>   （52px 图标轨 ↔ 320px 分页工具面板 + 周期/复权**分段控件** + 图表常驻**读数条**）；
>   ② **配色 = 样板 B（= 现状）**，**深色主题整体推迟**（用户原话："放到后面软件功能实现得
>   差不多了再统一去做"）；③ **画线工具移入左栏「✎ 标注」页**，工具行只留 ✎ 图标 + 条数 pill；
>   ④ **图标轨加折起按钮**；⑤ 工具行的主图/副图 chips **不是常驻全集，而是"最近使用优先"**
>   （规则见 §7-B6-D：**已启用项 ∪ 最近前 3**，取消勾选留灰态，溢出用 `＋` 完整菜单）。
>   样板存档：`design/1.23-trading-desk-ui/`（4 套可交互 HTML + 汇总页）。
>   **STEP 0 本批落地**：① 两个冒烟脚本开头**自设 UTF-8 stdout** —— 修掉"Windows GBK 控制台下
>   带 `↔`/`⇒` 的 print 抛 `UnicodeEncodeError` ⇒ 整段分节被跳过 + 假报失败"（修前 328/3、修后 356/0）；
>   ② `tests/smoke_pages_overlay.py` 新增**「行情工作台迁移护栏」分节**：把"迁移期不许改名/删除的
>   公共面"（32 个属性 + 6 项私有状态 + 附图高度常量 + 导航挂载点）写成**可执行断言**，
>   并把 `cb_period/cb_adjust/cmb_tool` 这 3 个"允许被替换"的旧入口单列出来，逼改的人显式处理。
>   断言数 180 → **184**（`smoke_chart` 仍 **356**）。
>   **下一步 = §7-B6 STEP 3**（页面顶部改 L1/L2 两行 + 分段控件接线）。
> v6.21 续：**STEP 1 / STEP 2 也已落地**（同一批，均为"公共件先行、页面未动"）：
>   ① **STEP 1 样式公共件**：`custom_widgets.py` 新增 **`SegmentedControl`**
>   （`segment_qss()` + `segment_button_qss(position)`，QButtonGroup 互斥、首尾圆角 + 中段分隔线
>   **成套生成**、`:checked` 自带底色/字色/加粗、单段与空段都不崩）；
>   `mini_label` / `hint_icon` / `TAB_QSS_ON·OFF` 从 `backtest_panes` **上收**（回测页改为
>   **再导出** ⇒ 既有 import 路径与 184 项页面断言**零改动**），并顺手删掉 `dashboard.py`
>   里**第二份复制**的「?」角标（§9-O7 类重复）。
>   ② **STEP 2 读数条升级**：`ChartHost` 新增 `set_readout_provider(fn)`
>   （宿主只转交 `(窗格, x, y)`，**不猜业务**；返回空/抛异常 ⇒ **静默回退**内置文案）
>   + `set_readout_visible(True)`（读数条可**常显**，不依赖十字光标开关）。
>   ③ **STEP 3a（分钟周期 = §7 D3 的数据链路）**：`core.utils` 周期体系扩分钟
>   （`5m·M5·5min·5分钟`都能认，**裸 `M` 仍是月线**；`resample_ohlcv` 对分钟**直通**）+
>   新浪分钟源 `fetch_a_share_minute`（**签名里没有 adjust** —— 实测它本就不生效）+
>   `ZONE_MIN`/`minute_key(600519@5m)`/`SingleSyncWorker(period=)`；
>   分钟**不做"已最新就跳过"**、空返回按真失败（与日线的同步语义**故意不同**）。
>   **开工前先做取数实测**（§7-B6-B2）：新浪固定回 1970 根 ⇒ 1m≈9 日 / 5m≈42 / 15m≈124 /
>   30m≈247 / 60m≈493；东财分钟接口被代理拦截 ⇒ 不接入；`adjust` 不生效 ⇒ 分钟只有真实价一种口径。
>   **验收：`smoke_chart` 383 → 408（+25）+ 真实联网端到端复跑（5m/1m/60m 三档落盘、二次同步 added=0）。**
>   ④ **STEP 3b（顶栏第 2 行）**：`cb_period`/`cb_adjust` 两个下拉被 **`SegmentedControl`** 取代
>   （左栏里那两组入口**同批删除**，只留"数据口径"回执 —— 免得两套入口"改一处漏一处"）；
>   一级 `日|周|月|分钟` + 二级 `1|5|15|30|60`（**只在"分钟"时出现**）+ 深度诚实提示；
>   分钟下**复权禁用 + 说明**（不是隐藏：藏起来用户会以为前复权也在生效）。
>   新增**取数唯一入口** `_data_zone_and_key()`，竞态守卫升级为**标的+分区+档位三者吻合**；
>   日 ↔ 周/月 **同源只重渲染（不联网）**；换源时先清空旧图（**绝不出现"分钟数据配日线标题"**）；
>   分钟档位落 `preferences.desk_ui` **记住上次**。
>   **验收：`smoke_pages_overlay` 184 → 202（+18，含"旧入口不该再存在"的反向断言）
>   + 离屏截图人眼核验两态（日线态二级收起 / 分钟态五档+深度+禁用说明）。**
>   ⑤ **STEP 3c（工具行 chips）**：新增纯函数件 **`ui/widgets/chip_mru.py`**（零 Qt：候选池 /
>   `push_recent` / `resolve_chips` = **已启用 ∪ 最近前 3** / `hidden_keys`）；主图/副图各 ≤3 个
>   chips + `＋ 更多` **完整候选池**菜单；**真源永远是既有复选框**（chip 只是投影），
>   并补 `_sync_chip_history()` —— **从左栏复选框打开也记进"最近使用"**（否则"关掉就再也
>   点不回来"）；"公式副图 N"的 chip 控制**该格显示**而不删用户的函数段。
>   **⚠ 同时踩到并修掉一个真问题**：`desk_ui` 会自动落盘 ⇒ 跑一次冒烟就把**用户真实
>   `preferences.json` 写脏**（全是测试的 chip 历史）。已①给脚本打桩 `Preferences.save`
>   （只写内存）②把 `preferences.json` 列入防污染自检 ③回滚被写脏的键；
>   教训写进 §11.7（"自动落盘的偏好必须打桩"）。
>   ⑥ **STEP 4（左栏改造）**：新增容器件 **`ui/widgets/desk_panel.py`**（纯容器零业务：
>   `IconRail` 互斥图标轨 + `DeskPanel` 五页空容器 + 折起按钮）；页面拆成 5 个 `_build_*_page()`，
>   16 个控件**搬家不重建**（对象与名字全不变 ⇒ 既有断言口径零改动）；
>   `cmb_tool` 下拉 → **`seg_tool` 分段控件**，**迁移期三处旧入口（`cb_period`/`cb_adjust`/`cmb_tool`）
>   至此全部清零**（护栏反向钉住"旧名不该存在"）；工具行只留 ✎ 入口 + "N 条标注"胶囊；
>   `panel_page` / `rail_collapsed` 记住上次。
>   **⚠ 实测抓到并修掉一个真 Bug**：`QSplitter` 记的是**绝对宽度**，子控件隐藏后不会主动让出宽度
>   ⇒ "面板看不见了、左边却留一块空白"（离屏实测折起前后同为 1203）。修法 = 折起时显式重排。
>   **验收：`smoke_pages_overlay` 213 → 225（+12，含"折起后图表宽度 691 → 1386"）
>   + 离屏截图三态人眼核验。**
>   **验收（STEP 0–4 阶段）：`smoke_chart` 356 → 423（+67）+ `smoke_pages_overlay` 180 → 225（+45）
>   + 真实联网端到端复跑 + 离屏截图人眼核验 + 全仓 compileall。**
> v6.22：**`1.23` 收官 = §7-B6 STEP 5 / STEP 6 + 发版**（行情工作台版式收口完整落地）。
>   ① **STEP 5（读数条接线 + 三处回执一行化）**：页面把读数 provider 接到**本次真正渲染的那一份
>   df**（`render_charts` 里定格 `_rendered_df`；**不现场重算** —— 否则悬停读数与图上的 K 线
>   可能不是同一份，§10-4）；文案 = 日期/开/高/低/收/量/MA5/MA20（列名与 `TAEngine.add_ma` 同源）；
>   `ChartHost` 新增 **`set_readout_text()`** —— 常驻读数**直接写文案**、不移动十字光标
>   （用 `update_crosshair` 会让两条线跳到最新一根，摘要不该有动画）；**只有主图给业务读数**，
>   副图 y 含义各异 ⇒ 交回宿主内置文案。复权 / 公式 / 标注三处回执改成**一行摘要**、
>   详解进 tooltip，并**保住既有文案口径**（单行时 `toolTip() == text()`、关键词
>   `除权`/`画线`/`根`/`真实成交价`/`交易日` 一个不少）。**`smoke_pages_overlay` 225 → 234。**
>   ② **STEP 6（拆 `trading_desk.py` + 版本收口）**：**1375 → 345（-75%）**，
>   行为分居 **9 个 `ui/widgets/desk_*.py`** —— `desk_layout`(361 版式装配) / `desk_data`(278 取数·
>   周期·复权) / `desk_layers`(219 渲染·坐标轴) / `desk_panel`(205 图标轨·分页·**折起控制**) /
>   `desk_chips`(161 最近使用 chips) / `desk_formula`(157 公式资产化) / `desk_annotations`(128 标注) /
>   `desk_watch`(67 自选) / `desk_readout`(52 读数条)；约定 = 1.22 同款
>   **"状态留页面、行为搬模块 + 同名薄壳"** ⇒ **成功判据 = 同一套断言零改动**
>   （`smoke_chart` 423 + `smoke_pages_overlay` 234 一个都没改）+ `compileall`；
>   另把"实现确实搬走了"与"页面 ≤500 行"写成**可执行断言**（`inspect.getsourcefile` 核对
>   15 个方法的归属 + 行数上限，**防止把薄壳写成空函数**）⇒ **234 → 251**。
>   **⚠ 收尾拍离屏截图时又抓到并修掉一个真 Bug**：截图肉眼可见"图表只占 45%"
>   （1911px 窗口实测展开态 `sizes=[969, 698]`），折起后左侧还留 ~270px 空白。根因 =
>   ① 从没设过 `stretchFactor` ⇒ `QSplitter` 的**富余宽度全被左栏吃掉**（这就是 §9-U 记的
>   "图表被让渡宽度"）；② 面板的 `qSmartMinSize` 会取 `minimumSizeHint`(343) ⇒ 只隐藏、
>   甚至只把 `minWidth` 归零都不管用。修法 = `setStretchFactor(0,0)/(1,1)` +
>   折起时**min/max 宽度同压 0**（展开还原）。**修后实测：展开 `[372,1295]`（图表 1283）、
>   折起 `[60,1607]`（图表 1595、面板 0），来回切幂等** ⇒ **251 → 255**（+4 条宽度不变量）。
>   常量（`SUB_PLOT_HEIGHT`/`RAIL_ITEMS`/`PERIOD_GROUPS`/`MINUTE_SEGMENTS`/`TOOL_SEGMENTS`/
>   `DESK_UI_KEY`）改由模块定义、**从 `trading_desk.py` 再导出**（既有 import 零改动，§11.7）。
>   ③ **发版三处同步（§9-A 首次用于本轮）**：commit message 首词 `1.23 …` +
>   `config/settings.py:APP_VERSION = "1.23"` + 仓库根 `version.json = 1.23`（`notes` 换成
>   用户可见变更：两行顶栏 / 可折起左栏 / 常驻读数条 / 分钟周期 / 画线工具进左栏 / 一行回执；
>   并明说"数据口径与计算结果完全不变"）。
>   **总验收：`smoke_chart` 423 + `smoke_pages_overlay` 255 全过（失败 0）+ 全仓 compileall +
>   9 个新模块均可独立导入。**
> v6.23：**用户实测反馈驱动的两项修复（复权可读性 + 数据源脏数据护栏）**。
>   用户报两件事：① "转增股开不开复权看着没啥变化"；② "7/26~30 这种大面积显示错误经常出现"。
>   **核对结论（只读探针，跑完即删）**：① 是**定义**不是 Bug —— 前复权的差异只在除权日之前
>   （实测 600326 最近 750 根两份**逐根差 0.000%**，差异全在 2023-08-08 转增日之前）；
>   ② 用户截图里的 000880 是**真的连板**（7/26、7/29、7/30 三天一字板 + 后两天继续涨停，
>   独立源逐根一致）—— 但**同批体检全湖时挖出一个真 Bug**（见下）。
>   ① **复权回执加「差异定位」**（A 小改）：`前复权 · 4041 根 · ⚠ 除权跳空最近一次 2023-08-08（-24.8%）`
>   （比对两份数据、取**比例发生变化**的那天 = 除权日；按 symbol 缓存），tooltip 讲清
>   "前复权以最新价为锚、只改除权日之前" —— 不然每个用户都会以为开关坏了。
>   ② **读数条加两项纯数据事实**：**涨跌幅**（相对前收）+ **「一字」标签**（最高==最低）。
>   实机效果：`2024-07-30 开 6.78 高 6.78 低 6.78 收 6.78 量 23,938,437 +9.89% 一字`
>   —— 用户一眼确认"是数据本身"。⚠ **不**按涨跌停规则猜标签（主板 10%/创业板 20%/ST 5%
>   各不相同，按规则推会给出会骗人的标签）。
>   ③ **⚠ 真 Bug（§9-V-2）：兜底源静默换口径 ⇒ 脏文件**。本机 `kline_daily/300750` 里
>   **混进了兜底源（东财）数据**：成交量正好是新浪源的 **1/100**（788 vs 78,829 —— 东财单位是
>   "手"、新浪是"股"）、出现 `close=-0.68` 的**负价**、2018-10-15 有 **+24.7%** 单日涨幅
>   （当时创业板不可能）。**后果** = 量能副图 100× 台阶 + 一根负价把主图 y 轴拽到负数区
>   ⇒ **整张图被压扁**，正是用户说的"大面积显示错误"。
>   **修法（入口 + 界面两层）**：新增 `drop_unusable_price_rows()`（非正价/缺价行丢弃并
>   `logger.warning` 出声）+ `em_volume_to_shares()`（兜底源出口把「手」换算成「股」）；
>   界面新增 **`DeskData._data_health_note()` 数据体检**：命中就把
>   `⚠ 疑似数据异常：非正价 1 根、量能接缝 2026-04-20（×81）` 写进口径回执并点名
>   「🗄 数据管理 → **重新全量下载**」（增量修不回历史）。**判据要精确**：量纲接缝用
>   **前后 20 根的中位量**比（≥50×）⇒ "复牌/涨停放量"这类单日跳变自动排除（2015 那批
>   复牌股实测零误报）。
>   **验收：`smoke_chart` 423 → 437（+14）+ `smoke_pages_overlay` 255 → 261（+6）+ 全仓
>   compileall + 真实数据复验**（300750 回执真的报出"非正价 1 根 · 量能接缝 ×81"；
>   000880 读数真的显示 `+9.89% 一字`）。**该文件的修复动作 = 用户点一次「重新全量下载」。**
>   ④ **同日二次反馈（600326 · 2025-07-21 起"一字板显示问题"）⇒ 又抓到一个真缺陷（§9-V-3）**：
>   数据仍然没问题（与现拉源**逐根一致**：四个连续一字板 10.54/11.59/12.75/14.03 + 07-25 的
>   T 字板 15.43；量能指纹也对：一字板日仅 3.7M~9.9M 股、07-24 放量 99.6M、07-25 巨量 608M
>   —— 11 天 +118% 的真连板）。**问题在画法**：一字板实体高度 0 ⇒ 老画法只剩 **1px 细横线**
>   ⇒ 4px/根 缩放下几乎看不见，一串一字板看着像"K 线断口"。
>   修法 = `CandlestickItem` 对零高度实体（开=收，覆盖一字板/T 字板）改画
>   **加粗横档 2.5px**（笔宽**不随缩放变化**⇒ 任何缩放都看得见），一字板无影线就不画。
>   **只改画法、不动数据**。验收：离屏像素断言 **8 → 30 个非白像素**（1px 参考线的 3.75 倍），
>   普通 K 线不受影响；对比图 `screenshots/1.23-steps5-6/{e,f,g}`。
>   **总验收（v6.23 全部）：`smoke_chart` 423 → 444（+21）+ `smoke_pages_overlay` 255 → 261（+6）**
>   **+ 全仓 compileall + 真实数据/真实像素双复验。**
> v6.24：**`1.24` 立项 = 行情工作台「侧边栏重设计」（§7-B8）· 零业务代码变更（只动 `design/`）**。
>   承接 §7-B7（用户 2026-09-17 指定"侧边栏展开后的 UI 排版混乱、不友好"）：
>   ① 先出 **4 套可交互侧边栏样板**（`design/1.24-desk-sidebar-ui/`：手风琴卡片 / 主从下钻 /
>   状态优先 / 紧凑设置表单，共用同一外壳与同一段示意行情 ⇒ 差异只在面板内部，便于对照）；
>   **用户 2026-09-17 拍板采纳 A『手风琴卡片』**。
>   ② 用户对**现有功能**同时提出一批改进要求，并把前提定死为
>   **"对于现有里面的功能我们一起改好后再做"**（**功能先行、版式收尾**）⇒ 归纳为
>   **R1–R12**（见 §7-B8 主案）；本轮按此改出 **A v2**（`a2-accordion.html`）作方向确认件。
>   ③ **顺手核实出 2 处真实缺陷（代码级，非审美）**：**副图不能调顺序**
>   （`chart_host.py` 只有 `add_pane` 追加/`remove_pane`，**没有 `move_pane`**；
>   `desk_layers.render_charts()` 还按硬编码顺序创建）、**副图纵轴缩进不一致**
>   （`chart_style.style_axis()` 只设画笔与文字色、**从不设轴宽度**，
>   而 `AxisItem` 宽度随自身刻度文本长度变化 ⇒ `3,120,400` 与 `-0.05` 左槽不同宽、左边缘必错位）。
>   ⇒ 登记为 **R7 / R8**，属 §9 系列性质（能力缺口 / 渲染不一致）。
>   ④ **与既有远期项的一次合并**：用户想新建的同级栏目「**组合配置**」（分组成分设权重 + 组合回测 +
>   与左栏分组标签互通）= §6.5「远期」早已登记的**组合级多标的引擎**（"全新模块，勿并入 M1"）
>   ⇒ 作为其具体化，**不再新开平行债**（R4）。
> v6.25：**§7-B8 R11「画线类型 30+」补齐 —— 目录里 22 种「已登记未实现」全部落地（5 → 27 种）**。
>   起因 = 用户一句"完成市场行情中画线工具里待实现的部分"：v6.24 只把**目录**做全了
>   （32 种 / 6 类 / 无搜索栏），能画的却仍只有 v6.12 那 5 种，其余点了只回一句"还没实现"。
>   ① **模型层**（`data/annotations.py`）：新增 22 个 `KIND_*` + `KIND_LABELS` +
>     `REQUIRED_POINTS` + 语义档位常量（`FIB_EXT_RATIOS` / `FIB_FAN_RATIOS` /
>     `FIB_TIME_RATIOS` / `PERCENT_RATIOS`）；`fib_levels()` 抽出 `ratios` 参数 ⇒
>     斐波扩展 / 百分比线**复用同一套插值**（不许各写一份）；文字口径三态
>     （`requires_text` 用户填 / `AUTO_TEXT_KINDS` 由价位派生 / `MARK_GLYPHS` 符号）。
>   ② **画法层（新增三个文件，别再往交互层堆）**：
>     · `ui/widgets/annotation_items.py`(180) —— **与类型无关**的图元小件：
>       `_ClickableText`（v6.13 沿用）/ 新增 **`_RegionBand`**（`LinearRegionItem`
>       **不发点击信号** ⇒ 不补信号就没法单独删除，与文字子类同源的坑）+ 箭头头
>       （⚠ 角度按**场景坐标**算：x 是"第几根"、y 是"多少钱"，用数据坐标算会画歪）
>       + 档位虚线 / 标签 / `highlight()` 高亮**唯一分发口**（文字改字色 / 区间带改填充 / 其余改画笔）；
>     · `ui/widgets/annotation_decos.py`(143) —— **附属图元**（档位 / 标签 / 箭头 / 周期竖线 /
>       测量文案），共同点 = **都由主图元派生**（主图元一动就按新锚点重建）；
>     · `ui/widgets/annotation_shapes.py`(369) —— **规格表**：`ShapeSpec` 一种类型一行
>       （①锚点数 ②画法 ③回读 ④默认落点 ⑤是否重建附属图元），`SHAPES` / `DRAWABLE_KINDS`
>       是"能画哪些"的**唯一事实来源**（与目录 `implemented` 有**双向断言**钉着）。
>   ③ **交互层**（`annotation_layer.py` **418 → 290 行**）：从此**不认识任何具体画法** ——
>     只准备 `DrawCtx` 交给规格表，管存/删/选中；`_rebuild` 从"斐波专用"升级为通用
>     "重建附属图元"；新增 `snap` 钩子（水平射线被拖斜 ⇒ 存成水平、图上也回正）。
>     ⚠ 坑：**回正的判据必须读图元当前手柄**，读存储值永远"看起来已经水平"（§11.5-34 同族）。
>   ④ **仍不做 5 种**（回归通道 / 甘氏扇形线 / 波浪 / 头肩形态 / 斐波弧）—— 它们依赖
>     **回归或形态识别算法**、或角度随窗口比例变化，不是"画法"问题 ⇒ 目录照常展示、
>     点它明说"还没实现 + 登记在哪一类"，**不静默**（§9-Q 纪律）。卡片抬头也改成
>     「**27 种可画 · 共 32 种**」（不许含糊，更不许假装都能画）。
>   ⑤ 验收：`smoke_chart` 536 → **552**（+16：四件套齐全 / 锚点数逐位一致 / 档位常量 /
>     文字口径 / **27 种逐个"拖完写回存储"** / 矩形·价格带·交叉线·通道·箭头回读 / 水平射线回正）；
>     `smoke_pages_overlay` 362 → **367**（+5：27 种逐个"选→加→画→删"、附属图元全进容器、
>     价格标签文字随价位变、价格带回读）+ 全仓 compileall + **离屏截图人眼核验**（临时探针跑完即删）。
>     ⚠ 顺手修掉一条**假样本**：冒烟拿 `arrow` 当"未知类型"，它现在是真类型 ⇒ 换成永不实现的
>     假名（**别拿目录里的真类型当负向样本**，下次扩容还会被打脸）。
> v6.26：**§7-B9 画线交互重做（"在图上点出来"）+ 通道修正 + 32 种全通 + H-7 第二轮实测修复** ——
>   详见 §7-B9（C/H/H-7）、§8 该行、§11.5-41~46。
> v6.27：**H-8 第三轮实测修复** —— ① **预览图元吃掉点击**（"怎么点都画不出来"）⇒ 预览一律不吃鼠标；
>   ② 扇形按"屏幕 45°"画 ⇒ 所有射线冲出画面 ⇒ 基准改**可视区间对角线**、上下各 4 条。
>   详见 §7-B9「H-8」、§11.5-47/48。
> v6.28：**H-9 第四轮实测修复 —— 画线工具"能操作"三连真 bug + 甘氏扇形重做（两点版）**。
>   起因 = 用户："扇形线完全无法使用（写上去无法移动、自动上下对称无法调整），
>   **综合浏览整个画线工具看看到底出什么问题**" ⇒ 综合复查（真实鼠标事件 + 探针）挖出：
>   ① **文字/标记类"拖不动 + 点不中"**：`_ClickableText` 接受 press 后 pyqtgraph **不发**
>     `mouseClickEvent`/`mouseDragEvent`（既定的"谁接受 press 谁自己处理"契约）⇒ 那两个方法
>     **一次都没跑过**；旧断言用**手搓假事件**直接调 `mouseDragEvent` ⇒ 三个版本一直绿
>     （§11.5-49/50）⇒ 改走 **Qt 三件套**（press/move/release），"没移动 = 点击选中、
>     移动过 = 松手落盘"，位移换算到父坐标系、拖动阈值用像素；
>   ② **ROI 类点线身选不中**（`pg.ROI` 默认 `acceptedMouseButtons=NoButton` ⇒ `sigClicked`
>     永不发；`PolyLineROI` 线段是子图元；附属图元无人挂点击）⇒ `connect()` 统一声明左键 +
>     新增 **`_pick()` 按形状拾取**（"点标注任何一处都算选中它"，§11.5-51）；
>   ③ **甘氏扇形重做**：一点版只能让程序猜角度（用户无输入口）⇒ **两点版**（起点 + 参考点，
>     参考线就是 **1×1**）：方向随用户拖（**不再自动对称**）、陡缓随用户拖，9 条经典倍率 +
>     `gann_label()`；`view_dependent` 只剩斐波弧（§11.5-52）。
>   验收：`smoke_chart` 574 → **577**（新增**32 种逐个真实鼠标"拖得动 + 点得中"扫描**）、
>   `smoke_pages_overlay` **372**、全仓 compileall、离屏截图人眼核验。
> v6.29：**H-10 第五轮 —— 斐波扇形加 1×8 线 + 回归通道改"三端"（起点/终点/通道区间，带宽可拖）
>   + 自查自改（点选容差按像素、回归区间夹边界、落库规范化改纯计算）**。详见 §7-B9「H-10」、
>   §11.5-53~56；`smoke_chart` **589** / `smoke_pages_overlay` **372**。
> v6.30：**发版 `1.26` = §9-U 其余页面版式收口 + §9-L 剩余拆分**（在既有暂存改动
>   `ui/widgets/review_layout.py` + `core/preferences.review_ui` 基础上接着收官）。
>   ① **§9-U 复盘页收口**：宏观(日历·图表)/微观(清单·编辑) 从**写死的 5:4 拉伸比**改为
>   **可拖竖向分栏**（`macro_micro_splitter`，子项不可折叠到 0）+ 高度**记住上次**
>   （偏好键 `review_ui.v_sizes`，拖动后 300ms 防抖落盘）；主体显式 `stretch=1`（§11.5-13）。
>   常驻行 ≤3（标题行 + 筛选行 + 分栏主体吃满）；`data_manager`/`dashboard` 核对后**无需改造**。
>   ② **§9-L 剩余拆分**：`ui/views/review.py` **1067 → 173**（行为分居 5 个
>   `ui/widgets/review_*.py`：版式 / 图表 / 回放 / 编辑 / 流程）；`data/data_feed.py`
>   **827 → 365**（拆成 `feed_cells`/`feed_fifo`/`feed_report`/`feed_months` 四个纯函数模块，
>   公共名再导出 ⇒ 旧 import 零改动）。至此 **`ui/views/` 全部回到 400 线内**。
>   ③ **验收**：`smoke_chart` **589** + `smoke_pages_overlay` **372 → 396**（+24：复盘页迁移护栏 +
>   §9-U 分栏不变量）+ 全仓 compileall；`data_feed` 拆分另跑**临时逐位回归探针**（`git show HEAD`
>   旧实现 vs 新实现：标量工具 / FIFO / 月份推断 / 漏月检测，**0 差异**，跑完即删）。
>   **发版三处同批同步（§9-A）**：commit 首词 `1.26 …` + `config/settings.py:APP_VERSION = "1.26"`
>   + 仓库根 `version.json = 1.26`（`notes` = 自 `1.24` 以来的用户可见变更）。
> 状态图例：`[x]` 完成 · `[~]` 部分/半成品 · `[ ]` 未开始/占位。

---

---


> v6.32：**文档分层重构（零业务代码）** —— 用户问"rules 太庞大，每次只能检索读取，会不会有影响"。
>   诚实回答：**不影响"能不能干活"，但影响"干得对不对"，且是静默的**。本文件当时 **297k 字符**，
>   而单次读取上限 **100k** ⇒ 物理上读不完 ⇒ ① 猜不中关键词就检索不到（本次为拼出 §9-A 的版本规则搜了 4 轮）；
>   ② **同一数字十几处重复且互相矛盾**（文件头写"1.25 收尾 / APP_VERSION 未动"，§9-A 写"当前值 1.23"，
>   代码是 1.25）⇒ **已真实造成一次版本号事故**；③ 历史行与现状行口气一样，无法可靠区分。
>   ⇒ **根因不是长度，而是"现状与历史混在一起 + 同一数字多处重复"。**
>   做法 = **拆四册 + 立三条防漂移纪律**：主文件 **297k → 84k（< 100k，从此可一次读完）**；
>   ① 文件头 661 行版本叙事 + §8 全表 → `docs/JIAN_HISTORY.md`；
>   ② 6 份已完成主案（§7-B3…B9）+ §9 版本子节 + §11.6 历史刷新块 → `docs/JIAN_ARCHIVE.md`；
>   ③ §11.5 踩坑清单（50 条）→ `docs/JIAN_PLAYBOOK.md`（主文件只留**编号索引**）；
>   ④ 文件头换成 **「当前状态快照 + 文档地图 + 三条纪律」**（并新增"断点"一行，收工必填）；
>   ⑤ 新增 **§10-14**（文档分层与防漂移，硬指标 < 100k / 同一事实只存一处 / 历史行数字冻结）；
>   ⑥ §10-13 根目录白名单新增 **`docs/`** 角色目录（同批更新 §4 目录地图）。
>   **编号全部保持不变**（`§11.5-25`、`§7-B6`、`§9-V` 引用照旧成立，只是全文在归档文件里）。
>   **逐行无损校验通过：非预期缺失 0 行**（唯一"缺失"是标题行本身被 v6.32 替换）。

---

## 8. 版本演进备忘（压缩 changelog）

| 版本 | 里程碑 | 一句话备注 |
|---|---|---|
| v6.65(文档v6.65) | **复权自愈 + 字体版权收口（`1.47`）** | 用户实测："我无论怎么切换前复权还是不复权，图表始终没有变化 ⇒ 严重怀疑复权功能是损坏的"。**探针实测三件事**：(a) **UI 链路本身正确**（`_reload_for_current_view` 真换分区 `kline_daily` ⇄ `kline_daily_raw`、清屏、回执）—— 不是"开关没接线"；(b) 本地 `kline_daily/300803.parquet` 是**混合体**：9/18=82.00（旧口径）× 9/21=56.86（新口径），而**源端 qfq 连续**（56.55 → 56.86）⇒ 用户看到的"跳空缺口"是**假跳空**（数据错，不是除权）；(c) `kline_daily_raw/300803` **从未下载** ⇒ 切过去只有空图 + 现取（失败时回执还永远挂着"正在取"）。**根因** = `data/sync_service.refresh_one` 增量起点只取「本地末日 + 1」= **零重叠**（注释却写"多留一点重叠"——文档撒谎）⇒ **复权因子一变，已落盘历史永远不重算**。**落地**：① `QFQ_OVERLAP_DAYS = 10` 真重叠窗口 + `_qfq_factor_drifted()`（重叠日收盘价差 >0.5% ⇒ 判定因子变了）⇒ **当场整段重拉**（只多一次请求）并回 `rescaled`，回执说明"已重算整段前复权历史"；只在 qfq 分区判定。② `DeskData._price_seam()`：体检补第三类"物理上不可能"——单日收盘涨跌 ≥25%（创业板上限 20%）⇒ 点名"价格接缝 …（疑似除权后历史未重算）· 建议重新全量下载"（旧版只查量能接缝 ⇒ 只能靠肉眼怀疑）。③ `_on_sync_finished` 失败/空数据两条路调 `_refresh_adjust_hint(failed=True)` ⇒ 回执退出"正在取数"态改说"取数失败"；`_adjust_diff_note` 在另一份口径未下载时明说"差异暂时比不了"。④ **§10-15 落到代码**：新增 `ui_painter_font(pt)`（磅值版，只换字体族 ⇒ 零版式漂移），清掉全仓最后 **14 处**专有字体名（`Consolas`×5 / `Microsoft YaHei`×5 / `Arial`×4），并加源码级断言扫全 `ui/` 禁止再进 QSS/QFont。断言 `smoke_pages_overlay` **682 → 686**、`smoke_chart` **855 → 864**。坑 = **§11.5-91**。⚠ 已污染的 300803 需一次「🗄 数据管理 → 重新全量下载」修回；自动自愈覆盖**下一次**除权。 |
| v6.64(文档v6.64) | **§7-B12 P8 续 · 三条用户实测反馈（并入待推的 `1.46`）** | ① **撤「📤 送行情页」于 M2/M3**（用户原话："m2/m3 是**统计学层面**的回测、不是针对个股的，放一个完全没必要"）：`history_kinds._ACTIONS_SCAN` 去 `ACC_SEND`（按钮不再出现）+ `_on_send_to_market` **动作层自校验 kind**（诚实回执"统计口径…不针对个股"）+ `_formula_payload` 收回 M1-only —— **撤销 P8 早先"公式完整即顺手开放"**。② **字体版权**（用户先要求"连字体一起照抄样板"、随即**自行撤销**："想到字体还有版权问题"）⇒ 新建 **§10-15 字体版权纪律** + `custom_widgets.UI_FONT_STACK`/`UI_MONO_STACK`/`apply_ui_font()`/`ui_font_status()` = 全站字体**唯一出口**（逐个注明 OFL / 免费商用；`backtest_history_ui._CODE_QSS` 的 Consolas 已换）；**只"点菜"不"捆绑"** —— 不随包分发字体，本机一款都没装则回落系统默认（**不报错、不显方框**），装「思源黑体 / Noto Sans CJK SC」即自动还原；启动日志诚实报告命中与否。③ **M1 排版**：净值图按样板 `.minichart` 包「白底 + 浅边框 + 圆角」卡片（内含下边距 10px，曲线/坐标轴不再贴卡片边；⚠ M2/M3 必须**连卡片一起收**，否则右栏留空边框）+ 预览内容区段间距 **8 → 12px**。断言 `smoke_pages_overlay` **681 → 682**（动作集/送行情两条**就地换写**、字体栈**新增 1 条**）、`smoke_chart` **855** 不变。遗留：另 5 处 QSS 仍写专有 `Consolas`（→ §7-B12 末条 `[ ]`）。 |
| v6.63(文档v6.63) | **§7-B12 P8：🗂 运行历史按样板重做 + M1 一并美化（并入待推的 `1.46`）** | 用户实测反馈："m2/m3 部分我**直接盲目照抄 m1** 的导致那部分简直是灾难"（右侧信息栏无效内容 / 列表数据 / UI 都乱，且**右侧展开栏展开时遮挡历史列表**）；并要求"把 M1 也按你样式里的设计语言美化"。**定性 = 结构性问题**（kind 差异散在 6 处 `if kind` + 形状错配），不是零散 bug。**落地**：① 新 `ui/widgets/history_kinds.py` **声明式注册表**（`ColumnSpec`/`KindSpec`：列 / 过滤轴 / 可用动作，一行一种 kind）⇒ 列表·预览·动作层的 `if kind` 归零；② 新 `ScanStatBar`（**四态占比条**：命中/未命中/数据不足/被粗筛剔除，段宽按只数、太窄不写字进 tooltip）+ `SectionCard`（带标题小节）+ 条件**全文代码块**（等宽深底、可复制）+ 粗筛摘要；M2/M3 **不放净值图**（旧版留 132px 空白）；M1 保留净值图但统一版式（卡片 / 深底代码块 / 换行动作行）；③ `data/backtest_archive.py`：scan 记录**显式分组** `scope`/`condition`/`counts` + `day`/`range_start`（**不再借 M1 的 `total_trades`/`win_rate` 键** —— 那套"一套键两种含义"会让任何按 win_rate 的汇总混进横截面数据）；`_entry_of` **规范化索引项**（三种 kind 同形 ⇒ 列表层不再 if kind）；`filter_options()` 取代 M1-only 的 `symbols()`（旧版 M2/M3 的"标的"下拉永远只有一个"全部"）；`_evict` 分组改用 `scope`（旧版按 `(kind,'')` ⇒ **所有范围共用 20 份配额**）；④ **范围名带指数**：`readiness_flow.scope_snapshot()`（`沪深300 · 300只` + `sh000300`）+ `mark_archived()` 给自动存档补**可见回执**（旧版纯静默）；⑤ 修三处真 bug：**重跑**对「指数成分」必空跑（`apply_config` 异步名单 + `start_scan` 连招 ⇒ `_symbols` 空早退）⇒ 新增 `request_run()`（`ScanFlow`/`BreadthFlow` 置待跑标记，等**名单 + 体检**都就绪再扫；`ReadinessFlow` 加 `on_probe_done` 回调）· 动作层**无 kind 守卫**（只靠按钮灰）⇒ 动作入口自校验 · 自动存档静默；⑥ 版式：预览栏最小宽 **612 → 220**（旧版 6 按钮挤一行不可换行，实测 `minimumSizeHint().width()==612` ⇒ 分割器把宽度给右栏、左列表被裁 = 用户说的"遮挡"）⇒ 动作行走 `FlowHost` 自动换行 + 删除隔离到最右 + 左右最小宽 + `setChildrenCollapsible(False)`；顺手修 `FlowLayout` **不跳过隐藏子件**（标准 `QBoxLayout` 靠 `isEmpty()` 跳过，自定义布局漏了 ⇒ 隐藏按钮照样占位、撑出空档）；⑦ 搜索防抖 220ms + 空态引导 + 过滤轴标题随 kind（旧版 M2/M3 仍写"按标的/策略搜索"）。**样板** `design/1.46-run-history-ui/index.html`（可交互：切 kind / 拖宽度看换行）。**旧 M2/M3 存档已按用户拍板直接删除（实删 4 份）、不做兼容**。**验收**：`smoke_pages_overlay` **671 → 681**（+10：范围带指数 / 去 M1 键污染 / 注册表双向 / 过滤轴换词 / 四态占比条 / 动作集 / 条件全文 / 动作层守卫 / 送行情可送 / 重跑走 request_run）、`smoke_chart` **855**（改 1 条：`RESERVED_KINDS` 退役）+ 全仓 compileall。坑 = §11.5-89。 |
| v6.62(文档v6.62) | **M2/M3 两处体验缺陷修复：更新覆盖结果区 + 历史不足说清楚（并入待推的 `1.46` 批次）** | 用户 2026-09-25 实测两条体验反馈。**① 「⬆ 更新到最新」后页面卡在"更新中…"** —— 根因：`ui/widgets/readiness_flow.py` 的 `_launch_sync` **无条件** `_result.set_empty('更新中…')`（`set_empty` 内含 `empty_box.show()+table.hide()`）⇒ 把**已有扫描结果**藏起来（KPI 仍是旧的、表格被隐藏，用户截图正是此态）；而下载结束后的 `_on_fill_finished → start() → _render_readiness` 走的是"**已有结果**"分支，该分支只换「正在体检」占位（`if '正在体检' in lbl_empty.text()`），**不会换回"更新中…"** ⇒ **永久卡住**（下载早已完成、底部下载条显示"已完成"）。修法 = **入口侧判状态**：`_launch_sync` 只在 `p._outcome is None` 时才 `set_empty` 占位；**有结果就不碰结果区**（进度归回执/进度条/下载条 —— §7-B11 进度真源在 hub），并在 `_on_fill_finished` 明说"数据已更新，结果区仍是旧数据的扫描结果 —— 点「▶ 开始扫描」可刷新"（不自动重扫、不假装已刷新）。**② 沪深300 每次都有 1 只「历史不足」、更新后依旧** —— 经本地探针定位 = `001280 中国铀业`（**199 个交易日**，`min_bars` 默认 250），且本地末日 = **2026-09-24（已是最新）** ⇒ 属 `partial`（**次新股、上市时间短**，数据源没有更早行情），**不是 bug、更新也补不齐**。按 §10-10（关键概念不能只给 tooltip）新增 `ReadinessFlow._partial_note()`：空态**看得见地**点名标的 + 行数 + "多为新股/下载起点偏晚 —— 数据源没有更早行情、更新也补不齐（不是缺陷）" + 出路"调小「🎚 粗筛 → 上市 ≥」"；同时进 `need_action` 与"无缺口"两条渲染分支。**坑 = §11.5-88**（临时占位必须有"能把它换回来的主人"；用户看不懂的数字要给看得见的第一手解释）。**验收**：`smoke_pages_overlay` **667 → 671**（+3 已过扫描时更新不覆盖结果区 + 完成回执明说旧数据 +1 历史不足可见说明）、`smoke_chart` **855** 不变（`data/readiness.py` 口径未动）、全仓 compileall。**本批并入待推送的 `1.46`**（§7-B12 同批；三处版本号同步待 push 时执行）。 |
| 整合记录 | **2026-09-20 · 批次整合为两次提交** | 用户要求把未推送的 1.28–1.32 五个批次按模块整合：**`1.31` = 扫描模块代码与验收**（M2 全市场筛选 / M3 广度统计 / 就绪度体检 / 补齐缺失 / 成分股两轮修复，22 文件）、**`1.32` = 文档与版本发物**（本表 / PLAYBOOK / ARCHIVE / 主文件 / settings / version.json / 忽略表，8 文件）。下文 v6.38–v6.41 四行是**当时的批次叙事**（"本批 = 1.29/1.30/1.31/1.32 提交"），其内容已并入上述两次提交；1.28 / 1.29 / 1.30 三个批次号**未推送即被消化**（远端自 1.27 直达 1.31）。备份分支 `backup/1.28-1.32` 保留原五提交。 |
| v6.61(文档v6.61) | **§7-B11 后续（续）· 统一下载设置入口（`1.45`）** | 用户问：“预下载里调的间隔/并发，怎么同步到 M2/M3、数据管理？”——现状三入口各调各的（预下载弹窗一套旋钮、数据管理页一个独立间隔旋钮、M2/M3 无入口根默认），改了不联动。用户拍板：C（统一入口）+ 各页旋钮都去掉。**落地**：① 新增全局偏好块 `preferences.download_prefs`（`interval/jitter/circuit_breaker/skip_fresh/concurrency`）= 批量下载参数的**唯一真源**；② 新 `ui/download_hub.download_policy_from_prefs()`（UI 层构 data 层 ThrottlePolicy，jitter bool→float、concurrency 夹 [1,4]、容错回落默认），`DownloadHub.submit` 无策略⇒吃它、有策略⇒完全尊重（取代旧 `_with_download_concurrency`）；③ 新唯一编辑面 `ui/dialogs/download_settings.py::DownloadSettingsDialog`（OK ⇒ `preferences.set`）；入口三处（预下载弹窗头部 / 数据管理工具栏 / 下载队列面板）都 `exec()` 同一个它；④ **删掉** bulk_download 的 spin_interval/spin_breaker/chk_jitter/chk_skip_fresh 与 data_manager 的 spin_interval（防“两套值”漂移），submit 不再传页私 policy；M2/M3 的 `readiness_flow._estimate_stale` 改走 `download_policy_from_prefs()`。**边界**：force_full/起点/分区仍是“每次任务”参数不进全局；并发上限 K≤4。**验收**：`smoke_chart` 841→848（并发池⑤段重写 + ⑥“改全局⇒处处生效”闭环 + 1.45 源码级块）、`smoke_pages_overlay` 637→636（§10-9 宽度断言改验设置对话框；预下载弹窗模态断言改为只钉 `_bulk_dialog`）；全仓 compileall。真实联网端到端待用户实测。 |
| v6.60(文档v6.60) | **§7-B11 后续 · 下载性能档（批内并发 + 全市场快照秒补，`1.44`）** | 用户实测：“M2/M3 补数据一家一家来，300 家要 5–7 分钟，体验灾难”；要求“安全前提下最大限度触及速度边界”。拍板：A+B 分层、均衡档（聚合请求率不变、靠重叠）、spot 追加 qfq 当天根。**落地**：① **批内并发池** —— `ui/workers.SyncWorker.run` 拆串行快路径（`concurrency<=1` 逐字节同 v1.43）与 `_run_pool`（`ThreadPoolExecutor` 编排现成 `refresh_one`，数据层仍纯同步）；提速核心 = **全局节流阀 `RateGovernor`**（多线在共享时间线上预约槽、锁外睡、fetch 重叠），**聚合发起间隔仍==interval ⇒ 请求率不变、不封 IP**；熍断/代理计数改跨线程共享（`stop_event` 全体收手）。`ThrottlePolicy.concurrency` 默认 1（回滚），**`DownloadHub.submit` 唯一入口升到均衡档 3**（去重键 `_policy_key` 不含 concurrency）；任务级仍 K=1。② **全市场快照秒补** —— `AkShareFeed.fetch_market_spot_daily()`（1 次 `spot_em`，含单位归一：换手÷100 / 量×100）+ `MarketSyncService.fetch_spot_snapshot()` 门面；`refresh_one(..., spot_bar, spot_prev)` 仅当“本地末日==上一已定稿交易日”才追加当天（`reason=spot`）；`SyncWorker._build_spot_ctx` 循环外取一次快照（盘中/无日历/非qfq ⇒ 不开）。**三硬边界全守**：15:05 定稿后、只当天、qfq 最新根==真实价才自洽（缺口>1日/首次退回逐只，绝不造历史）。**日历每批只取一次**：新增 `_load_calendar_safely` + `inject_expected_latest(calendar=...)` 哨兵（inject 与 spot 共用，不重复取）。`ReadinessReport` 新增 `last_by_symbol`（零额外 I/O，供秒补估算）。**坑**：§11.5-86（并发变量多写需共享、日历取一次传下去）、§11.5-87（spot 写 qfq 分区前必对齐量纲/复权防 100×）。**验收**：`smoke_chart` **817 → 841**（+13 并发/节流/熍断 + 11 秒补归一/定稿/幂等）、`smoke_pages_overlay` **637**；全仓 compileall。**真实联网端到端待用户实测**。③ 单纯降 `interval` 仍不做；**偏快档（K>3/降 interval）待实测后另拍**。 |
| v6.59(文档v6.59) | **§7-B11 后台下载 · 收口轮（`1.43`，与 1.42 合成一个 commit）** | 用户拍板"走收口、不回撤、合成一个 commit"，并要求顺带查"**还缺什么 / 后台跑与进度是否真没问题**"（原话：这批代码**写完根本没测过**）。本轮**不改规格、只堵窟窿**：**① 真 bug（收尾漏发回包）** —— `DownloadHub.cancel(None)` 早先只给排队中的任务改状态、**不发 `job_finished`**，而消费方（M2/M3 的 `_syncing`、批量弹窗）**只靠它收尾** ⇒ 该页永远停在"进行中"（回执冻结、`_busy_elsewhere()` 恒真、再点「更新到最新」只会去"停止"、**只能重启应用**）；修法 = 取消也发 `{"aborted":True,"aborted_by":"cancel"}` + 新增 `_settle_activity()`（取消路径不走 `_on_finished`，漏了它角标会一直亮着骗人）。**② 真 bug（按钮名 ≠ 动作）** —— 下载条「中断」原用 `cancel(None)`（= 停当前 **+ 清空排队**），而 tooltip 写"停止当前任务" ⇒ 静默干掉还排着的任务；改为 `cancel(current.id)`，"全部中断"只留队列面板。**③ 收敛** —— 单只互斥 token 由 4 处手写（`desk_data` / `backtest_flow` 指数+个股 / `breadth_flow`）收成唯一公共件 **`SingleSyncGate`**（`blocked_by` 问一句 / `hold` 幂等占位 / `release` 可重复释放 —— 原写法漏一条 return 路径就**永久占住该标的**、且界面上看不出原因）；`_find_duplicate` 去重键补 **`min_date` + 档力度**（原会"同清单、不同起点被判重复 ⇒ 第二方范围被静默忽略"）；批量弹窗 `parent` 改**主窗口**（原挂页面，切页可能连窗一起消失）；`readiness_flow` 取件统一走 `hub_of` 并降级。**④ 新增验证层（本批最重要）** —— 前两层冒烟（纯组件 / 页面级）**都把 `SyncWorker` 打桩成不 `start` 的假线程 ⇒ 假线程永远按时回包，测不出①**；补一个 **真 SyncWorker / 真 QThread / 真跨线程信号** 的端到端探针（只打桩 `refresh_one` 与日历注入，避免联网/写湖），**15 项全过**：串行不断链 / 跨线程进度到达主线程 / 中断保留真实进度（`2/5`，不被充成 100%）/ `shutdown()` 真等到线程结束且不留运行中的 QThread / 回包后队列清空。**⑤ 顺手堵一个"假按钮"** —— 下载条 `✕` 在任务在跑时按了也收不起来（投影立刻弹回）⇒ 只在回执态出现。**验收**：`smoke_pages_overlay` **611 → 627**（+16：两个 bug + `SingleSyncGate` + 源码级断言"只有 hub 里能出现 `note_single(`"）、`smoke_chart` **817** 不变，全绿；探针 15/15。**遗留三笔（非阻塞，已登记 §9-F）**：`shutdown()` 阻塞主线程最多 15s 且无反馈（超时仍可能销毁运行中的 QThread）/ 中断后没有一键"继续未完成"（批量侧靠重提同范围 + `skip_fresh` 天然续传）/ `FLAT_QSS` 归属漂移（另 4 处私有副本配色已不一致）。坑 = **§11.5-82**。**本批 = `1.43` 一个提交**（1.42 数值控件宽度并入，不单独占号；§9-A 三处同批同步）。 |
| v6.58(文档v6.58) | **§7-B11 后台下载：任务归属从弹窗搬到主窗口（`1.43`）** | 用户实测：“批量预下载这个窗口会强制置顶并且无法拖动，下载期间无法进行其他任何操作”，并要求“不仅仅这个弹窗，**本软件所有包含下载/抓取数据的功能都要能后台下载，同时用户还要有办法知道后台进度**”。**两层根因**（不是“置顶/不能拖”本身）：① `data_manager._open_bulk` 用 `dialog.exec()` = **应用级模态** ⇒ 整个主界面被冻在弹窗后面；② 线程是 `SyncWorker(..., parent=弹窗)` ⇒ **线程随弹窗生灭**，旧代码因此写了 `_try_stop_worker()`（关窗前 `cancel()` + `wait(15000)`，否则 QThread 运行中被销毁 = 崩溃）。⇒ **窗口与线程绑死才是“必须守在旁边”的真因**。**落地**：新 `ui/download_hub.py` —— `DownloadHub(QObject)` + `DownloadJob`，**只编排现成 `ui.workers.SyncWorker`、不自造 QThread**（§2 不破）；**串行 K=1**（与“宁可慢也不封 IP”一致）；`submit / cancel(单个或全部) / retry_failures / snapshot`；**同样清单不重复入队**；单只互斥 `note_single / release_single / is_busy_for`；退出 `shutdown()`。三处呈现**全是 hub 的投影、不存任务数据**：底部 `ui/widgets/download_bar.py`（任务名 + 进度 + `223/5849 · 000622` + 约剩多久 + 排队数 + 详情/中断/✕；**空闲 hide**、长文案进 tooltip、单行标签 `QSizePolicy.Ignored` 不撑窗 §11.5-73）、`🗄 数据管理` 导航角标（`activity_changed` 驱动；角标是按钮子件 ⇒ 事件不冒泡到父件过滤器，**两个都要装 eventFilter**）、非模态队列面板 `ui/widgets/download_queue_panel.py`（任务表 + 只重试失败 + 复制失败清单 + 全部中断 —— 旧能力从弹窗里搬出来）。**接线**：弹窗进“投影模式”（只认自己那个 `job_id`，`_try_stop_worker`/`closeEvent` 硬等逻辑整体删除）；数据管理页 `_sync_selected` 提交即返回（**`_set_busy` 删除**，不再锁 8 个按钮），完成由 `_on_hub_finished` 触发 `_rescan`（不在前台则记 `_pending_rescan`，`showEvent` 补刷）；`readiness_flow._launch_sync`（M2/M3）交队列，顺带修旧隐患 `SyncWorker(symbols, zone=...)` **没给 parent**；三处 `SingleSyncWorker`（行情页 / 回测滞后补 / M3 指数补拉）保持当页回包，只加“启动前查 `is_busy_for` + 跑期间占位”（同一个 parquet 不能两个写者）。**两个体验决策**：后台完成**不再弹“部分失败”模态框**（那是打断），改可点回执 + 面板清单；失败“常见原因”原本两页各写一份且**已经不一致**（一份列代理、一份没列）⇒ 收成 `sync_service.failure_hint(stats)` 单出口。**样板** `design/1.43-bg-download/index.html`。**验收**：`smoke_pages_overlay` **577→611**（+34）、`smoke_chart` **817** 全绿 + 全仓 compileall；⚠ 测前必须把 `download_hub.SyncWorker` 打桩为不 `start()`（否则跑测试 = 真下载，写脏数据湖并撞 §11.5-79）。坑=**§11.5-81**。**同批文档纠偏**（上一轮全仓核对发现的滞后，均属 §10-14“同一事实只存一处”）：§9-A 版本号从 1.37 补回当前、§4 红榜全量现测重写（`trading_desk.py` 345→499、`backtest_panes.py` 395→419、`custom_widgets.py` 931 首次登记）、§3 标题 5→6 页、数据管理 8→9 分区、§9-D 偏好键 1→7、`design/` 登记进 §10-13 白名单。**本批 = `1.43` 提交**（push 待用户）。 |
| v6.57(文档v6.57) | **§10-9 还清：数值控件宽度工厂（`1.42`）** | 用户实测：“批量预下载页面里的间隔，预设值是 0.6 但文本框给的太小，用户只能看到 0”。**根因**：`ui/dialogs/bulk_download.py` 把 `spin_interval`（与 `spin_breaker`）`setFixedWidth` 钉在 **64px**，而原生 `QDoubleSpinBox` 右侧要留约 18~22px 放上下箭头 ⇒ 文本区只剩 ~30px，“0.6”被截成“0”；**对照证据**：数据管理页同一个“同步间隔(秒)”是 82px ⇒ 同一件事两个尺寸，直接违反 §10-9（同类控件同一张脸 + 数值控件最小宽度 ≥ 72px）—— 这是一条**存量违规**。**修法**：`custom_widgets`（§10-9 契约段）新增 `SPIN_MIN_WIDTH = 72` + `double_spin()` / `int_spin()` 唯一工厂：**只给 `minimumWidth`、绝不 `setFixedWidth`**；步长跟小数位自适应（判据与 `backtest_panes.number_spin` v6.18 同源）；两页改用，**默认值 0.6 / 范围 0~10 / 步长 0.1 一律不变**（只修宽度，不顺手改口径）。**断言用真判据**：`resize(sizeHint())` + `processEvents()` 后验 `lineEdit().geometry().width() >= fontMetrics().horizontalAdvance("0.6")`，再加两条源码级防漂移（全 `ui/` 不许出现 `setFixedWidth(64)`；两页不许直接实例化 `NoWheel*SpinBox()`）。**同批修一个竞态污染（冒烟脚本自身）**：收尾自检 2 红（`backtest_strategies.json` / `backtest_results` 被写）—— §7-A4 的 `_on_rerun()` 真起了一个异步回测，它在**后面任意一段**转事件循环时才回包 ⇒ `StrategyBridge.record()` 写真实策略库，而 `backtest_archive.auto` 当时已被还原成 True ⇒ 又往真实目录落一份快照（实测新文件时戳与测试结束同一秒，内容就是 600000/长江电力/绿蓝红 测试夹具）；**红与不红取决于那次异步回测跑不跑得完** —— 典型竞态（§11.5-79 同族）。修法 = 与 annotations/formula_library/watchlist 同处理：脚本开头把 `view.store.path` 重定向到临时目录（新增“打桩④”），且 §7-A4 末尾**不再把 `auto` 还原成 True**。**验收**：`smoke_pages_overlay` **566→577**（+11）全绿、`smoke_chart` **817** 不动。**本批 = `1.42` 提交**（push 待用户）。 |
| v6.56(文档v6.56) | **§7-B1/B2 补漏 ·「需要动作必有入口」（`1.41`）+ 主文件瘦身 99.1k→96.8k** | 用户实测反馈 + 明确要求"**不要我提哪里有问题你就改哪里，要举一反三**"。现象：M2 统计范围里选任意范围后，体检显示"未下载 456 / 历史不足"，而**整页没有任何更新入口**（用户截图）。查出的根因**四类，一次性全治**：**① 唯一入口依赖结果区状态** —— 能触发"更新到最新"的只有结果区**空态**按钮，而它只在 `_outcome is None`（还没扫过）时出现 ⇒ **一旦扫过一次，整页再也找不到补数据的入口**；**② `resolve_scope` 从不清旧 `_outcome`** ⇒ 范围换成中证500 后，结果区上方还挂着**沪深300 的统计**（命中 6 · 300 只 / 有效样本 295/300），而空态写的是**中证500 的就绪度**（未下载 456/500）—— **两套数字同屏打架**；且 `_render_readiness` 因此误走"已有结果"分支、**不再给入口**；**③ 文案指称的按钮已不存在** —— v6.47 把"补齐缺失"并入「⬆ 更新到最新交易日」后，6+ 处文案（`scan_flow` 的二次确认 / `shift_date`、`breadth_flow`、`core/cross_section` 的逐只说明）仍在指引用户去点旧名按钮 ⇒ **指路指到空处**；**④ "需要动作"却"没给动作"** —— `_render_readiness` 的"已有结果"分支只 `setText`（无按钮）；"只有坏文件、无缺口"分支也只给一句话而**没有入口**。**落地**：**按钮名收成唯一出口** `ui/widgets/custom_widgets.SYNC_ACTION_LABEL`（UI 常量文件，零依赖不成环），各处文案一律引用 ⇒ 改名即全改；**`core/` 与 `data/` 不许指名 UI 按钮**（分层纪律）改成"页面的下载入口"；**M2/M3 摘要条各加常驻 `btn_sync`**（同一文字、同一动作）⇒ **任何状态下都有入口**；**范围切换作废旧结果** —— 两页 `_drop_stale_result((范围, 指数代码))` 清 `_outcome` + 新增 `ScanResult.clear()` / `BreadthResult.clear()`（KPI / 口径摘要 / 表格复位），**同范围重解析不清**（别白丢用户已跑出的结果）；**"已有结果"分支绝不调 `set_empty`**（那会 `empty_box.show()+table/chart.hide()` 把刚扫出的结果**藏起来**，比"没按钮"严重得多）；**有坏文件也属"需要动作"** ⇒ 补「去数据管理」入口（判据是"要不要用户动手"，不是"有没有缺口"）；**旧按钮名彻底退役** —— UI 层源码不许再出现「补齐缺失」、`core/`+`data/` 不许出现按钮全名，**两条都用源码级断言钉住**（这类漂移功能测试与肉眼都抓不到）。验收：`smoke_pages_overlay` **557→566**（+9）、`smoke_chart` **817** 全绿 + 全仓 compileall。坑 = §11.5-80。**同批完成用户要求的 rules 瘦身**（99.1k → 96.8k 字符）：按 §10-14「先搬走再写新内容」压缩四处（§11.6 的 v6.49–v6.53 五块→3 行归档指针 / §0「断点」行 2.0k→0.6k / §11.6 v6.54 块→5 行 / §8 收回到"最近三版"），**只删移"已有归档的复述"，未丢任何决策记录**。**本批 = `1.41` 提交**（§9-A 三处同批同步；push 待用户）。**⏸ 另有 1 项待用户拍板**：下载速度性能档（并发 K=3~4 / 批量快照通道 `stock_zh_a_spot_em`），已按用户 2026-09-23 要求写入 §11.6 顶部待拍板区。 |
| v6.55(文档v6.55) | **§7-E5 下载层新鲜度对齐真交易日历（`1.40`）** | 用户实测反馈驱动（"大池子每天更新到最新很影响体验"）。**先量再改**：诊断为两层 —— ① **节流 sleep 就是全部成本**（`ThrottlePolicy.interval=0.6s`；单只本地开销 = 整文件 parquet 读 + 全历史 concat/去重/排序 + 整文件写，只占 **2–5%**；自证：文档写"5000 只 ≈50 分钟"而 5000×0.6s=50min）⇒ **明确不做**读写/存储优化；② **最大一块不是"慢"而是"纯白干"** —— `_is_fresh` 认的是**日历日**（只有"末日==今天"才算最新），而"今天"不是交易日 ⇒ **周末 / 节假日 / 盘中（< 15:05 未定稿）**全池每一只都判"不新鲜"，逐只发一次注定返空的请求（5400 只 ≈**50 分钟换来零变化**）；而 M1/M2/M3 的滞后提示**早已用真交易日历**（`data.trade_calendar.latest_settled_trading_day`）正确判定"已最新"⇒ **两把尺子打架**。**落地**：`ThrottlePolicy.expected_latest`（注入式"该到哪天"）+ `_is_fresh(last, days, expected_latest)` 改判 `last >= expected`；**注入而非自取**（`data/trade_calendar` 已 import `sync_service` ⇒ 反向 import 成环；且日历**每批只该取一次**，冷缓存含网络）⇒ 新增 `ui/workers.inject_expected_latest`，`SyncWorker` / `SingleSyncWorker` 都在 `run()` 注入一次；**拿不到日历 ⇒ 原样返回同一对象 ⇒ 完全回落旧判据（零行为变化、可安全回滚）**；盘中路径也顺带受益（原为"拉回半根 bar 再被定稿守卫裁掉"，现直接跳过），定稿守卫降为**第二道防线**。附带修**劝退式预估**：`estimate_seconds(count, policy, stale_count=None)` 按"真正要联网的只数"算 + `format_duration` 收成**唯一展示出口**；M2/M3 二次确认用体检已有的 `coverage_at` 报"其中约 N 只真正联网、约 X 分钟，其余自动跳过"；批量弹窗改口"**最多约…（已最新的自动跳过）**"。验收：`smoke_chart` **803→817**（+14 条：判据四态 / 端到端"周末零请求 + 盘后必拉 + 未注入回落旧判据" / 注入件每批只取一次 + 拿不到日历原样返回 / 预估与格式化；并给 E2 段落打桩 `load_or_fetch` 防联网与污染真实日历缓存）、`smoke_pages_overlay` **557** 全绿 + 全仓 compileall。坑 = §11.5-78。⚠ **验收时的如实发现（登记 §11.5-79）**：`smoke_chart` 那 3 条"真实分区缓存一致性"断言红 —— **先做基线对照**（`git stash` 掉全部改动）证得**基线同样 800/3 红**，并查明真实数据湖**正被并发写入**（6 秒内 581→583 个文件，有 python 进程在跑）⇒ **存量脆弱性**（`data_version()` 指纹含 `mtime_ns`，湖一动缓存即换版本），不是本次回归；教训：**断言大面积红之前先确认"数据是不是正在被改"**。**本批 = `1.40` 提交**（§9-A 三处同批同步；push 待用户）。 |
| v6.54(文档v6.54) | **§7-E4 可行性闸门「时点成分股」：实测数据源不可得 ⇒ 就地结案（零代码变更）** | §7-E 队列最后一项（治 M2/M3 幸存者偏差）。按拍板 P3，**先过闸门、不过就结案**，两关都实测：**① akshare 侧无路（决定性）** —— `akshare 1.18.60` 的 4 个成分股接口（`index_stock_cons_csindex` / `index_stock_cons` / `index_stock_cons_sina` / `index_stock_cons_weight_csindex`）+ 申万两个（`index_component_sw` / `sw_index_third_cons`）+ `index_stock_info()`，**签名里全部只有 `symbol`（或无参），没有任何日期类参数** ⇒ "按历史日期取成分股"**根本不存在这条路**（不是"没试出来"）；**② 官网侧网络可达但无可用端点** —— `csindex.com.cn` 首页 **HTTP 200**、`sse.com.cn` 指数页 **403**（反爬），连试 5 个候选 REST 端点（`/csindex-home/search/notice-list` / `/index/notice` / `/news/news-list` / `/index/sample-adjust` / `/index/cons-download`）**全部 404**（后端确为 Spring Boot 服务，但路径要从站点前端 JS 里挖、猜不到），且官网历史调样以**公告 PDF/Excel** 发布 ⇒ 即便挖到端点仍要自建解析器 + **长期随格式维护**；**③ 旁证**：`index_stock_cons_csindex('000300')` 返回 300 行、列为 `日期/指数代码/指数名称/…/成分券名称`，其中「日期」= **快照日（2026-09-22）**，**无入选日、无退出日**（与 v6.43 结论一致）。**处置**：**不发版、不写 UI、不引入"看着支持"的假能力**，保留现有两处诚实提示（`constituent_snapshot_text` 报快照日 + 两页"区间早于快照 ⇒ 幸存者偏差"警告）；§7-D4 状态改 `[~]` 并记明"**数据源实测不可得**（不是没设计好，是外部数据不存在）"；§7-E4 保留**复活路径**（须先用**浏览器**打开官网、从 Network 面板抓真实端点，再走"事件流 JSON + `constituents_at(asof)` 回放 + `asof` 只进参数不进缓存键"的原方案 —— 属"数天 + 长期维护"投入，需与用户对齐）。**§7-E 队列至此全部闭环**（E1 提交 1.37 → E2 1.38 → E3 1.39 → E4 结案）。**本批零代码变更（纯结论记录）**；push 待用户。 |
| v6.53(文档v6.53) | **§7-E3 日线落盘列白名单（`1.39`）** | 按 §7-E 队列续做。**① STEP 1 先探针实测**（只读 parquet **footer**，跑完即删）：`kline_daily` **420 文件 / 48.7MB** —— OHLCV+`symbol` 100%、`amount`/`turnover`/`outstanding_share` 98.6%（414）、`持仓量`/`动态结算价` 5、**东财中文六列 1**；`kline_daily_raw` 1 文件（干净）；`index_daily` 29 文件（**无 `amount`**）；`kline_min` 3 文件（**只有 6 列 OHLCV、无 `symbol`**）。⇒ 结论：没有"第四类"未发现的杂列；**杂列只涉 6 个文件**（全仓 0 引用可安全裁）；`index_daily`/`kline_min` 就是"**必须 apply-if-present**"的硬证据（require 会砍坏正常分区）。**② STEP 2 落地**：`data/akshare_feed.py` 新增模块级 `DAILY_KEEP_COLUMNS = OHLCV_COLUMNS + (symbol, amount, turnover, outstanding_share)`（**唯一白名单**，`amount`/`turnover`/`outstanding_share` 是 `core/cross_section` 的"受支持列"，**裁掉会让换手率/市值筛选当场全变「数据不足」** —— §7-E3 拍板 P2 保留）+ `_normalize_ohlcv` 末尾裁列，消除"**日线没有列裁剪、分钟却有**"的不对称。**③ STEP 3 断言**（`smoke_chart` **796 → 803**，+7）：白名单组成/不含杂列/**★★ 白名单 ⊇ 横截面内核所需全部列**（`_BASE_COLUMNS` ∪ `columns_for(启用换手率+市值)` —— 防"将来顺手裁列让筛选**静默失效**"的交叉护栏，§9.1 的核心警告）/脏数据清洗后只剩白名单列/apply-if-present 不报错不造列/受支持列不被裁/分钟路径行为不变。（`smoke_pages_overlay` **557** 不动全绿。）**④ STEP 4 存量数据不动**（数据主权，想瘦身用「重新全量下载」），**不做自动迁移**。**⚠ 两处如实登记**：原计划"改造旧断言（怕它变假绿）"的预判**有误** —— 实测**存量不动** ⇒ 旧断言前提仍在、保留即可（**涉及存量数据的推断必须等探针跑完**，§11.5-77③）；"数据管理页加一句瘦身说明"**本轮不做**（收益边际 + 该页有 6 条护栏不变量），诚实登记而非假装做了。坑=§11.5-77。**本批 = `1.39` 提交**（§9-A 三处同批同步；push 待用户）。 |
| v6.52(文档v6.52) | **§7-E2 代理失败"说人话" + 熔断提前（`1.38`）** | 用户 2026-09-23 批准 §7-E 施工队列后按序开工（E1 提交 `1.37` → E2）。**① 分类层修一个真陷阱**：`requests.exceptions.ProxyError` → `ConnectionError` → `RequestException` → **`IOError`（就是内建 `OSError`）** ⇒ 旧代码"先 `isinstance(e, OSError)` 再看文本"会把代理失败**先归成 network** —— 这正是 §9.3 里"文案归类没错、但用户看不出该去查代理"的成因；修法 = 新增 `looks_like_proxy_error()`（纯函数）+ `_classify_error` **先判文本指纹、再退回 isinstance**，新增 `"proxy"` 档（断言把 `ProxyError` 造在 `OSError` 子类上钉死，改回去立刻红）。**② 三出口文案各加一档**（`short_fetch_reason` / `friendly_fetch_message` / `friendly_constituent_message`）：说清"本机系统代理断连/切节点"+"去查代理软件"，并明确**"这不是行情源挂了、也不是被限流，所以稍后重试通常没用"**；⚠ **不写死 `127.0.0.1:7897`**（那是本机诊断值，进用户文案＝对新用户撒谎），加断言钉住三出口都不许出现 `127.0.0.1`/`7897`/markdown 星号（Qt 弹窗是纯文本）。**③ 熔断提前**：`ThrottlePolicy.proxy_circuit_breaker = 3`（通用仍 12）—— 代理全灭时失败率 100%，12 连败 = 白等十几次超时；`SyncWorker` 另计 `consecutive_proxy`，达阈值即停并回传 `aborted_by ∈ ""/cancel/proxy/circuit`。**④ 中断原因做成公共件** `abort_reason_text(stats)`：`SyncWorker.finished` 有三个消费方（批量预下载 / 数据管理页 / M2-M3 的更新与补齐），各写一遍"（已中断）"必然有一处漏 ⇒ 统一出口（§11.5-11 做全套）。**⑤ P4 顺手还清旧挂账**：`ui/dialogs/bulk_download.py` 的手写 `_cons_token` → 公共件 `JobGuard`（`workers.py` docstring 里"择机改用"的挂账）；至此"发起新任务 ⇒ 旧回包作废"**全仓只有一处实现**。**验收**：`smoke_chart` **782 → 796 项**（+14：分类顺序陷阱/互不误伤/三出口/三档文案各不相同/不写死端口/无星号/`abort_reason_text`/阈值 3<12/代理 3 次即停/非代理仍 12/未达阈值不中断/cancel 区分）、`smoke_pages_overlay` **554 → 557 项**（+3：JobGuard 收编 + 迟到回包丢弃 + 最新回包生效）+ 全仓 compileall 全绿。坑=§11.5-76。**本批 = `1.38` 提交**（§9-A 三处同批同步；push 待用户）。 |
| v6.51(文档v6.51) | **§7-E 施工队列落档（只落方案不动代码）+ v6.50 文档瘦身** | 用户 2026-09-23 拍板"不用改了直接写进去，然后在改动之前提交 git"。**① 文档瘦身（v6.50）**：主文件 **118,169 → 75,996 字符**（超 §10-14 硬指标 18k）⇒ 按"**先搬走，再写新内容**"：逐文件目录树（298 行 / 22k 字符）→ **新建 `docs/JIAN_CODEMAP.md`**（23.6k，§4 只留顶层骨架 + 指路 + 红黑榜）；**§7-B1/B2 主案规格** + **§7-B10 主案规格**（两份均已收官）→ `ARCHIVE`「一」（各留"仍现行口径"4 条 / 3 条，不用翻归档就能干活）；**§11.6 历史块 v6.48/v6.47/v6.46/v6.44/v6.43/v6.39** → `ARCHIVE`「三」；全文 **12 处"见下方主案规格"失效引用**改指归档；文档地图/§10-13/§10-14 册数说明同步；**§11.7 新增末条"收工量字符数"** —— 把纪律变成动作（"别靠多读几遍硬扛"），并把这条写进 §10-14 的"v6.50 又超了一次"复盘。顺手修一处误导状态：`B1 [~] 待施工：8 发版` → `[x]`（M2 早已完工，留着会让人看错"下一步"）。**② §7-E 施工队列定稿**（E1 提交 1.37 / E2 代理文案+熔断提前 / E3 日线列白名单 / E4 时点成分股），含**已核实的代码落点**与拍板 P1–P5（P5「不走系统代理」开关**本轮不做** —— app 目前**没有设置页**，属独立话题）。**③ 提交**：`1.37`（17 文件 / +2983 −722，含 5 个新文件）**本批 = `1.37` 提交**（本地领先远端 2，push 由用户执行）。 |
| v6.49(文档v6.49) | **§7-A4 回测历史存档（v6.49 复核重做 · `1.37`）** | 用户实测反馈驱动的重做："重点选不上 / 点一下就有颜色 / 右侧功能一塌糊涂"。**① 版式按 1.22 同款分工拆出** `ui/widgets/backtest_history_ui.py`（460 行：`HistoryFilterBar` / `HistoryTable` / `MiniEquityChart` / `HistoryPreviewPane` / `HistorySettingsBar`），页面 `ui/views/backtest_history.py` 收敛为**薄壳**（181 行：状态 + 接线）。**② 修三处 UI 真 bug**：pin 后整表重建清空选中（"重点选不上"）⇒ 改**就地更新一行 + 保住选中**；表格数字着色用 `item.setForeground` 在蓝色选中高亮上叠红绿字（"点一下就变色"）⇒ 改 `QStyledItemDelegate`、选中态交给样式表；`MiniEquityChart.plot()` **遮蔽** `pg.PlotWidget.plot`（`__getattr__` 转发，不在 MRO 里 ⇒ `super().plot` 直接 AttributeError）⇒ 改名 `set_series` + 显式 `getPlotItem().plot`。**③ 存储层三处真 bug**：`sample_equity` 均匀抽样漏末点（而 `cumulative_return == equity[-1] − 1` ⇒ 载入查看的累计收益算错）⇒ **端点保底**（`round(i*(n-1)/(k-1))` + 显式补首尾）；`delete('不存在的 id')` 会 `os.makedirs` **把真实目录建出来** ⇒ 读路径/空删一律不写盘；`_evict` 改旧→新明确截断 + `seq` 单调键 + 僵尸索引剔除。**④ 只读回放彻底做实**：预览不再改写 `_last_result/_last_meta`（否则预览里点导出＝"存档结果 + 上次参数"这对错误组合）⇒ 保存/恢复现场 + **预览期禁用导出** + 黄色横幅「退出预览」；净值曲线**补画买卖点**（`buy_at`/`sell_at` = 成交日净值，落在曲线上）；K 线页给诚实提示"存档不含逐日 OHLC"。**⑤ 补齐**：导出菜单「💾 存为历史快照」手动兜底、**「📤 送行情页」**、kind M2/M3 **置灰**标"预留"、参数详情**可折叠**、`_last_config` **发起瞬间定格**（保证"存档的配置 = 跑的那次"）。**验收**：`smoke_chart` **745 → 782 项** / `smoke_pages_overlay` **530 → 554 项** + compileall。坑=§11.5-75。**本批 = `1.37` 提交**。 |
| v6.48(文档v6.48) | **§7-A2 回测结果图表导出（`1.36`）** | 用户实测"CSV 里图表完全无法显示" ⇒ 根因：**CSV 是纯文本、物理上不能内嵌渲染图**（那一大段"图形绘制/QSD/GLX"只是公式源码被当注释写进去了）。做法：① `build_daily_series(result)` —— 逐日净值 + 买卖点（取 equity + trades）作**共同数据源**；② **CSV 回到"参数快照 + 逐笔"**（用户反馈"逐日全量太占空间"），看图交给 xlsx；③ 新增 `ui/widgets/backtest_xlsx.py` —— openpyxl **内嵌真正的净值曲线图 + 买卖点 marker**（买卖点 y 用**成交日净值**定位、不是成交价 —— 量纲不同会跑出坐标轴）；逐日数据放**隐藏 sheet「净值数据」**、图放可见「净值曲线」（主表只剩图）；导出菜单加「📊 导出 Excel 图表…」；`_build_workbook` 与存盘解耦。**验收**：`smoke_chart` 745 / `smoke_pages_overlay` 514→**521**（xlsx 对**构建时的 Workbook 对象**断言 `ws._charts` —— openpyxl 读回会丢图；写盘用 BytesIO 不污染用户目录）。坑=§11.5-74。**本批 = `1.36` 提交**。 |
| v6.47(文档v6.47) | **§7-B10 全案收官：数据新鲜度（M2/M3 收尾 · `1.35`）** | 承 v6.46 的 M1 切片，补齐 STEP 2–4：**① 一键「⬆ 更新到最新」** —— `ReadinessFlow.update_latest()` 对**整批当前范围**跑 `SyncWorker` 增量（缺的补 / 旧的拉到最近交易日 / 已新鲜自然 skip），与"补齐缺失"**合并**为单一空态动作（共用 `_launch_sync`，`fill_missing` 退位次级保留）；**② 滞后提示** —— `start_calendar_fetch()` 挂 `CalendarWorker` → `trading_target`，`_render_readiness` 用 `trading_days_between`（**真日历精确数**）+ `format_stale` 出"约 N 交易日滞后"；**离线不提示、不许 busday 虚报**；**③ M2 基准日诚实化** —— `scan_layout.lbl_asof_hint`（上限=本地最新、要更先更新）+ `_calibrate_asof_date` 抬升上限并回显；**④ 用户实测后修正两处**：`report.latest` 用**全局 max** 会被"单只刚同步的标的"掩盖 ⇒ `representative_latest`（中位日）+ `coverage_at(基准日)` 诚实提示"基准日当天仅 N/total 有数据"（**就绪 ≠ 扫描**：就绪=历史行数够、扫描=基准日当天有行 —— 两套口径，不改默认算法只加诚实提示）。**验收**：`smoke_chart` **745** / `smoke_pages_overlay` **514** 全绿 + compileall；两页起 `CalendarWorker` 测中均打桩不联网；窄屏不撑窗（单行 QLabel 设 `QSizePolicy.Ignored`）。坑=§11.5-71/72/73。**本批 = `1.35` 提交**。 |
| v6.46(文档v6.46) | **§7-B10-M1 切片：日线收盘定稿守卫 + 真交易日历 + M1 区间修复（未单发版，并入 `1.35`）** | 用户实测 M1 单股回测"区间末日总是旧日"。**① 定稿守卫（STEP 0/1）**：`sync_service` 新增 `DAILY_SETTLE_HHMM`（默认 15:05）+ `is_daily_bar_settled`（纯函数，判据**只此一处**）+ `_drop_unsettled_tail`；`refresh_one` 落盘前对**日线类分区**裁掉"今天且未定稿"那一根（**分钟分区不裁** —— 它本就是盘中语义）；附带自洽：盘中裁掉今天 ⇒ 本地末日=昨天 ⇒ 盘后同步 `_is_fresh` 自然不跳过、起点=昨天+1=今天 ⇒ 拉到完整当天（无需改 `_is_fresh`/起点语义）。**② 真交易日历（推翻原 F「不引日历」）**：`akshare_feed.fetch_trade_calendar` + `data/trade_calendar.py`（`load_or_fetch` 当日 JSON 缓存 + 失败回吐旧缓存 + 全无则 None）+ `ui/workers.CalendarWorker`（异常回 None ⇒ UI 回退本地最新）。**③ M1 区间修复**：`backtest.py` 构页起后台抓日历 + `lbl_range_note` 回执；`backtest_flow` 三方法 `_on_calendar`（精修默认终点）/ `_prepare_stock_then_run`（滞后自动联网补）/ `_apply_end_date_receipt`（补不到 ⇒ 回退有数据那天 + 回执，**只回退不前移、绝不静默截断**）。**验收**：`smoke_chart` **745** / `smoke_pages_overlay` **514** 全绿；防污染自检新增 `trade_calendar.json`（CalendarWorker 测中打桩不联网）。坑=§11.5-71。 |
| v6.44(文档v6.44) | **§7-B8 R7 副图换序收尾 —— 模型可持久化顺序 + ⬆⬇/拖拽 + 测试偏好隔离补正** | 用户 2026-09-21/22 实测确认手感。**① 模型层（零 Qt）**：`ui/widgets/layer_model.py` 新增 `set_sub_order/move_sub/apply_order/sub_order_keys`，构造收 `order=`；**换序只改格位、绝不改 target**（`_reorder_group` 只动同类条目相对位置 + 源码级断言钉死）。**② 持久化**：新键 `desk_ui.sub_order`，`trading_desk` 构造与 `desk_formula._rebuild_model`（增删改名配方后）均带序。**③ UI**：配方页“⇅ 副图换序”= ⬆⬇ + **拖拽**（复用自选股 R15 的 `DragHandleListWidget` 三道闸：平时 `NoDragDrop` 不响应拖、只认行首 ⣿ 手柄、可撤销 + 边缘自动滚动；`desk_layout` 建按钮/工具条/列表 + `rowsMoved` 只连一次；`desk_formula` 四方法，拖完 `rowsMoved → QTimer.singleShot(0) → apply` 绝不在信号里重建列表；每次移动落模型后走 `_on_layer_switch_changed` 统一回流即时重渲染 + 持久化）；`trading_desk` 同名薄壳。旧两条 R7 stash 半成品（假弹窗 + 坏 docstring）作废。**④ 测试偏好隔离补正（§11.5-70）**：`Preferences` 单例 import 时已从**真实** preferences.json 载入 `desk_ui`，光重定向 `path` 不够 ⇒ 新键 `sub_order`（及 breadth 的 ratio/chart/overlay/index_style）在构造期泄入、冲乱“默认渲染顺序”断言；修法 = 冒烟脚本在 **stub `save` 之后**把用户态在内存抹平（并根治此前一次误写真实库）。**验收**：`smoke_chart` **707 → 721 项**（+14 R7 模型断言）/ `smoke_pages_overlay` **482 → 493 项**（+11：⬆⬇+拖拽端到端 + breadth 显示前置钉死 + 状态还原防污染）+ compileall 全绿；未改 M2/M3 与产品口径。**本批 = `1.34` 提交**（§9-A 三处同批同步；push 随本批）。 |
| v6.43(文档v6.43) | **§7-B1/B2 STEP 6.5：M2/M3 体验修复轮 —— 图表可读性根治 + 先选后扫 + 缺数据全链路诚实化 + 成分股换权威源** | 用户 2026-09-20/21 两轮真机实测反馈（"图完全不可读 / 日期不能先选 / 点一下就卡死 / 名单 429 只只扫 27 只不告知 / 沪深300 名单只剩 000300 自己"）。**① M3 图表可读性根治**：`breadth_chart` 从 epoch 秒轴改回 **bar 序号轴** —— 第一版给 `adaptive_axis.attach_all` 喂 epoch 秒，而该模块契约明写只适用 bar 序号 ⇒ provider 收到 1.7e9 当序号、切片恒空、**两窗格 y 跟随 100% 静默失效**（指数副图对全历史自动量程 2000~6000，底部日期刻度钉在视野外）（§11.5-64）；指数 df 改为**按日精确对齐到广度轴**（只认日期相等，绝不拿后一天冒充）且只画当前区间；**柱状/折线双视觉**（默认柱状+MA5 趋势线，家数纵轴刻度取整）；**指数副图四图形**（折线/面积/K线/美国线 —— 新图元 `custom_widgets.OhlcBarItem` 与 `CandlestickItem` 同数据格式同色板；缺开高低诚实退折线）；**两窗格轴对齐**（`align_axis_widths` R8 第二版固定左槽，对齐不留白）；**分界把手拖动调高**（宿主级新设施 `ChartHost.enable_divider_drag`：可见浅灰胶囊 + 悬停 ↕ 光标 + **按下瞬间锁定参考系**（第一版拿实时几何反推 ⇒ 布局回流正反馈振荡，"拖一点就失控/往上没反应"，§11.5-67）+ 双击恢复 3:1）。**② M2 先选后扫**：基准日 `NoWheelDateEdit`（日历弹窗自由选，默认=本地最新交易日，体检回包自动校准且用户动过后不覆盖）；内核新增**轴外基准日就近落位**（平手取前一日 + warnings 出声，不再"全市场数据不足"空跑）；◀▶/「最新」/日历三方双向同步。**③ 缺数据全链路诚实化**：`read_daily` 按文件读时静默跳过无文件标的（名单 429→只扫 27 之谜，§11.5-65）⇒ `scan(missing=…)` 逐只记「数据不足」进 `counts.total` 与 warnings，`status_on` 切片同步补上（切日期数字不跳变）；"读数为空"早退路径不再吞名单（界面永远有东西可看）；扫描前闸门 `_confirm_scan_with_gaps`（体检有缺口/未完成 ⇒ 二次确认，选否不启动；本地齐了不打扰）；体检占位文案不再永久冻结在"正在体检…"（§11.5-66，用户"三分钟没变化"的真因是文案冻结不是体检慢）。**④ 成分股换权威源**：同花顺快照缺斤短两（沪深300→288 / 中证500→429）⇒ 候选顺序改**中证指数官网优先**（实测给满 300 只带「日期」快照列）；payload 加 `count`/`snapshot_date`，界面诚实一行 `constituent_snapshot_text`（快照日 + 名义只数核对）；修选列回归：官网 df 的「指数代码」被"第一个含'代码'列"误命中 ⇒ 权威列名白名单 + 排除兜底（§11.5-68，打桩补线上同构列 + 回归钉）；幸存者偏差：实测三接口都无入选/退出日期列 ⇒ 本轮界面声明（基准日早于快照⇒回执提示），根治路登记 **§7-D 类 D4 时点成分股**。**⑤ 表格性能**：`ResizeToContents`×全A 5400行 = 平方级冻死 UI（点 ◀▶ 卡死真因，§11.5-69）⇒ 填表三件套（关更新→一次性测宽→tooltip 只给说明列）+ 2000 行截断说实话。**⑥ 冒烟方法论补正**："挂了跟随器"≠"y 真跟随"——新增缩放后 y 数值范围/刻度可见断言；真实鼠标拖动模拟（QTest 事仿）分界线双向；测试不许依赖用户真实偏好（breadth_ui 的 overlay/index_style 隔日误报实锤，改显式控制前置）；顺手根治 vline 拖拽断言的 1px flaky（抓取点改取 viewbox 中线 + 窗口位置钉死 + round）；PyQt6 `mapFromScene(QRectF)` 返回 QPolygon 不是 QRect（逐点映射）。**验收**：`smoke_chart` **695 → 707 项**（连跑两轮全绿）/ `smoke_pages_overlay` **461 → 482 项** + 全仓 compileall；联网实测沪深300 → **300 只、不含 000300、快照 2026-09-18**。**本批 = `1.33` 提交**（§9-A 三处同批同步；push 随本批）。 |
| v6.42(文档v6.42) | **诊断（只记录不改码）：系统代理环境下行情拉取"批量全灭"** | 用户实测「⬇ 补齐缺失」每只都 `ProxyError('Unable to connect to proxy', …)` 无一幸免。**根因实锤 = 本机 Clash 类代理瞬断**（`requests` 默认读系统代理 127.0.0.1:7897；代理正常时直连与走代理都通，死端口逐字复现报错）—— 不是行情源挂、不是限流；熔断按设计工作但偏慢。**判据**："批量同错 + ProxyError 字样" ⇒ 先查 `requests.utils.getproxies()` 与代理进程，别怀疑源别加重试。待修清单（需用户拍板）：ProxyError 专属人话文案 / 代理全灭时熔断提前 / 可选"不走系统代理"开关（默认必须跟随系统）。详见 `JIAN_RULES.md` §9.3。**本批零代码变更（纯诊断记录）。** |
| v6.41(文档v6.41) | **修复：M2/M3 成分股名称列全空（回包漏装配 `_names`）+ 内核警告 UI 出口** | 用户 2026-09-20 实测（1.31 修复代码格式之后）：沪深300 扫描成功（288 只 / 命中 11 / 2.60 s）但**名称列全空**。**根因两层**：① `resolve_scope` 三分支各自装配页面状态 —— 自选装 `names_map()`、全A 装 `roster_names()`，唯独**指数成分走异步回包** `_on_constituents` 只回填了 `_symbols` 漏了 `_names` ⇒ 名称列空 + **「剔除 ST / 退市」整轮静默失效**（内核拿不到名称就没法剔 ST，且广度统计会把 ST 股算进家数）；② 内核对这种口径残缺**出过警告**（`scan()` warnings："未提供花名册名称…未生效"），但两页 `_on_finished` 把 warnings **整个吞掉** ⇒ 坏结果看起来完全正常（§10-4 静默教科书案例）。**修法**：① M2/M3 `_on_constituents` 成功分支补装配 `_names`（与全A 同源 `DataEngine.roster_names()`，§9-H）；② 两页 `_on_finished` 给警告出口（回执 ⚠ 首条摘要 + tooltip 全文；缓存命中不带旧警告）；③ 顺手堵 readiness 同族洞（`_on_probed` 的回执写入合并"已有扫描结果"守卫 —— 扫描刚完成时在途体检回包会覆盖扫描回执）。**固化**：§11.5-63（异步回包分支要装配与同步分支同样的状态；内核警告必须有 UI 出口）。**验收**：`smoke_pages_overlay` **457 → 461 项**（+4：名称映射补齐 / 内核警告前置证明 / 回执 ⚠ 出口 / M3 同款）+ `smoke_chart` **695** 全绿。**本批 = `1.32` 提交**（§9-A 三处同批同步；**push 待用户**）。 |
| v6.40(文档v6.40) | **修复：M2/M3「指数成分」全部失败 —— 两套指数代码约定碰撞（`normalize_cons_code` 边界规范化）** | 用户 2026-09-20 00:34 实测 M2/M3 选沪深300 成分股，三个候选接口全失败（`index_stock_cons`: `'NoneType' object has no attribute 'find'`；`index_stock_cons_csindex`: `Excel file format cannot be determined`）⇒ "所有候选接口均不可用"。**根因 = 两套指数代码约定并存**：指数**日线**接口要带市场前缀（`sh000300`，新浪 `stock_zh_index_daily`），成分股三接口只要 **6 位纯数字**（`000300`）；M2/M3 的指数下拉**复用了** `INDEX_PRESETS`（日线用）的带前缀键直接透传。批量预下载一直正常（它的 `CONSTITUENT_INDEXES` 用裸码）—— 同一功能两种表现的对照锁定了方向。**实锤方法**：同一时刻同一接口，`'sh000300'` 返回 0 只、`'000300'` 返回 **288 只** —— 先分清"代码格式"还是"接口挂了"，别急着怪网络。**修法**：新增 `akshare_feed.normalize_cons_code()`（带前缀→裸码，与 `is_index_symbol` 同源判据；坏码原样返回）收在**行情源边界**一处，`fetch_index_constituents` 先规范化 + **格式不对快速失败**（明确报错、不空跑三个接口）；调用方（M2 / M3 / 批量预下载）零改动。**真机复验**（走 `MarketSyncService` 完整链路，即 M2/M3 页面同一条）：`sh000300` → **288 只**、`sh000016` → **50 只**（用户日志里失败的另一只，上证50 数目正好 50）。**固化**：§11.5-62（复用键必须问"两个下游要的格式真的一样吗"；规范化放消费方边界）+ 冒烟 3 项断言（规范化映射 / 假 ak 打桩验证"带前缀在进接口前被规范化" / 乱码快速失败不发请求）。**验收**：`smoke_chart` **692 → 695 项**全绿 + `smoke_pages_overlay` **457 项**全绿。**本批 = `1.31` 提交**（§9-A 三处同批同步；**push 待用户**）。 |
| v6.39(文档v6.39) | **§7-B1/B2 STEP 6：联动收尾 —— 就绪度体检 + ⬇补齐缺失 + 成分股人话诊断 + 扫描就绪预设** | 用户拍板（2026-09-19）："必须保证用户能**真正获取**对应的股票，出问题必须说清是什么问题" —— 本批把"范围解析只给名单、缺数据要到扫描后才发现"的断链补齐。**① 就绪度体检（D6-1）**：新建 `data/readiness.py`（**只读 parquet footer**：行数 + date 统计，实测 **0.66 ms/只** ⇒ 全市场 ≈3.6 s；四分类 ready/partial/missing/unreadable，**坏文件进问题清单绝不静默跳过**；`latest` = 本地日线最新日期；`summary_line()` 一行人话 + `gap_preview()` 缺口点名）；`ui/workers.py` 新增 **`ReadinessWorker`**（第 8 个 Worker，进度/取消/竞态守卫同款）。**② `ReadinessFlow` 两页共用控制器**（`ui/widgets/readiness_flow.py`，D6"就绪度模型只有一份"）：M2/M3 范围就绪后**自动体检** → 回执「就绪 N/M · 未下载 X · 历史不足 Y · 文件损坏 Z · 本地最新」→ 缺口给「⬇ 补齐缺失」→ `fill_missing()` 走 `SyncWorker`（温柔抓取可中断、>50 只二次确认、完成后自动复检）；**回调绑定各自 scope** —— 旧范围的迟到进度/回包一律丢弃（施工中实测抓到"旧全A体检进度覆盖增量回执"的真竞态，已修）；已有扫描结果后体检不写回执/空态。**③ 成分股失败人话诊断**：M2/M3 的 `_on_constituents` 失败分支改用 `friendly_constituent_message`（bulk_download 早已在用，扫描页漏接）—— 不再裸甩"接口未返回成分股"，分类讲清网络/源未收录/代码有误 + 替代路径；**真机联网实测链路通过**：000300→**288 只**、000905→**429 只**（8~14 s ⇒ 解析中界面注明耗时预期）。**④ bulk_download「⚡全市场扫描就绪」预设**（D6-2）：一键填好 全A · 2016起 · 跳过已最新（只填控件不代点开始）。**⑤ STEP 6 前置复核**：真机 368 只冷扫 **2.89 s（7.85 ms/只）**（与 B3 复核一致）⇒ 全市场外推 ≈42 s ⇒ **决策接受引擎路径性能，STEP 7 暂不排期**（等用户补齐全 A 后按需评估）。⚠ **D6 两处如实变通**：D6-4 缺口按成交额优先 —— missing 本地无数据无额可排 ⇒ 按花名册序（首轮补齐后再谈）；D6-5 问题清单回流 —— 扫描侧已呈现（unreadable/失败名单进 tooltip + 指引数据管理），管理页侧栏位登记后续。**验收**：`smoke_chart` **678 → 692 项**（+14：四分类/footer latest/summary_line/缺口预览/进度收尾/取消抛异常/空范围/目录不存在/成分股文案）；`smoke_pages_overlay` **447 → 457 项**（+10：两页体检接线、缺口可见可动作、补齐打桩只发 missing、停止补齐、迟到回包丢弃、M2/M3 成分股诊断、预设一键填充）。**本批 = `1.30` 提交**（§9-A 三处同批同步；**push 待用户**）。⚠ **并行会话记录**：本批施工期间检测到 Qoder 会话在并行施工 **§7-B8 R7**（`desk_formula.py`/`desk_layout.py` 未提交半成品 + `tests/test_r7_sort.py`），其间一次"desk_formula docstring 未闭合导致全仓无法 import"被 stash 备份后由并行会话自行重写修复；**1.30 提交不含这三个文件**（验收在其摘除后的稳定树上完成）。 |
| v6.38(文档v6.38) | **§7-B1/B2 STEP 5：M3「广度统计」页落地（`BreadthView` + `breadth_layout/flow/result/chart`）+ ⚡增量续接** | `backtest_module.py` 的 `page_breadth` 从 `_ComingSoonPage` 占位换成真页面（**占位类退役**）；M1/M2/M3 三个子页全部上线。**新能力全部落新文件**（F-2）：`ui/views/breadth_view.py`（**197 行**，状态 + 同名薄壳）+ `ui/widgets/breadth_layout.py`（**238**，版式装配）/ `breadth_flow.py`（**390**，运行流程）/ `breadth_chart.py`（**165**，双窗格图表）/ `breadth_result.py`（**35**，空态/忙碌态），全部 < 400 线（非空行口径）。**双窗格 x 联动**（B2 拍板：不做真·双 y 轴）：主窗格 = 广度折线（家数 / 占比% + **MA5 平滑**（可关）+ 末点标记），副窗格 = 指数收盘线（画它自己的日期，靠 x 联动对齐重叠区间）；`ChartHost` + `adaptive_axis.attach_all` + 读数条 provider（§7-B6 同款约定，业务文案"日期 · 命中 N 只（占比 x%）· MA5"）。**ƒ 筛选条件 / 🎚 粗筛两卡直接复用 `scan_layout` 的同款 EditPane**（M2/M3 同一套编辑器与阈值换算处）；**新增 `BreadthDisplayPane`**（平滑 / 占比口径 / 指数副图选择，29 档 INDEX_PRESETS）。**⚡增量到最新（D7）**：`scan_cached(incremental=True)` = `ScanStore.find_base()`（同配置、旧数据版本的条目）+ `merge_tail()`（尾段续接：dates/counters/status_matrix 拼接）—— **历史一天都不动**；三条诚实退化路径全有回执：touch（数据版本变但无新交易日）⇒ 沿用原矩阵 + "怀疑被修订请用全量重算"；标的集合变了 ⇒ 不许硬接矩阵、退化全量；缓存命中 ⇒ "矩阵已算到最新，无需增量"。⚠ **如实登记一条口径边界**：D7 原文"增量只算最后一天（秒级）"在**引擎逐只路径**下只对小范围（自选/指数成分）成立 —— per-symbol 固定开销主导（B3 复核），全市场增量耗时 ≈ 全量重扫，以回执实测为准；真秒级要等 STEP 7 宽表档（已写进主案 G-STEP 5）。**⟳全量重算** = 危险动作隔离（muted 样式 + tooltip 讲清"只有数据被修订/换复权口径才需要"）+ `QMessageBox` 二次确认（选否 = 原结果不动）。**区间六档**（近3月→全部历史）= `breadth_frame()` 纯切片零成本；**口径印在标题行**（范围 · 前复权 · 有效 N/M · 区间）+ 近5日迷你读数。**指数副图数据链**：读 `index_daily` 分区，缺数据后台补拉一次（`SingleSyncWorker` 走 `MarketSyncService`，§9-H；独立 `_index_guard` 不与扫描互踢；失败只出声不连累主图）。偏好键 **`breadth_ui`**（配置九样，绝不存扫描结果）。`CrossSectionWorker` 加 `incremental` 通道。**验收**：`smoke_chart` **667 → 678 项**（+11：增量合并 == 全量重扫**逐位一致**、历史逐位不动、touch 沿用原矩阵、标的集合变了诚实退化、find_base 判据、Worker 增量通道含"无基座诚实退化"）；`smoke_pages_overlay` **419 → 447 项**（+28：M3 公共面护栏 67 项、常驻行 ≤3、阈值往返零漂移、端到端真数据 5 只 0.15 s、双窗格 x 联动、区间零成本、占比口径切轴、增量命中回执、⟳ 二次确认正反两路、指数补拉打桩（不联网、不重复发车）、偏好只存轻量配置）；全仓 compileall 通过。**本批 = `1.29` 提交**（§9-A 三处同批同步：commit 首词 / `APP_VERSION` / `version.json`；**push 待用户**）。 |
| v6.37(文档v6.37) | **§7-B1/B2 STEP 4：M2「全市场筛选」页落地（`ScanView` + `scan_layout/flow/result`）** | `backtest_module.py` 的 `page_scan` 从 `_ComingSoonPage` 占位换成真页面；**新能力全部落新文件**（F-2）：`ui/views/scan_view.py`（**164 行**，状态 + 同名薄壳）+ `ui/widgets/scan_layout.py`（**327**，版式装配）/ `scan_flow.py`（**262**，运行流程）/ `scan_result.py`（**137**，结果渲染），全部 < 400 线。**版式 = 样板 A**（E 节）：L1 操作轴（统计范围 · 基准日 ◀▶·最新 · **复权口径印在界面上**（D8）· ⚙配置）→ L2 摘要条（配置 chips + 一行回执 + 进度条 + ▶/✕ 主按钮）→ L0 结果区（KPI 三态 + **有效样本** + 结果表 + 三种空态各带动作 + 口径脚注）→ L3 抽屉（覆盖层，**常驻 ≤3 行**有断言）。**范围三档**：我的自选（`WatchlistStore`）/ 指数成分（`ConstituentsWorker` 异步，联网走门面 §9-H）/ 全 A 花名册（`DataEngine.list_stock_symbols()` + **新增门面 `roster_names()`**（代码→名称，UI 不碰 DatabaseManager））。**粗筛阈值收进抽屉**（D5：内置默认 + 全部可调 + 「↺ 恢复默认」；单位走用户量纲亿元/%，**内核⇄界面唯一换算处在 `ScanFilterPane`**，往返零漂移有断言；换手率/市值关闭时必须是 `None` —— 变成 0 = 误杀一片）。**切日期零成本**（D3）：◀ ▶「最新」只调 `status_on(date)` / `counts_on(date)` 纯切片，**不进线程、不读盘**；且 **D3 的 STEP 4 决策项已定** —— 选 (a) 切日期后**只显示状态**，数值/明细只在扫描基准日提供，界面脚注明示 + **负向断言**（非基准日数值列必须是 '—'，绝不拿旧快照冒充当日）。**双击结果行 → 行情工作台**（复用 `load_symbol` + `switch_to('market')`，不另做看图器）。偏好键 **`scan_ui`**（只存轻量配置，绝不存扫描结果）。⚠ 顺带真实踩坑一条：离屏冒烟里 `isVisible()` 恒 False（页面在非当前页签上）⇒ 先切到前台再断言（§11.5-61）。**文档层面**：按 §10-14"先搬走再写新内容"，把 **§6 已完成能力清单（13.4k）**整段搬进 `docs/JIAN_ARCHIVE.md`「六」（主文件换成一页**能力索引表** + 指针），主文件从 99,496 → **85,452** 字符，余量恢复充足。验收：`smoke_pages_overlay` **396 → 419 项**（+23 全绿，含**端到端真数据 5 只 0.13 s**、常驻行 ≤3、阈值往返零漂移、抽屉开合、偏好只存轻量配置），`smoke_chart` **667** 全绿，全仓 compileall 通过。**本批 = `1.28` 提交**（§9-A 三处同批同步：commit 首词 / `APP_VERSION` / `version.json`；**push 待用户**）。 |
| v6.36(文档v6.36) | **§7-B1/B2 STEP 3：后台扫描线程 `CrossSectionWorker` + 竞态守卫 `JobGuard`（D4 / §9-O5）** | ① **内核开口**：`scan()` / `scan_lake()` 收 `progress=f(done,total,note)` 与 `should_stop=f()->bool`，**每 `chunk`（默认 200）只**报一次进度、轮询一次取消（回调本身不成为开销）；进度**两段** —— 先 `(0,0,'读取日线分区')`（此时还不知道总数），再进逐标的计算段。② **★ 取消 = 抛 `ScanCancelled`，绝不返回半成品矩阵**：最省事的写法是"取消就 break、把已算的部分返回"，但那是**半个矩阵** —— 命中家数 / 广度占比 / 有效样本数全错，而且界面上**看不出异常**（跟"真的就这么多"长得一样），更糟的是它会进会话缓存、之后"切日期"一直看到半成品 ⇒ 改用**异常**让调用方在**类型层面**无法误用，缓存**只落成功的结果**，线程回 `finished(job, None)` 明确表示"没有结果"（§11.5-60）。③ **竞态守卫收成公共件 `JobGuard`**（`next()` 取号 / `accept(id)` 判定 / `latest`）：`CrossSectionWorker.finished(job_id, outcome)` **原样回包 job_id**，页面一行 `if not guard.accept(job): return` 即可丢弃迟到回包 —— 此前 `ui/dialogs/bulk_download.py` 的 `_cons_token` 是同一套判据的手写版，**已登记择机改用本类**（§11.5-11 / §9-O5）。④ 线程仍**只在 `ui/workers.py` 定义**（§9-O2，第 7 个 Worker）；命中会话缓存时**不进计算循环**（连进度信号都没有）。验收：`smoke_chart` **655 → 667 项**（新增 12 项全绿，含 **真并发**竞态验证"旧任务回包被守卫丢弃"、**取消后缓存 0 条**、"进度单调不减且收尾到底"、"公式错走 failed 不静默"），`smoke_pages_overlay` **396** 全绿，全仓 compileall 通过。**未 push**（APP 版本仍 `1.27`）。 |
| v6.35(文档v6.35) | **§7-B1/B2 STEP 2：会话内存缓存 `data/scan_store.py`（D3）** | 新建 `data/scan_store.py`（**247 行**，**没往核心里堆** —— 正是 §4 那条"STEP 2–5 的新增一律另起模块"的示范）。**只缓存结果、不缓存价量**：状态矩阵 = `交易日 × 标的 × 1 B`（uint8 四态，全市场 ≈**22 MB**），而把价量宽表留在内存要 500 MB(f64) / 250 MB(f32) ⇒ 省 20 倍，且**进程退出即消失**。**键 = 六样**（公式指纹「忽略注释/空白/大小写」· **粗筛阈值** · 标的域 · 复权口径 · **数据版本** · 快照列）。**本轮自己抓出 D3 漏了一条**：**粗筛阈值必须进键** —— 它决定四态里的「被粗筛剔除」桶，不进键就会"改了阈值、界面还展示旧分桶" = **静默陈旧**。同批校正命中边界：`asof` **留在键外**（矩阵本来就是全日期的）⇒ **切日期 / 换统计窗口 / 重开同一视图 = 零成本**，而**改粗筛 / 改公式 / 改标的域 = 必须重扫**（诚实告知 + 进度条，不假装能"重放"）；命中时 `status/counts/snapshot/detail` 属"扫描时那个 asof" ⇒ 切日期后一律走 `status_on(date)` / `counts_on(date)`（写进 docstring + 主案 + §11.5-59）。**失效做进键里**：数据版本 = 分区 `.parquet` 的 `(名, 字节, mtime_ns)` 摘要 + 文件数 ⇒ 数据一变键就变 ⇒ **自动不命中**，结构上就不存在"忘了检查过期"这种漏。另有 LRU 双闸（条数 + 字节）+ `stats()` 占用可见可清（§10-10 数据主权）。验收：`smoke_chart` **632 → 655 项**（新增 23 项全绿，含 **"数据一变 ⇒ 键必变 ⇒ 必然不命中"的负向断言**、**"缓存切片 == 强制重扫"逐位一致**、**源码级"本模块不写盘"** 三条硬护栏），`smoke_pages_overlay` **396** 全绿，全仓 compileall 通过。**未 push**（APP 版本仍 `1.27`）。 |
| v6.34(文档v6.34) | **§7-B1/B2 STEP 1：横截面内核 `core/cross_section.py` 落地（M2 横截面 / M3 广度）** | 新建 `core/cross_section.py`（**564 行**，新建即越 400 线 ⇒ 按 §4 口径**登记不返工**）。核心 = **四态**（命中 / 未命中 / **数据不足** / 被粗筛剔除）+ **粗筛漏斗**（D5 全阈值：成交额 / 价格 / 有效天数 / 非停牌 / 剔ST · 剔一字板 / 涨跌幅 / **换手率** / **流通市值**）+ **M3 逐日广度** + **uint8 缓存矩阵**（`status_on(date)` ⇒ 改日期秒回，D3 的 22 MB/全市场口径落地）。**三个"设计即防错"的点**：① 三态铁律写进代码 —— `min_bars` / 缺列 / 停牌 / 当日无行 ⇒ **「数据不足」**，绝不并进"未命中"，且**广度分母 = 通过粗筛的有效样本**（不是全市场只数）；② **数值为空(NaN) ≠ 不满足阈值** —— `ds.dataset()` 全量扫会把缺列补成"一整列 NaN"，只判 `col in df.columns` 会把它静默判成"成交额不达标"（§11.5-57）；③ 性能 —— 逐日家数走「日期→整数下标 + **一次 `np.bincount(pos*4+状态码)`**」，不写 per-row Python 循环（§11.5-58）。**与 M1 同口径**：`prepare_frame` ≡ `BacktestEngine.run` 的数据准备，并有**源码级断言**钉住（改一处漏一处立刻红；此条正是 §11.5-11「同类防护只改一处」的正解）。验收：`tests/smoke_chart.py` **589 → 632 项**（新增 43 项全绿），`smoke_pages_overlay` **396** 全绿；真实行情 368 只端到端 **2.60 s**（7.06 ms/只），四态分类完备 + M2/M3 同源自洽 + 缓存切片 ≡ 重扫，全部在**真数据**上再验一遍。⚠ 遗留：`vline` 拖动断言**偶发**红（2 跑 1 红，HEAD 基线 589/0 也非每次复现），疑似"竖线 1px 命中宽度 + 像素取整"的抓取点问题，**本轮未动**（属既有项）。 |
| v6.33(文档v6.33) | **§7-B1/B2 STEP 0：四个探针实测 —— 立项拍板被自己推翻 3 条（零业务代码）** | 先跑 P1–P4 再动代码，结果推翻立项时的 3 项拍板：① **落盘面板 `kline_panel` 取消**（P1 实测 IO **不是**瓶颈：全市场 `dataset` 一次扫 ≈2.6 s、列投影只省 4%、row group 全 = 1 ⇒ 尾部窗口无收益）⇒ 改**会话内存**，且缓存**只存"结果矩阵"不存价量宽表**（22 MB vs 500 MB）；连带**数据管理页结构一字不动**（原"方案 B 分组"取消）；② **换手率其实可用**（P1b 发现 `turnover` / `outstanding_share` **本来就在分区里** —— 源自新浪源透传、覆盖 98.6%，代码里 `grep` **0 命中**，属**隐性依赖**）；③ **复权只能单口径**（`kline_daily_raw` 只有 1 只 ⇒ 复权因子算不出来）⇒ 本轮**只上前复权 + 界面明示**，raw 备齐后再开。另记录 **4 个探针数字**：IO 2.6 s / 面板构建 490 MB(f64) / 引擎逐只 **1.60 ms/只** vs 宽表向量化 **0.409 ms/只（仅 4×）** / 端到端 368 只 1.79 s（全市场外推 ≈26 s）。**两处自我修正**：宽表 `shift` 与逐标的口径有 **0.1%** 分歧（且"按位置截断"会造出 86–168 处**假差异** ⇒ 先定对齐再谈一致性）；"漏斗降一个数量级"在宽条件下**不成立**（实测只砍 3.8%）。产出：主案 **B2/B3 + D1–D8 全部改写 + §9.1 / §9.2 两条实测发现 + §11.5-57/58**，并同步修正 `design/1.27-.../README.md` 的过时结论（标注为历史讨论稿）。**零业务代码**。 |
| v6.32(文档v6.32) | **文档分层重构：`JIAN_RULES.md` 297k → 84k（可一次读完）+ `docs/` 三册 + §10-14 防漂移纪律** | 起因 = 用户发现"每次访问 rules 都无法全量读取、只能检索"，问"会不会对你有影响"。**诚实结论：不影响"能不能干活"，影响"干得对不对"且是静默的** —— 297k 字符 vs 100k 单次读取上限 ⇒ 物理上读不完 ⇒ ① 猜不中关键词即检索不到；② **同一数字十几处重复且互相矛盾**（"当前值 1.23" vs 代码 1.25）⇒ 已真实造成一次版本号事故；③ 历史行与现状行无法区分。**根因 = 现状与历史混在一起 + 同一数字多处重复，而非长度本身。** 做法：① 文件头 661 行版本叙事 + §8 全表 → `docs/JIAN_HISTORY.md`；② 6 份已完成主案（§7-B3/B4/B5/B6/B8/B9）+ §9 版本子节（F…V / M / N / O 系列）+ §11.6 历史刷新块 → `docs/JIAN_ARCHIVE.md`；③ §11.5 踩坑 50 条 → `docs/JIAN_PLAYBOOK.md`（主文件留**编号索引**，**编号不变**）；④ 文件头换成 **≤60 行「当前状态快照」（含"断点"一行）+ 文档地图 + 三条防漂移纪律**；⑤ 新增 **§10-14**：主文件硬指标 **< 100,000 字符**、**同一事实只存一处**（版本号→§9-A / 断言数→§11.7 / 行数→§4 红榜）、**历史行数字冻结**、收工必更新断点；⑥ §10-13 白名单新增 **`docs/`** 角色目录。**逐行无损校验：非预期缺失 0 行**。零业务代码（只动文档与目录）。APP 版本升 **`1.27`**（§9-A 三处同批同步）。 |
| v6.31(文档v6.31) | **§7-B1/B2 立项：M2 全市场横截面筛选 / M3 广度统计（设计定稿 · 零业务代码）** | 用户 2026-09-19 看完可行性方案与 HTML 原型后拍板 6 条：① **主轴 = 派生扫描面板 `kline_panel`（日期×标的宽表，**按年分区**）+ 两段式漏斗**（先用便宜列粗筛砍到几百，再跑完整公式）——**不做"逐只读文件直接算"**；② **复权一次到位**：面板存「不复权 OHLCV + 复权因子（= `kline_daily ÷ kline_daily_raw`，两分区都已本地）」，**指标按用户需求复权**（不复权/前复权/后复权）；③ **面板可见性 = 方案 B**：🗄 数据管理 左栏**分两组**「行情源分区」/「派生缓存」，明细与勾选删除共用一套，**底部动作按类型切换**（派生 → 从日线重建 / 增量补齐 / 释放空间）；④ **缓存 L1 先、L2 后置**（重复扫描 > 2s 才做 L2，`scan_store` 预留位）；⑤ **粗筛多阈值 + 内置默认 + 全部可调**（成交额 / 价格 / 上市天数 / 非停牌 / 剔ST / 剔一字板 / 涨跌幅 / 相对量…）；⑥ **三态铁律**：命中 / 未命中 / **数据不足（绝不用 0/False 冒充）**，广度必须显示"有效样本数"。**两条诚实边界**：`valuation`/`fin_report` 无数据源 ⇒ 估值类筛选与**真·换手率本轮不做**（先用「相对量 `V÷MA(V,20)`」替代，接股本数据源登记为后续项）；扫描页**只读湖、不联网**（联网一律走 `MarketSyncService`）。**核心设计 = 一个引擎两种视图**（M2 = 某日纵向取列 / M3 = 跨标的按日求和），M3 天然增量（新交易日只算最后一天）。产出：`design/1.27-scan-breadth-ui/`（README 方案 + index 总览 + a-scan / b-breadth 两个可点原型）+ **§7-B1/B2 主案规格**（拍板记录 / 现状实测 / D1–D7 规格 / UI 规范 / 影响面 / STEP 0–8 / 风险 / 不做清单 / 验收口径）。**零业务代码变更**；APP_VERSION 未动（仍 `1.26`）。 |
| v6.30(文档v6.30) | **发版 `1.26`：§9-U 其余页面版式收口 + §9-L 剩余拆分（`review.py` 1067→173 · `data_feed.py` 827→365）** | 在既有暂存改动（`ui/widgets/review_layout.py` + `core/preferences.review_ui`）基础上接着收官。**§9-U（复盘页版式收口）**：旧版是宏观(日历·图表) `stretch=5` / 微观(清单·编辑) `stretch=4` 的**写死比例平铺** ⇒ 短屏上交易明细与回放图被挤死、且用户无法"拉大图表收矮清单"；改为 **`macro_micro_splitter` 可拖竖向分栏**（子项不可折叠到 0）+ 分栏高度**记住上次**（偏好键 `review_ui.v_sizes`，拖动后 300ms 防抖落盘）；主体显式 `stretch=1` 防顶部 QLabel 吞掉富余高度（§11.5-13）。常驻行 ≤3（标题行 + 筛选行 + 分栏主体吃满），符合 §10-14。另核对 `data_manager.py` / `dashboard.py`：前者是"工具页"（控件即主体）、后者纯展示（无配置行），均**已是 ≤3 常驻行、无需改造**。**§9-L（剩余拆分）**：① `ui/views/review.py` **1067 → 173**，行为分居 5 个新模块 —— `review_layout.py`(310 版式装配) / `review_charts.py`(244 日历·月度·时长) / `review_playback.py`(238 交易回放双锚点+单点降级) / `review_editor.py`(310 清单·详情头·保存·孤儿缝合) / `review_flow.py`(197 筛选·导航·视图刷新)，约定 = 1.22/1.23 同款**"状态留页面、行为搬模块 + 同名薄壳"** ⇒ **成功判据 = 同一套断言零改动**；② `data/data_feed.py` **827 → 365**（纯重构，拆出 `feed_cells.py`(208 单元格/工作表工具) / `feed_fifo.py`(218 模型+FIFO) / `feed_report.py`(56 结算月报) / `feed_months.py`(63 月份推断+资金链)，公共名**再导出** ⇒ `from data.data_feed import ...` 零改动）。**验收**：`smoke_chart` **589** + `smoke_pages_overlay` **372 → 396（+24：复盘页迁移护栏 + §9-U 分栏不变量）** 全过 + 全仓 compileall；`data_feed` 拆分另跑**临时逐位回归探针**（`git show HEAD` 旧实现 vs 新实现：标量工具 / FIFO / 月份推断 / 漏月检测，**0 差异**，跑完即删）。**发版三处同批同步（§9-A）**：commit 首词 `1.26 …` + `settings.py:APP_VERSION = "1.26"` + `version.json = 1.26`。 |
| v6.29(文档v6.29) | **H-10 第五轮：斐波扇形加 1×8 线 + 回归通道"三端"（可调）+ 自查自改两条真坑** | 用户："**给斐波那契扇形加个 1×8 的线**（现在的线有点少）……**回归通道**点击的起点终点并不对应通道该在的地方，正常通道线应该**分为三端（起点、终点、通道区间）**，而且调整后整个通道**不会相应调整**；画线工具还有没有问题，**自查自改**。"⇒ ① 斐波扇形补 **1/8（=1×8=0.125，最缓那条）** ⇒ `FIB_FAN_RATIOS=(0.125,0.382,0.5,0.618)`；② 回归通道改 **三端**：p1/p2=拟合区间、**p3=通道区间**（到中线距离=带宽，默认 ±2σ，带 ⇕ 手柄可拖），新增 `ShapeSpec.reshape`（拖完把**中线重新贴回回归结果**，治"中线在 A、上下轨在 B"）与 `ShapeSpec.normalize`（落库前**纯计算**规范化锚点，治"存储的锚点 ≠ 图上画的线"）；③ 自查挖出 **两条真坑**：**点选容差被当数据单位用**（`HIT_TOLERANCE=6` 直接喂图元坐标 ⇒ 小量程图上"点哪儿都算命中"）⇒ 用 `viewPixelSize()` 换算像素；**区间不夹 + 刚进场景回读**（手柄拖到窗口外按未夹区间外推 ⇒ 存进 76.8；落库后立刻回读图元 ⇒ 图元变换还是旧的，把 104 读成 13089）⇒ `_reg_clamp` + 改纯计算规范化（§11.5-53/54/55/56）。**另按 §4 处置拆了一个越线文件**：`annotation_shapes.py` **587 → 296**（`place_*`/`read_*` + 两个上下文对象 + 回归几何搬进新增的 `annotation_layouts.py`(360)，两文件都回到 400 线内）。**验收**：`smoke_chart` 577 → **589**（三端/带宽可调/中线贴回/⇕ 真实拖动/像素级点选/斐波弧缩放重算）、`smoke_pages_overlay` **372**（32 种"点出来"落点校验改成按可视区点击 + 回归通道按拟合值校验）、全仓 compileall、离屏截图人眼核验。APP_VERSION 未动（仍 1.23，**未 push**） |
| v6.28(文档v6.28) | **H-9 第四轮：画线工具"能操作"三连真 bug + 甘氏扇形重做（两点版）** | 用户："**扇形线完全无法使用，写上去后无法移动、自动上下对称无法调整，综合看看画线工具到底出什么问题**" ⇒ 综合复查（真实鼠标事件 + 探针）挖出**三个跨类型真 bug**：① **文字/标记类拖不动也点不中** —— `_ClickableText` 接受 press 后 pyqtgraph **不再发** `mouseClickEvent`/`mouseDragEvent`（框架既定契约："只在没有任何图元接受 press 时"才发），而旧断言是**手搓假事件**直接调 `mouseDragEvent` ⇒ 三个版本一直绿（§11.5-49/50）⇒ 改走 **Qt 三件套**（press/move/release：按住跟随、没移动=点击选中、移动过=松手落盘），位移换算到父坐标系、拖动阈值用**像素**；② **ROI 类点线身选不中**（`pg.ROI` 默认 `acceptedMouseButtons=NoButton` ⇒ `sigClicked` 永不发；`PolyLineROI` 线段是子图元；附属图元没人挂点击）⇒ `connect()` 统一声明左键 + 新增 **`_pick()` 按形状统一拾取**（描边放宽 6px，"点标注任何一处都算选中它"）；③ **甘氏扇形重做**：一点版只能让程序猜角度（用户无输入口）⇒ **两点版**（起点 + 参考点，**参考线就是 1×1**）：方向随用户拖（不再自动对称）、陡缓随用户拖，9 条经典倍率 + `gann_label()`；`view_dependent` 只剩斐波弧。**验收**：`smoke_chart` 574 → **577**（含**32 种逐个真实鼠标"拖得动 + 点得中"扫描**）、`smoke_pages_overlay` **372**、全仓 compileall、离屏截图人眼核验（上/下两个扇形各由两点定方向）。APP_VERSION 未动（仍 1.23，**未 push**） |
| v6.27(文档v6.27) | **H-8 第三轮：预览吃掉点击 + 扇形基准改"可视区间对角线"** | 用户："**扇形线无法在图片中画出，点击后直接在十字光标生成一个扇形线**" ⇒ ① **预览图元把点击吃掉了**（预览锚点画在光标处，`_ClickableText`/`_RegionBand` 按下就 accept ⇒ 场景不发 `sigMouseClicked`）⇒ 预览一律 `setAcceptedMouseButtons(NoButton)` + **真实鼠标事件断言**（§11.5-47）；② 扇形按"屏幕 45°"画 ⇒ 实测所有射线在前 1/3 屏内冲出画面 ⇒ 基准改**可视区间对角线**、上下各 4 条（§11.5-48）；顺带预览改虚线、场景钩子自愈、删死代码 `unit_per_bar`。`smoke_chart` 574 / `smoke_pages_overlay` 372 |
| v6.26(文档v6.26) | **画线交互重做「在图上点出来」+ 通道修正 + 32 种全通 + H-7 第二轮实测修复** | 用户实测三条（默认线不合体验 / 通道成了四角形 / 还剩 5 种没做）⇒ 立项 §7-B9，7 条拍板后施工完成：① 新增 **`annotation_draw_session.py`**（绘制会话：选类型 ⇒ 依次点锚点 ⇒ 橡皮筋预览**复用规格表** ⇒ 落库并**自动回浏览模式**；Esc/右键取消；**点到已有标注也算落点**——绘制期临时 `setAcceptedMouseButtons(NoButton)`，因 pyqtgraph 图元 accept 后场景不再发 `sigMouseClicked`；**允许重合**）；② **平行通道修正**：基线 ROI + 平行线 + **`_FillBand` 填充带** + **`⇕` 宽度手柄**，平行性由"基线动 ⇒ 宽度点跟着平移"的结构保证（判据读图元不读存储）；③ **最后 5 种**：回归通道（`bind()` 喂 `closes`，最小二乘 ±2σ）、波浪（**固定 5 拐点**，弃"不定长"避免与"右键取消"打架）、头肩（3 点 + 颈线(近似)）、**甘氏扇形（挂 `sigRangeChanged` + 2% 节流重算，1×1 恒 45°，用户点名不许缩水）**、斐波弧（屏幕半径采样）；④ **「➕ 添加标注」删除**（按钮+薄壳+护栏断言同批）；⑤ 抓到一个**静默覆盖**坑：新写的甘氏 `deco_fan` 被同文件后定义的斐波扇形 `deco_fan` 覆盖（§11.5-41）。**H-7（2026-09-19 用户第二轮截图反馈 · 五项真问题）**：① 通道下线画成了**水平线**（"只平移 y 又共用同一个值"）⇒ 逐点平移 + 补"下线斜率 == 上线斜率"断言（§11.5-43）；② **填充盖住 K 线** ⇒ alpha 18/36 + **唯一口径** `chart_style.annotation_fill`（深色主题只改这一处，§11.5-44）；③ **文字标注不跟随点位**（声明式 `rebuild` 忘写）⇒ **删掉该字段**、由"有 deco 就重建"推导（§11.5-45）；④ 扇形该是**射线**（"代表未来可能支持的位置"）⇒ 斐波扇形/甘氏/水平射线统一延伸到数据末尾、甘氏改为**单点起点**（§11.5-46）；⑤ 顺带：单点默认落点从最右一根挪到 0.65 处（避免文字被右边缘裁掉）、场景信号钩子改**幂等替换**（图表重建后不挂旧场景）。**H-8（2026-09-19 用户第三轮：扇形线"画不出来"）**：① **预览图元把点击吃掉了** —— 预览锚点画在鼠标位置上，`_ClickableText`/`_RegionBand` 按下就 `accept()` ⇒ 场景**不发 `sigMouseClicked`**（v6.26 只静音了"已有标注"、漏了"预览"）⇒ 用户"怎么点都落不了地"，只见预览跟着光标跑；修 = 预览一律 `setAcceptedMouseButtons(NoButton)` + **真实鼠标事件**断言（关掉修复即变红，§11.5-47）；② **扇形按"屏幕 45°"画 ⇒ 实测 5 条射线全在前 1/3 屏内冲出画面**（本软件并不知道甘氏的"每点价值/时间周期"）⇒ 基准改成**可视区间对角线**（1/8…8/8，最陡那条正好落在对角线上 ⇒ 永远看得见）并**上下各 4 条**（上=未来压力、下=未来支撑，用户点名的用途），仍随缩放重算（§11.5-48）；③ 顺带：预览一律虚线、"点工具那一刻补挂场景信号"（自愈）、删除死代码 `unit_per_bar`。验收：`smoke_chart` 571→**574**、`smoke_pages_overlay` **372** + compileall + 离屏截图与探针双核验。APP_VERSION 未动（仍 1.23，未 push） |
| v6.25(文档v6.25) | **画线工具补齐（5 种 → 27 种）** | 目录里 22 种「已登记未实现」全部落地：模型层新增 22 个 `KIND_*` + 语义档位常量（`FIB_EXT/FAN/TIME` + `PERCENT_RATIOS`）+ 文字三态口径（用户填 / 价位派生 / 符号）；**画法拆成三个新文件** —— `annotation_items.py`(图元小件，含新增 `_RegionBand`，因 `LinearRegionItem` 不发点击信号) / `annotation_decos.py`(附属图元：档位·标签·箭头·周期竖线·测量文案) / `annotation_shapes.py`(**规格表**：`ShapeSpec` 一种类型一行 = 画法/回读/默认落点/是否重建附属图元)；`annotation_layer.py` **418 → 290** 且**不再认识任何具体画法**；`_rebuild` 通用化 + `snap` 回正（水平射线拖歪也存成水平）。**仍不做 5 种**（回归通道/甘氏扇形/波浪/头肩/斐波弧）⇒ 目录照常展示并明说「还没实现」，卡片抬头写「27 种可画 · 共 32 种」。验收：`smoke_chart` 536→**552**、`smoke_pages_overlay` 362→**367** + 全仓 compileall + 离屏截图人眼核验；顺手修掉"拿真类型 `arrow` 当未知类型样本"的假断言。APP_VERSION 未动（仍 1.23，本轮未 push） |
| Phase 1~3 | 基础 MVP | UI 组件物理隔离、SQLite 防重、CFMMC 跨月缝合 |
| Phase 4.1 | 基础设施 | Parquet 湖、TAEngine、AkShare 封装 |
| v1.1 | 数据契约重构 | 双时间→单一 `trade_time` 锚点；旧库自动无损迁移；删虚构"时长分析" |
| v1.2 | 交割单字段补全 | 补 entry/exit 价与 `*_fill_time`/乘数/time_source；持仓与月报解析；
        FIFO 加权分配；跨月真缝合(顺序无关)；漏月告警；孤儿显式化；净额口径；时间双轨 |
| v1.2.1 | 微观测算修正 | 盈亏分配"点数×乘数×手数"自洽、孤儿片接余额；孤儿语义收敛(CFMMC专属)；
        手工录入账户仅真实+可新建 |
| v1.3 | 范围聚焦+时长回归 | 删股票导入链路；持仓时长三级口径回归(卡+复盘页签)；手工录入补开仓时间(选填) |
| 阶段A(文档v5.1) | 回测信号升级 | 多函数段共享变量池；条件组 Gate(全部/任一/至少N→COUNT_TRUE)；策略快照兼容 |
| 阶段B(文档v5.2) | 回测风控落地 | 引擎风控离场器(止损/止盈/移动止盈/最长持仓,盘中触发硬规则优先) + exit_reason 全链路 + 明细着色 |
| 阶段C(文档v5.3) | 指数门控落地 | 新浪指数接口 + index_daily 数据湖 + align_by_date 对齐 + ③指数Gate(买许可AND/卖破位OR)，引擎零改动 |
| v5.6(文档v5.6) | 回测 UX 补强 | 风控行箭头统一为原生（根治"上箭头只有右半边可点"）+ 区间下沿 2018→2016 + 新增近3月/近6月/近5年三档；新增 §10-9 控件一致性铁律 |
| v1.4.0 | 回测总集 + 甲方案收尾 | 多函数段共享池 / 条件组 Gate / 风控离场器 / 指数 regime 门控；年度面板净额口径统一；花名册覆写安全闸；Dashboard 每日净额日历(C1)；`APP_VERSION` 与 `version.json` 同步升 1.4.0 |
| v1.4.1 | 数据管理页 + 同步收敛 | A3 落地：数据湖删除/清点原语；`MarketSyncService` 同步门面（增量合并 + 温柔抓取 ThrottlePolicy）；`ui/workers.py` 统一线程；第 6 导航页 + 批量预下载弹窗；§9-H 架构债清账；**含 v5.9 独立复查加固**（§9-M1~M4 时效/空增量/起点/竞态） |
| v5.12(文档v5.12) | 全仓通读对账（**无代码变更**） | 第二次"陌生代码"复查：核对 30 个 .py，产出 §9-O 系列 10 项发现；校正 §3/§4 的 KPI 项数与全量行数；§4 补 `.gitignore` 与 `core/formula/` 五个子文件；刷新 §11.6 下一步顺序 |
| v5.13(文档v5.13) | 第一/第二梯队落地 | O-1 盈亏筛选改净额（用户拍板）；O-2 三个 UI 线程迁入 `ui/workers.py`；O-3 同步 docstring 起点改 2010；O-4 云端同步改默认增量（本地有=增量/没数据=全量，改名 `sync_cloud`）+ 状态回执；O-5 回测页补竞态守卫×2；O-7 新增 `ui/widgets/chart_style.py`（4 合 1，PlotWidget/PlotItem 兼容）；O-8 复盘页三小坑 + 画廊失效路径不再静默丢弃。**25 项冒烟断言 + 离屏构造主窗口/全部弹窗通过**。APP_VERSION 未动（仍 1.4.1）。**修订**：O-4 新增的同步状态 QLabel 触发了 Qt 布局坑（顶部栏末尾 QLabel 会吞掉主区高度），已给 main_splitter 加 `stretch=1` 根治，并固化为 §11.5 第 13 条 |
| v5.14(文档v5.14) | §7-A2 导出落地（半程） | 回测页「导出明细」按钮 + `_last_meta` 配置快照（start_backtest 定格）+ `_compose_result_csv`（逐笔明细 + 净值曲线，utf-8-sig）。**只导出、不删除**（用户拍板）；「结果历史存档」列为 §7-A4 远期方案。**14 项导出断言全过** + 全仓编译通过。APP_VERSION 未动（仍 1.4.1）。⚠ 其"注释头裸写"做法已被 v5.16 推翻 |
| v5.15(文档v5.15) | 导出升级：PNG 报告图 | 用户拍板：① CSV 移除 200+ 行净值段（溯源=参数+逐笔，曲线由图承担）；② PNG 报告图含离场原因饼图；③ 导出按钮改下拉菜单（CSV/PNG 两动作）。新增 `ui/widgets/backtest_report.py`（单页 1120×660 = 标题 + KPI + 净值曲线 + 饼图 + 参数简表，离屏 grab 渲染）；离场原因 标签/配色/风控文案上收 `core/backtest.py` 三处同源。**16 项断言全过** + 全仓编译通过。APP_VERSION 未动（仍 1.4.1） |
| v5.16(文档v5.16) | CSV 兼容性修复（WPS 实测） | 用户用 WPS 打开 CSV 发现"函数段·第三部分图形绘制"被拆列成乱码：根因 = 表头注释行裸写，含英文逗号的 `STICKLINE(...), COLORFF0000;` 被表格软件当多列。修复：`_compose_result_csv` **逐行单格 csv 转义**（含逗号自动引号、空行 writerow([])）。**9 项 csv.reader 重解析断言全过**。APP_VERSION 未动（仍 1.4.1） |
| v6.0(规划) | §7-B3 公式统一绘图（**P0 · 设计定稿，无代码**） | 用户复盘定位"绘图语句从没被画出来"= `program.py` 长期 SKIP 的历史欠账；定稿"引擎产出统一绘图 IR + 唯一 OverlayPainter"为全图表统一修改窗口（避免未来逐图一对一改）。里程碑 M0 契约层 / M1 求值层 / M2 渲染+回测页 / M3 收尾 / M4 远期全图复用。完整规格见 §7-B3 主案；优先级见 §11.6 |
| v6.1(文档v6.1) | 图表架构总纲（**升级 B3 为骨架，无业务代码**） | 用户拍板：把 §7-B3 从"公式叠层单点方案"升级为**四层架构**（① 引擎 IR `core/formula/draw.py`；② 图表宿主 `chart_pane.py`+`chart_host.py`；③ 渲染器 `draw_overlay.py`；④ 标注对象 `data/annotations.py`+`annotation_layer.py`）；厘清**两条管线**（A 公式叠层=重算不持久 / B 用户标注=持久可单删）；定 **P0–P8 路线**（P0 ChartPane 插在 M2 之前 / P2 ChartHost 多窗格 / P3 回测页验证 / P4 行情页公式叠层 / P5 副图 / P6 标注 JSON 先行且 API 与存储解耦 / P7 函数资产互送 / P8 通达信式重做）；内置指标与用户公式**统一图层协议**（D4）。另据第三次核对新增 §9-P（净额展示层残留 / 行数漂移 / FUNCTIONS 计数）；§4 行数按 v6.1 实测重刷 |
| v6.2(文档v6.2) | **P0 + P1 落地** | §7-B3 总纲开工第一批。**P1**：新增 `core/formula/draw.py`(124 行：`COLOR_TABLE` 含 COLORRRGGBB 直通 / `DrawSpec`/`DrawData` IR / 属性校验)；`program.py` 113→**265 行**，语句分类 **3 态→4 态**（ASSIGN/OUTPUT/DRAW，**SKIP 移除**，未知函数/颜色/属性/元数一律报错）；新增 `execute_programs_with_draws()`（同一 EvalContext 一遍同时出变量+draws）。**P0**：新增 `ui/widgets/chart_pane.py`(76 行)，`ChartPane` = PlotItem + 公式叠层容器 + 标注容器，渲染器入参从此固定为 pane。**验收**：新增 `smoke_chart.py` 冒烟入口（当时 57 项断言全过）；全仓 compileall 通过；主窗口离屏构造通过；`execute_programs` 旧行为逐位不变（Gate/检测/指数门控零影响）。APP_VERSION 未动（仍 1.4.1）。下一步 P2/P3 |
| v6.3(文档v6.3) | **P2 多窗格落地** | 新增 `ui/widgets/chart_host.py`(262 行)：`ChartHost` = 主图 + **N 副图**，x 轴联动（`setXLink`，pyqtgraph 0.14 用 `ViewBox.linkedView(axis)` 判联动）、**只有最下窗格显示底部轴**、窗格增删（`QGraphicsGridLayout` 不自动塌缩 → 删后重排行）与行高度权重、`pg.DateAxisItem` 日期轴切换、**跨窗格同步十字光标**（竖线贯穿全部窗格 / 横线只留悬停窗格 / 顶部数值回执）。`chart_style.py` 抽出 `style_axis()`（换轴后重新着色的唯一来源，防 §10-9 类问题）+ `CROSSHAIR_COLOR`。十字光标**不进** `ChartPane` 的叠层/标注容器（否则 `clear_overlays()` 会误删）。**验收**：`smoke_chart.py` 扩到 **97 项断言全过**；全仓 compileall + 主窗口离屏构造通过。APP_VERSION 未动（仍 1.4.1）。下一步 P3 |
| v6.4(文档v6.4) | **P3 公式叠层渲染 + 回测页接入** | 新增 `ui/widgets/draw_overlay.py`(210 行)＝全 app 唯一叠层渲染器：`OverlayPainter(pane, bars_x).render(draws)`（line→PlotDataItem、stick→自绘 `_StickItem` 支持 width/空心、icon→ScatterPlotItem + 图标号映射），附 `slice_draws` / `overlay_extent` 宿主工具。新增 `chart_style.ensure_contrast()` **对比度守卫**（黑底公式的 COLORWHITE/COLORYELLOW 在白底仍可见；判据 = 亮度差 ≥0.30 且 WCAG 对比度 ≥3:1，不足则**保留色相**地压暗/提亮）。`chart_pane` 持有源控件强引用（防 Qt 对象被 GC）。**回测页接线**：`_on_data_ready`/`detect_function` 改走 `execute_programs_with_draws`（不二次求值、错语句检测期即报）；`_render_kline` 用 **"排序后位置→原始行号"映射**切窗口（防错位漂移）；`🕯 K线买卖点` 新增「显示公式叠层」开关（只切可见性不重置缩放）；y 范围纳入叠层。**验收**：`smoke_chart.py` **126 项** + 新增 `smoke_backtest_overlay.py` **20 项页面级**（含离屏 grab 像素差异证明真画上去）全过；全仓 compileall + 主窗口离屏构造通过。APP_VERSION 未动（仍 1.4.1）。下一步 P4 |
| v6.5(文档v6.5) | **事故修复：存量函数回归** | 用户实测反馈"已保存的函数突然跑不了"，报错 `无法识别的语句 'STICKLINE(...), COLORFF0000': 公式末尾存在无法解析的内容: ','`。**根因 = v6.4 回归**：`_compile_draw` 把整条绘图语句（含**尾部颜色属性**）直接喂 `parse()`，而通达信允许 `STICKLINE(...), COLORFF0000;`；旧版整行 SKIP 所以"能用"，且 `start_backtest` 依赖检测成功 ⇒ 函数彻底跑不起来（§9-Q-1）。**修三处**：① 新增 `_split_draw_attrs()` 按顶层逗号摘掉属性尾巴再解析（与 OUTPUT 同源）；② 未实现绘图函数**不再 hard fail**，登记 `kind='unsupported'` + 检测期非阻断提示（`CompiledProgram.unsupported`/`unsupported_count`，`_set_detect` 文案进 tooltip）；③ `STICKLINE(cond,P,P,w,0)` 零高度时改画 **cosmetic 横杠**（Q-3）。用**用户真实函数**端到端复验通过（解析 OK / 27 变量 / 2 线 + 4 色柱 + 1 图标）。断言：`smoke_chart.py` **134 项** + `smoke_backtest_overlay.py` **25 项** 全过；全仓 compileall 通过。APP_VERSION 未动（仍 1.4.1） |
| v6.6(文档v6.6) | **P4 落地：行情页公式叠加** | 新增 `ui/dialogs/formula_overlay.py`(144 行)：公式编辑器（复用 `FunctionSegments` + 参数框 + 检测/清空/应用），自检口径与回测页同源。`ui/views/market.py` 新增「🧮 自定义公式」区：`cb_formula`（与均线/布林带**同一个 `render_charts` 开关行为**）+「编辑公式…」+ 状态回执。**核心 = §7-B3 D4 统一图层**：`_builtin_layers()` 把 MA/BOLL 翻成 `DrawData`，`_formula_layers()` 把用户公式求值成 `DrawData`，二者交给**同一个 `OverlayPainter`** 上到同一个 `ChartPane` —— 对比度守卫/线型/粗细/空白处理**自动一视同仁**。抽出三件共用件消除跨页重复：`core.utils.parse_params_text`、`core.utils.synthetic_bars`、`core.formula.program.probe_missing_parameters`（`backtest.py` 全部改为委托，删除本地副本与 `import re`）。公式在某标的执行失败**不打断整页渲染**，只在面板给 ❌ 提示。`smoke_backtest_overlay.py` 更名 **`smoke_pages_overlay.py`**。**验收**：`smoke_chart.py` **146 项** + `smoke_pages_overlay.py` **36 项** 全过；全仓 compileall + 主窗口离屏构造通过。⚠ `market.py` 350→**425 行**（已越 400 线，P5/P8 必须新建文件）。APP_VERSION 未动（仍 1.4.1） |
| v6.7(文档v6.7) | **P5 前置：公式副图 + 对话框返工 + 共用组件修 Bug** | 用户实测驱动。**修 `ui/widgets/function_segments.py` 两个真实 Bug**：① 僵尸段（`removeWidget+deleteLater` 留下可见重复控件 → 对话框出现两个"函数段 1"）改为先 `setParent(None)`；② 版面塌陷（编辑框 `setFixedHeight` 让多余高度变成标签-编辑框之间的大空档）改为 `Expanding` + 最小高度 110，多余高度变成编辑区。**重做 `ui/dialogs/formula_overlay.py`**：删重复文案、加**目标窗格** + 随选动态说明 + **示例模板菜单**（选模板自动切目标）+ 编辑区入 `QScrollArea`。**行情页支持副图叠加**：`market.py` 新增「公式副图」窗格（x 联动、y 独立、跨 0 给零轴）。**新增 `ui/widgets/chart_layers.py`(65 行)**：`builtin_indicator_layers` + `layer_value_range` + `scale_mismatch_hint`。**验收**：`smoke_chart.py` **161 项** + `smoke_pages_overlay.py` **58 项** 全过。APP_VERSION 未动（仍 1.4.1） |
| v6.8(文档v6.8) | **P5 落地：多副图 + 窗格编排收编 ChartHost** | ① 每段可选目标窗格（主图/副图 1/2/3）：`FunctionSegments` 新增 `accessory_factory`（每段附件控件，回测页不传 ⇒ 零影响）。② 引擎新增 **`execute_programs_with_draws_grouped`**（draws 按函数段分组）—— 各段共用**同一个变量池**，所以"每段不同目标"只能**一次求值、再按段归位**；三个执行入口收敛到唯一的 `_run_programs`。③ **行情页窗格编排迁移到 ChartHost**（§10-12 违规清账）：`ChartHost` 补 `fixed_height` / `bottom_axis_mode`(hide/no_values) / `clear_sub_panes()` / `clear_pane_content()`（不会误删十字光标）/ `setBackground()`。④ **多副图按需创建**。⑤ 新增 `ui/widgets/indicator_panes.py`（成交量/MACD 内容构建搬出页面）；量柱/MACD 仍用原生 BarGraphItem（P5 明确保留的例外）。**验收**：`smoke_chart.py` **161 项** + `smoke_pages_overlay.py` **63 项** 全过（含跨段共享池/窗格顺序/x 联动/y 独立/钉死高度/刻度值只在最下窗格）。**market.py 463→448（首次下降）**。APP_VERSION 未动（仍 1.4.1） |
| v6.17(文档v6.17) | **§7-B5 立项：回测成交真实性（零业务代码变更）** | 用户提出"指标滞后 / 开盘容量限制 / 盘中信号要等次日"三个痛点，并拍板四项：① **T+1 修正默认开启且不给旧口径开关**（"数字难看无所谓，贴合真实最重要"）；② **「当日收盘」成交档照做**；③ **触发式委托（条件单）照做**；④ **P1（账户+容量+成本）/ P2（盘中即时成交）搁置**、设计留档。立项前用合成 K 线探针**实测 7 组**（跑完即删），坐实 5 条现状事实：F1 次日开盘成交 / F2 **引擎无资金无仓位**（1 股归一化）/ **F3 T+1 违规**（止损·止盈·max_bars 可当日买当日卖 ⇒ 低估风险）/ F4 成本只有佣金 / F5 数据无成交额。产出 **§7-B5 主案**：P0 规格（C1 三档成交时点、C2 T+1 闸门含"顺延到最早可卖根开盘价"、C3 触发价与跳空同源、C4 单一口径、C5 七条验收断言含**负向对照**）、影响面 5 条、实施步骤 0–5、以及 P1/P2 挂起设计（含明确"不做"清单）。APP_VERSION 未动（仍 1.20） |
| v6.18(文档v6.18) | **§7-B5 P0 落地（步骤 1–3）+ 用户指引返工** | ① **引擎**（`core/backtest.py` 315→**495**）：三档成交时点（`next_open` 默认 / `close` 当日收盘 / `trigger` 价格冲破·跌破才成交）+ **T+1 硬约束**（进场当根触发价格型风控 → 顺延到最早可卖根**开盘价**成交；`max_bars` 顺延到次根收盘；末根不建仓）+ **同根不重建仓**（判据 = **成交根**重合）。验收：C5 探针 **39 项**（含非空对照 + 借 `git show HEAD` 旧引擎做 40+40 组随机样本**逐位回归**，非预期差异 0）。② **口径连通 + UI**（`ui/views/backtest.py` 1570→**1691**、`data/strategy_store.py`、`ui/workers.py`、`ui/widgets/backtest_report.py`）：策略签名（**默认口径归并为空串** ⇒ 旧存档签名不变）、旧档回落、`_last_meta`、CSV 表头与「（T+1 顺延）」标记、PNG 报告图；新增「🎯 成交模型」行（探针 22 项全过）。③ **用户指引返工**（用户实测反馈"触发式条件单 / 触发跳数**完完全全看不懂**"）：文案全面换人话（`1 跳 → 0.01 元`、`当根 → 当天`、"信号作废"→"不买也不卖"）；新增**行内实时说明** + **教学弹窗** `ui/dialogs/fill_model_help.py`（一套固定价格数字三档对照）；参数只在第三档可见。纪律固化 **§10-10 追加条款** + **§11.5-22**；文案可懂性有正则级可测断言（24 项全过）。④ **步长 Bug 修复**（用户实测"买卖价要多等按一次上箭头就跳到 0.2 且无法继续上调"）：根因 = 公共工厂 `_risk_spin` 把步长写死成 `1 if decimals == 0 else 0.5`，对新的 2 位小数控件就是"一按顶到上限"；修为**随小数位自适应**（≥2 位 → `10^-decimals`）并把上限放宽到 1.00 元（§11.5-23）。回归：全仓 compileall 通过。⑤ **步骤 4 验收落地**：断言正式移植进两个冒烟脚本 —— `smoke_chart` 312→**356 项**、`smoke_pages_overlay` 150→**180 项**（含**文案行话正则检测**、**同根不重建仓非空对照**、CSV 8 列结构、教学弹窗文案、跳数步长；防污染名单加 `backtest_strategies.json`）；⚠ 原探针的 `git show HEAD` 逐位回归**故意不移植**（提交后 HEAD 会变），改为手算期望值。**用户真实资产端到端复跑**（临时脚本，跑完即删）：两个私有公式解析+求值通过（27/17 变量、各 7 条绘图指令 line/stick/icon）；**真实策略存档 2 套签名全部不变**；三档 + 风控全开对照 `next_open 49 笔(T+1 顺延 10)` / `close 41 笔(顺延 0)` / `trigger 33 笔(顺延 10)` —— 完全符合设计预期。⚠ 待办：**步骤 5 收尾 + 用户手动实测手感** |
| v6.23(文档v6.23) | **用户实测反馈驱动：复权差异定位 + 数据源脏数据护栏（§9-V）** | 用户报"转增股开不开复权看不出变化"+"7/26~30 这类大面积显示错误常出现"。**核对后是两个不同问题**：① 前者是**定义**不是 Bug（前复权只改除权日之前的价格；实测 600326 最近 750 根两份逐根差 0.000%，差异全在 2023-08-08 转增日之前）；② 用户截图的 000880 是**真连板**（7/26、7/29、7/30 三日一字板，独立源逐根一致）。**同批体检全湖挖出一个真 Bug（§9-V-2）**：`kline_daily/300750` 混进了**兜底源（东财）**数据 —— 成交量是新浪源的 **1/100**（东财=手、新浪=股）、出现 `close=-0.68` **负价**、2018-10-15 有 **+24.7%** 单日涨幅（当时创业板不可能）⇒ 量能副图 100× 台阶 + 负价把主图 y 轴拽到负数区 = **整张图被压扁**。**修法**：入口护栏 `drop_unusable_price_rows()`（非正价/缺价行丢弃 + `logger.warning` 出声）+ `em_volume_to_shares()`（兜底源出口统一量纲）；界面新增 **数据体检回执**（非正价 / 量纲接缝 ⇒ 点名「🗄 数据管理 → 重新全量下载」）。**同批两项可读性改进**：复权回执加「差异定位」（`⚠ 除权跳空最近一次 2023-08-08（-24.8%）`）+ tooltip 讲清"只改除权日之前"；读数条加**涨跌幅**与**「一字」**标签（`2024-07-30 … 收 6.78 量 23,938,437 +9.89% 一字`），⚠ 不按涨跌停规则猜标签。**判据精确性**：量纲接缝用**前后 20 根中位量**比（≥50×）⇒ 复牌/涨停单日放量零误报。**同批二次反馈（600326 · 2025-07-21）又抓到一个真缺陷**：数据仍与源逐根一致（四个连续一字板 + 一个 T 字板，11 天 +118% 真连板，量能指纹也对），**问题在画法** —— 一字板实体高度 0 ⇒ 老画法只剩 1px 细横线，4px/根 缩放下几乎看不见 ⇒ 看着像"K 线断口"。修法 = `CandlestickItem` 对零高度实体改画**加粗横档 2.5px**（笔宽不随缩放变化 ⇒ 任何缩放都看得见），**只改画法不动数据**；像素断言 **8 → 30 个非白像素**。验收：`smoke_chart` **423 → 444** + `smoke_pages_overlay` **255 → 261** + 真实数据/真实像素双复验（300750 报出"非正价 1 根 · 量能接缝 ×81"；000880 读数显示 `+9.89% 一字`；600326 一字板横档清楚可辨） |
| v6.22(文档v6.22) | **1.23 收官：§7-B6 STEP 5/6 + 拆分 `trading_desk`（1375→345）+ 发版 `1.23`** | ① **STEP 5 读数条接线 + 三处回执一行化**：读数 provider 接**本次真正渲染的那份 df**（`render_charts` 定格 `_rendered_df`；不现场重算 ⇒ 悬停读数与图上 K 线必是同一份，§10-4）；文案 = 日期/开/高/低/收/量/MA5/MA20（列名与 `TAEngine.add_ma` 同源）；`ChartHost` 新增 **`set_readout_text()`** —— 常驻读数直接写文案、**不移动十字光标**（`update_crosshair` 会让两条线跳一下，摘要不该有动画）；**只有主图给业务读数**（副图 y 含义各异 ⇒ 交回宿主内置文案）。复权/公式/标注三处回执改**一行摘要**（详解进 tooltip），并**保住既有文案口径**（单行时 `toolTip() == text()`；关键词 `除权`/`画线`/`根`/`真实成交价`/`交易日` 一个不少）。`smoke_pages_overlay` **225 → 234**。② **STEP 6 拆分 + 收口**：`ui/views/trading_desk.py` **1375 → 345（-75%）**，行为分居 **9 个 `ui/widgets/desk_*.py`**（`desk_layout` 361 版式装配 / `desk_data` 278 取数·周期·复权·分钟档位 / `desk_layers` 218 渲染·坐标轴 provider / `desk_panel` 196 图标轨·分页·**折起控制** / `desk_chips` 161 最近使用 chips / `desk_formula` 157 公式资产化 / `desk_annotations` 128 标注 / `desk_watch` 67 自选 / `desk_readout` 52 读数条），约定 = 1.22 同款**"状态留页面、行为搬模块 + 同名薄壳"** ⇒ **成功判据 = 同一套断言零改动**（`smoke_chart` 423 + `smoke_pages_overlay` 234 一个都没改）+ `compileall`；另把"实现确实搬走了"与"页面 ≤500 行"写成**可执行断言**（`inspect.getsourcefile` 核对 15 个方法归属 + 行数上限，防止把薄壳写成空函数）⇒ **234 → 251**。⚠ **收尾拍离屏截图时又肉眼抓到并修掉一个真 Bug**（1911px 窗口实测展开态 `splitter.sizes()=[969,698]`：**图表只占 45%**；折起后左侧还留 ~270px 空白）：根因 = ① 从未设 `stretchFactor` ⇒ QSplitter 的**富余宽度全被左栏吃掉**（即 §9-U 记的"图表被让渡宽度"）；② 面板的 `qSmartMinSize` 会取 `minimumSizeHint`(343) ⇒ 只隐藏/只把 minWidth 归零都不管用。修法 = `setStretchFactor(0,0)/(1,1)` + `setCollapsible(1,False)` + 折起时 **min/max 宽度同压 0**（展开还原）。**修后实测：展开 `[372,1295]`（图表 1283）、折起 `[60,1607]`（图表 1595、面板 0），来回切幂等** ⇒ **251 → 255**（+4 条宽度不变量）。常量（`SUB_PLOT_HEIGHT`/`RAIL_ITEMS`/`PERIOD_GROUPS`/`MINUTE_SEGMENTS`/`TOOL_SEGMENTS`/`DESK_UI_KEY`）改由模块定义、**从 `trading_desk.py` 再导出**（既有 import 零改动，§11.7）。③ **发版三处同步（§9-A）**：commit 首词 `1.23 …` + `APP_VERSION = "1.23"` + `version.json = 1.23`（notes = 用户可见变更：两行顶栏 / 可折起左栏 / 常驻读数条 / 分钟周期 / 画线工具进左栏 / 一行回执，并明说"口径与计算结果完全不变"）。**总验收：`smoke_chart` 423 + `smoke_pages_overlay` 251 全过（失败 0）+ 全仓 compileall + 9 个新模块可独立导入。** |
| v6.21(文档v6.21) | **1.23 立项：行情工作台版式收口（§7-B6）+ STEP 0 落地 + 清文档账** | 用户 2026-09-16 看完 4 套样板（`design/1.23-trading-desk-ui/`）拍板：**结构 = 样板 A（深色专业终端）的骨架 / 配色 = 现状（B 语言）**，**深色主题整体推迟**（"放到后面功能实现得差不多了再统一做"）；画线工具移入左栏「✎ 标注」页（工具行只留图标 + 条数 pill）；图标轨加**折起按钮**；工具行 chips 改「**最近使用优先**」= 已启用项 ∪ 最近前 3 + `＋` 完整菜单（规则见 §7-B6-D）。**STEP 0 已落地（零业务代码变更）**：① 两个冒烟脚本开头**自设 UTF-8 stdout** —— 治 "Windows GBK 控制台下带 `↔`/`⇒` 的 print 抛 `UnicodeEncodeError` ⇒ 整段分节被跳过 + 假报失败"（328/3 → **356/0**）；② `smoke_pages_overlay.py` 新增**「行情工作台迁移护栏」**（32 项公共面 + 6 项私有状态 + 常量 + 导航挂载点 + 3 项"允许被替换"的例外），断言 **180 → 184**。同批**清文档账**：§4 行数按实测重刷（`trading_desk 847` / `data_manager 473` / `backtest_result 432` / `backtest_panes 395` 退出 400 线）、`design/` 补进 §4 目录树、§7-B3/B5 状态改为已完成、§9-U 对本页病灶的表述**按实测纠正**（原"图表被压成下半屏"不成立）、1.22 "未 push" 记录更正（`origin/main` 已是 `e0f409a`）、§11.7 断言数与防污染名单同步。**STEP 1/2 同批落地（公共件先行、页面未动）**：① `custom_widgets.py` 新增 **`SegmentedControl`** + `segment_qss()`/`segment_button_qss(position)`（首尾圆角与中段分隔线**成套生成**、`QButtonGroup` 互斥、`set_current` 幂等、单段/空段不崩），并把 `mini_label`/`hint_icon`/`TAB_QSS_ON·OFF` 从 `backtest_panes` **上收**（回测页改为再导出 ⇒ 184 项页面断言零改动），顺手删掉 `dashboard.py` 里**第二份复制**的「?」角标；② `ChartHost` 新增 `set_readout_provider(fn)`（宿主只转交 `(窗格, x, y)`、**不猜业务**；空返回/抛异常**静默回退**内置文案）+ `set_readout_visible(True)`（读数条**常显**，不依赖十字光标）。验收：`smoke_chart` **356 → 383（+27）** + `smoke_pages_overlay` **184/0** + 全仓 compileall。**STEP 3a/3b 同批落地**：③ **分钟周期（= §7 D3 收尾）** —— `core.utils` 周期体系扩到 8 档（`5m/M5/5min/5分钟`都能认，**裸 `M` 仍是月线**）、`resample_ohlcv` 对分钟**直通**（不做本地聚合，否则历史被砍到 1/5）、新浪分钟源 `fetch_a_share_minute`（**签名无 adjust** —— 实测它本就不生效）、`ZONE_MIN` + `minute_key(600519@5m)` 按档位分键、`SingleSyncWorker(period=)`；分钟**不做"已最新就跳过"**、空返回按真失败。**开工前先做取数实测**（§7-B6-B2：新浪固定回 1970 根 ⇒ 1m≈9 日/5m≈42/15m≈124/30m≈247/60m≈493；东财分钟接口被代理拦截 ⇒ **不接入**未验证路径）。④ **顶栏第 2 行**：`cb_period`/`cb_adjust` 下拉换成 **`SegmentedControl`**（左栏两组入口同批删除）、一级 `日|周|月|分钟` + 二级 `1|5|15|30|60` + 深度提示、分钟下**复权禁用并说明**、`_data_zone_and_key()` 取数唯一入口、竞态守卫升级为标的+分区+档位三者吻合、日↔周/月**同源不联网**、分钟档位落 `desk_ui` 记住上次、`annotations.period_key` 扩到分钟（走唯一归一入口）。验收：`smoke_chart` **383 → 408（+25）** + `smoke_pages_overlay` **184 → 202（+18）** + 真实联网端到端复跑（5m/1m/60m 落盘、二次同步 added=0）+ 离屏截图人眼核验。**STEP 3c 同批落地**：⑤ **工具行 chips「最近使用优先」** —— 新增纯函数件 `ui/widgets/chip_mru.py`（候选池 / `push_recent` / `normalize_recent` / `resolve_chips` = **已启用项 ∪ 最近前 3**、已启用优先占位、灰态补空位 / `hidden_keys`），chip 样式上收 `custom_widgets`（`CHIP_QSS_ON/OFF/MORE`）；页面：主图/副图各 ≤3 chips + `＋ 更多` 完整候选池菜单、**真源永远是既有 QCheckBox**（chip 只是投影）、`_sync_chip_history()` 保证"从左栏打开也记历史"、"公式副图 N"chip 控制该格显示而不删函数段、历史落 `desk_ui`。**⚠ 踩到真问题并修掉**：`desk_ui` 自动落盘 ⇒ 冒烟脚本跑一次就写脏用户真实 `preferences.json`；已①打桩 `Preferences.save`（只写内存）②`preferences.json` 列入防污染自检③回滚被写脏的键。验收：`smoke_chart` **408 → 423（+15）** + `smoke_pages_overlay` **202 → 213（+11）**。**STEP 4 同批落地**：⑥ **左栏 = 图标轨 + 分页面板 + 折起** —— 新增容器件 `ui/widgets/desk_panel.py`（`IconRail` + `DeskPanel` + 折起按钮，纯容器零业务、可被其他页面复用）；页面拆 5 个 `_build_*_page()`，**控件搬家不重建**（16 个控件父级链挂到正确页、信号未断）；`cmb_tool` → `seg_tool`，**迁移期三处旧入口全部清零**；工具行只留 ✎ 入口 + 条数胶囊；`panel_page`/`rail_collapsed` 记住上次。**⚠ 实测抓到真 Bug 并修掉**：`QSplitter` 记绝对宽度 ⇒ 折起后面板隐藏但**宽度没让给图表**（1203 → 1203，左边留空白）；改为折起时显式 `setSizes` 重排（实测 **691 → 1386**）。验收：`smoke_pages_overlay` **213 → 225（+12）** + 离屏截图三态人眼核验 |
| v6.20(文档v6.20) | **1.22 回测页 UI 大改（样板 A 落地：摘要条 + 遮罩抽屉）+ §9-L 拆分收官（1697→787）** | 用户 2026-09-16 拍板"我更喜欢 a 的设计"（此前先出了 **4 套可交互样板** `design/1.22-backtest-ui/`，含分层表与取舍分析）。**版式**：旧"七区块平铺"（结果区只剩 ~60px）→ **摘要条 + 右侧遮罩抽屉 + 结果吃满**（离屏实测结果区 **625/867 ≈ 72%**）：① 工具栏行（标的 / 策略库）② **配置摘要条**（ƒ函数 · ⇄条件 · 📉大盘门控 · 🛡风控 · 🎯成交，五个 chips = 当前配置状态 + 点击展开；右端 = 运行回执 + ▶开始回测）③ 区间行 ④ **右侧遮罩抽屉**（`EditDrawer`：遮罩 + 抽屉 + 顶部页签；卡片**默认不在页面上出现**；覆盖层不进布局 ⇒ 打开配置时结果区高度不变）⑤ 结果区（KPI + K线控制 + 4 页签，吃满剩余高度）。**记住上次**：`core/preferences.py` 新增键 `backtest_ui={pane,last}`（用户拍板"打开页面用记住上次"）。**拆分（§9-L 第一批）**：`ui/views/backtest.py` 1697 → **1467**，新增 `ui/widgets/backtest_panes.py`(317：五张卡片 + `EditDrawer` 抽屉 + `mini_label`/`hint_icon`/`number_spin` 三个表单构件唯一来源)、`backtest_summary_bar.py`(111)、`backtest_export.py`(128：`compose_result_csv` 纯函数 + CSV/PNG 两个入口)；**第二/三批**：1467 → 1156 → **787（-54%）**，又拆出 `ui/widgets/backtest_result.py`(462：KPI 四卡 + K线控制行 + 4 结果页签 + 全部图表渲染与日期轴/量程自适应)、`backtest_flow.py`(304：运行流程)、`backtest_strategy.py`(194：策略库桥接)。统一约定 = **状态留在页面、行为搬进模块**（`self.page.*`）；页面保留控件**同名别名** + 私有状态 `property` 读口 ⇒ 既有 356+180 条断言与 `main_window` 调用口径**零改动**。⚠ 同批修掉两个**用户截图发现**的真 Bug（遮罩层叠顺序 / 抽屉内卡片被拉满，§11.5-27）。**纪律**：新增 **§10-14 版式分层**（L0 主角 / L1 操作轴 / L2 摘要 / L3 编辑抽屉 / L4 附属 + "新配置一律进摘要条与抽屉卡片"、禁止就地展开）、**§11.5-25**（`isVisibleTo(ancestor)` 口径）、**§11.5-26**（按需展开区必须显式定量）。验收：`smoke_chart` **356** + `smoke_pages_overlay` **180** 全过（含用户库防污染自检）+ 全仓 compileall；三张离屏截图实测（默认 / 展开成交卡 / 展开函数卡）。APP_VERSION 已随本批升 **1.22**（commit 首词 `1.22 UI重构 模块拆分`）；`design/1.22-backtest-ui/` 同批入库作为长期设计参考；⚠ 只提交、**未 push**（**v6.21 开工前核对：该批已 push —— `origin/main` 已是 `e0f409a`**，此处的"未 push"仅指提交当时） |
| v6.19(文档v6.19) | **提交发版：app 版本 → 1.21（零业务代码变更）** | §7-B5 回测成交真实性的实现与验收已在 v6.17（立项）/ v6.18（落地）完成；本批只做**三处版本号同批同步**（§9-A 新纪律的首次实跑）：① commit message 首词 `1.21 回测策略更新`；② `config/settings.py:APP_VERSION` → **1.21**；③ 仓库根 `version.json` → **1.21**，`notes` 换成这一轮的用户可见变更（三档成交时点 / T+1 / 同根不重建仓 / 口径入签名与导出 / 教学弹窗 / 步长修复）。**用户手动实测手感已通过**（原话"现阶段没问题"）⇒ §7-B5 步骤 0–5 全部收官。回归：`smoke_chart` **356** + `smoke_pages_overlay` **180** 全过 + 全仓 compileall。⚠ 本批**只提交、未 push**；`version.json` 的 `url` 仍指向 Releases 列表页（用户决定：软件未完工，暂不制作 Release） |
| v6.16(文档v6.16) | **A 类文档欠账清账（零业务代码变更）** | ① **§4 行数口径改版**（用户拍板：**只标 ≥400 行的文件**，其余不标 —— 根治"每次大改全量重刷且必然漂移"的维护债；§4 目录树 + 体积红黑榜按实测重写）；② **失效文件引用**（`ui/views/market.py` v6.12 已删除：2 处**活引用**改为 `trading_desk.py` 的真实落点（`formula_overlay.py` 的渲染方、`backtest.py` 的 `_on_sync_finished`），4 处历史叙事保留原文 + 补注"该文件已删除"；文档 §11.4 两条活引用同批修正）；③ **版本号体系改版**（用户拍板：**统一跟随 git** = 最近一次 push 的 commit message 首词，**每次 push 递增 0.01**；`APP_VERSION` 与 `version.json` 由 `1.4.1` → **1.20**；新纪律写入 §9-A）。回归：`smoke_chart` **312** + `smoke_pages_overlay` **150** 全过 + 全仓 compileall。**附**：`version.json` 的 `url` 由占位符 `https://github.com` 改为**项目 Releases 列表页**（用户决定：软件尚未完工，暂不制作 Release，先占位；正式发版时换直链） |
| v6.15(文档v6.15) | **§7-B4 坐标轴自适应（全 App 收口）** | 用户截图驱动立项的全 App 缺陷收官。**新建 `ui/widgets/adaptive_axis.py`(348)**＝全 app 唯一的刻度与量程来源：纯函数 `compute_ticks`（日期格式梯子：日内/同年/跨年）/`compute_text_ticks`（序数轴）/`slice_span`（可视极值，NaN 忽略）+ `attach_date_axis`/`follow_y`/`attach_all`（监听 `sigXRangeChanged` 重算，**同轴重复 attach 自动替换旧跟随器**，弱引用登记表 + 非幂等保护 + `handle_for` 读口）。**3 处静态 ticks 全部收敛**：行情主图/量/MACD/公式副图逐窗格（`_adaptive_providers` 回答 provider 契约）、复盘 K线回放、复盘资金K线；**回测页两处改为调用公共纯函数**（K线 `chk_follow` 开关语义保留、净值轴）。两个原始诉求已验证：放大到 10 根仍有 ≥2 刻度、纵轴按可视区间重算（副图不再压成一条线）。实施中固化两条新知：① **`ViewBox.setYRange` 会偷加自己的 padding**（实测 2%~4.7% 且漂移）⇒ 一律 `padding=0`（§11.5-21）；② 复盘"资金 K 线"的 x 其实是**当月第几日的序数轴**⇒ 增设文本刻度入口（套 `%m-%d` 会说谎，§9-v6.15）。断言：`smoke_chart` 290→**312**（+22）、`smoke_pages_overlay` 139→**150**（+11 页面级）全过 + 全仓 compileall。APP_VERSION 未动（仍 1.4.1） |
| v6.14(文档v6.14) | **全仓文件归置（零业务变更）** | 用户指出"根目录存在许多未分类文件"。立 **§10-13 文件归置规范**：根目录收敛为白名单（`main.py`/`requirements.txt`/`version.json`/`JIAN_RULES.md`/`.gitignore` + `config|models|core|data|ui` + `scripts|tests|samples`）。新增三个角色目录（判据 = 谁调用它）：`scripts/`（`sync_roster.py`，人工运行、不进 import 图）/ `tests/`（两个冒烟脚本）/ `samples/`（`实例函数*.txt` + `实例交割单*.xlsx`，是数据不是代码）。删除根目录空壳 `screenshots/` 与全仓 `__pycache__`。**移动脚本同批修路径引导**（子目录必须 `__file__` 反推仓库根，否则 `from data...` ImportError；`smoke_chart` 的 `Path(ROOT)/"ui"` 源码级守门会静默失效）+ 修**用户可见文案**（"请先运行 `scripts/sync_roster.py`"）与 3 处 docstring/注释。`.gitignore` 收紧为 `samples/*` + `!samples/README.md`（整目录忽略只放行说明，优于逐文件名模式）+ `_tmp_*` 兜底。`ui/views/market.py` 的删除同批 `git rm`。回归：`py tests/smoke_chart.py` **290** + `py tests/smoke_pages_overlay.py` **139** 全过 + 全仓 compileall；`git ls-files` 核对**无死代码**。登记待办（暂缓）：`ui/widgets/` 可再分 `chart/` 子包（51 处 import）。APP_VERSION 未动（仍 1.4.1） |
| v6.13(文档v6.13) | **P8 收尾（复权切换 + 文字拖动）** | ① **复权**：`market_db` 新增 `kline_daily_raw` 分区（不复权与前复权**各存一份**——前复权随除权整体漂移、不复权不会，两者无法互推）；`akshare_feed` 两个源都按同一 `adjust` 拉取（**降级不许静默换口径**）+ `ADJUST_QFQ/ADJUST_NONE`；`sync_service` 新增 `zone_for_adjust/adjust_label/ADJUST_*`（UI 唯一入口）与 `ZONE_KLINE_RAW` 抓取分支；「数据管理」新增该分区并可单独同步/删除；工作台复权下拉 + "取数中/不含旧口径根数"的诚实回执。② **文字标注拖动**：`_ClickableText` 自实现 `mousePressEvent/mouseDragEvent`（pyqtgraph 覆写 `mouseMoveEvent` 不调父类 ⇒ ItemIsMovable 无效），松手才落盘、坐标按 (日期,价格) 回写。断言：`smoke_chart` 278→**290**、`smoke_pages_overlay` 132→**139** 全过 + 全仓 compileall；临时脚本与输出文件已清理。APP_VERSION 未动（仍 1.4.1） |
| v6.12(文档v6.12) | **P8 行情页整体重做（换壳不换芯）** | 新建 `ui/views/trading_desk.py`(734 "行情工作台")，**删除** `ui/views/market.py`(661 越线文件)；页面只做装配，绘图能力全部复用既有组件（ChartHost/OverlayPainter/chart_layers/annotation_layer/indicator_panes/formula_store/formula_library/workers）。新增 **自选股** `data/watchlist_store.py`(143，原子写/坏数据容忍/名称随花名册刷新)、**周期 日·周·月** `core/utils.resample_ohlcv`(日线就地聚合，date=该周期最后一根真实交易日，指标在聚合后再算)、**画线 3 类→5 类**(斐波那契 = 两端点+7 档+标签附属图元；文字 = `_ClickableText`，因 `pg.TextItem` 不发点击信号就**无法单独删除**)、**标注按 (标的,周期) 隔离**(新增 `annotations.period_key`)；`main_window.page_market` 指向新页。断言：`smoke_chart` 238→**278**、`smoke_pages_overlay` 111→**132** 全过 + 全仓 compileall。⚠ P8 收尾待做：复权切换 / 分钟周期(D3) / 锚点拖动。**附带加固**：页面级冒烟脚本补三条打桩（模态框立即返回 / 更新检查不联网 / 结尾 `os._exit`），治好了"脚本看似卡死无输出"（§11.5-20）。APP_VERSION 未动（仍 1.4.1） |
| v6.11(文档v6.11) | **P7 函数资产化 + 行情↔回测互送** | 新增 `data/formula_store.py`(213，零 Qt：函数段+参数+**每段目标窗格** → 有名字的配方；`~/.jian_data/formula_library.json` 原子写；按 `name` upsert、`touch/last_used`；`get_formula_store()` 单例) 与 `ui/widgets/formula_library.py`(171：列表/预览/载入/改名/删除，**两页共用**)。互送由 `main_window` 当唯一传话筒（两页面**互不 import**）：行情页「📤 送去做回测」(目标不随行) / 回测页「📤 送到行情页」(每段落主图)；两侧均自动切页+自动检测。行情页开机**自动恢复上次配方** ⇒ 治本"公式重启就丢"。配方≠策略：配方只有公式，策略=配方+条件+风控+区间。断言：`smoke_chart` 212→**238**、`smoke_pages_overlay` 84→**111**（含**防污染自检**：用户真实库不得被测试写）。APP_VERSION 未动（仍 1.4.1） |
| v6.10(文档v6.10) | **红线收编 + P6 标注持久化** | 用户拍板"红线不放宽 ⇒ 收编"：`MarketSyncService.fetch_index_constituents()` 成为**联网抓取唯一入口**（`ui/` 全层零 `AkShareFeed`，源码级断言守门），失败文案按"网络 / 行情源未收录"分类。**P6 落地**：新增 `data/annotations.py`(255，零 Qt：`KIND_*` 模型 + `(标的,周期,id)` 原子写 CRUD + `DateAxis` 日期↔序号映射) 与 `ui/widgets/annotation_layer.py`(222：绘制/选中/**逐个独立删除**)；`ChartPane` 补 `remove_annotation()`；行情页三件套（趋势线/水平线/垂直线）+ 工具下拉 + 删除选中 + 清空(二次确认) + Delete/Esc；**坐标存日期不存序号**（"前置 5 根 ⇒ 序号 +5 不漂移"有页面级断言）。§7-A1 欠账被吸收清零。断言：`smoke_chart.py` 178→**212**、`smoke_pages_overlay.py` 63→**84** 全过 + 全仓 compileall 通过；§4 行数重刷。APP_VERSION 未动（仍 1.4.1） |
| v6.9(文档v6.9) | **复查 + 小债清账（无新功能）** | 第三次全仓"陌生代码"对账后收尾四项：① **§9-P1 净额收尾** —— 新增 `core/utils.record_net_amount()`（全站唯一口径）+ 5 处逐笔展示层改净额（流水页盈亏列**改名「净盈亏」并显示净额**、复盘页当日清单/详情头/回放标记色）；② **§10-9 控件契约** —— `custom_widgets` 新增 `combo_qss()/date_edit_qss()` 生成器 + 8 具名常量 + `NoWheelDateTimeEdit`，清掉 7 处就地半截 QSS，**治好"只写 `::drop-down` 导致下拉箭头整个消失"的长期隐形 Bug**（复盘页时间选择器 / 手工录入弹窗）；③ NoWheel 补齐（复盘孤儿日期 + 手工录入时间与 5 个数值框）；④ 行情页接线**跨窗格十字光标**。断言：`smoke_chart.py` 161→**178**（+净额 6 / +样式契约 11，含负向对照）、`smoke_pages_overlay.py` **63** 全过、全仓 compileall 通过；§4 行数按实测重刷（口径 = 非空行）。APP_VERSION 未动（仍 1.4.1） |
| 远期 | 排期 | 组合级多标的引擎（指数择时后全市场挑股+资金分配，见 §6.5） |

---
