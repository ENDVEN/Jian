# Jian - 项目开发规范与上下文记忆 (v4.8)

## 1. 项目愿景与定位
- **项目名称:** Jian (专业量化交易复盘与分析系统)
- **项目目标:** 采用 MVP 模式与敏捷开发，打造一个具有企业级地基、高扩展性、数据绝对安全的桌面端软件。
- **核心理念:** 
  - 坚持代码的高内聚、低耦合（SRP 原则），严禁模块臃肿。
  - 坚持程序本体与用户数据的物理隔离，保障数据主权。
  - 坚持操作的绝对幂等性（防呆、防重、防手残）。

## 2. 技术栈与核心设施
- **UI 框架:** PyQt6 / PySide6 (主界面与视图组件)
- **图表引擎:** PyQtGraph (自定义 `CandlestickItem`，强制抗锯齿、去描边、单色填充，严格遵循 Michael Pokorny 视觉规范)
- **数据源接入:** AkShare。日线行情统一走**新浪源为主 + 东财源兜底** (A股 `stock_zh_a_daily` 与期货 `futures_main_sina` 同源，多数受限网络可达；东财 `stock_zh_a_hist` 兜底北交所等标的)
- **指标计算:** 纯原生 `Pandas` 向量化 TAEngine (零编译依赖，完美兼容 Python 3.14+，已实现 MA/BOLL/MACD)
- **持久化方案:** 
  - **业务数据:** SQLite (`INSERT OR IGNORE` 事务与哈希去重)
  - **海量时序数据:** Data Lake 数据湖架构 (利用 `pyarrow` 实现高速分区 `.parquet` 存储)
- **并发控制:** 任何耗时 I/O 强制使用 `QThread` `Worker`，保证 UI 丝滑。

## 3. 核心架构设计原则 (MVC 深度解耦与数据守护)
本项目严格遵循以下开发铁律：

1. **防呆入库与反馈闭环 (Idempotent Design):** 
   - 数据库插入必须严格使用 `INSERT OR IGNORE`。
   - 任何导入操作必须通过统计报告（`inserted`, `ignored`）向 UI 层清晰反馈结果，让用户对数据的去重和拦截有绝对掌控感。
2. **确定性哈希主键:** 针对外部数据（如 CFMMC 期货交割单），使用 **MD5(账户名_单号_交易日期_数量)** 生成绝对唯一的 `internal_id`，以支持多次反复导入与跨月缝合。
3. **数据物理隔离:** 用户产生的 `.db` 文件与行情数据湖必须存在于操作系统的独立数据目录（如 `~/.jian_data`）中。
4. **组件物理拆分:** 严禁将不同的业务逻辑揉在一个大文件里（例如已将所有的 Dialog 拆分为独立模块）。
5. **单一时间锚点 (v1.1):** 交割单现实只有"交易日期"，严禁为填充字段而虚构开/平仓时间。一切日历/绩效/回放都以唯一 `trade_time`（盈亏实现日）为锚点；无法自洽的统计（如持仓时长）直接下线而非伪造。

## 4. 目录结构 (Phase 4 演进态)
```text
Jian/
├── main.py                 # 程序唯一启动入口，负责环境初始化
├── requirements.txt        # 依赖清单 (PyQt6 / pyqtgraph / pandas / pyarrow / akshare)
├── JIAN_RULES.md           # 本规范与记忆文件 (当前版本 v4.6)
├── config/                 # 【配置中心】
│   └── settings.py         # 全局常量、颜色、路径、UI 样式统一定义
├── models/                 # 【数据模型】
│   └── trade.py            # 核心数据契约 (TradeRecord, v1.1 单一 trade_time)，含强化版 internal_id 钩子
├── core/                   # 【核心逻辑引擎】
│   ├── engine.py           # 中央数据引擎 (DataEngine)，统筹内存状态与 DB 报告
│   ├── database.py         # 业务数据库管家 (SQLite)，负责复盘单据的 CRUD 与防呆拦截
│   ├── indicators.py       # 纯 Pandas 原生技术分析引擎 (TAEngine)
│   ├── analyzer.py         # 绩效评估与回撤计算引擎
│   ├── formula/            # 【通达信公式 DSL + 整段函数】(优先) + 远期 Python 扩展
│   │   ├── tokens.py       # 词法：通达信方言分词
│   │   ├── parser.py       # 语法：递归下降 -> AST
│   │   ├── runtime.py      # 指标/函数运行时 (MA/EMA/SUM/IF/REF/HHV/LLV/CROSS/MACD/KDJ...)
│   │   ├── program.py      # 整段通达信函数 (多语句/输出行/绘制指令/缺参探测)
│   │   └── __init__.py     # 对外门面 FormulaEngine / compile / evaluate
│   ├── backtest.py         # 单股事件模式回测引擎 (仅 LONG，A股无做空)
│   ├── utils.py            # 无副作用公共工具 (品种主体提取、时间/价格/点数格式化)
│   ├── preferences.py      # 用户偏好持久化 (JSON: 时间精度等，与业务数据物理隔离)
│   └── updater.py          # 异步后台版本检测 (QThread)
├── data/                   # 【数据来源与持久化】
│   ├── data_feed.py        # 账单解析抽象层 (BaseTradeParser + PARSER_REGISTRY) + CFMMC FIFO 缝合
│   ├── akshare_feed.py     # 外部金融数据爬取代理 (A股/期货日线及花名册)，含市场智能路由
│   ├── market_db.py        # 工业级 Parquet 数据湖管家 (DataLakeManager)
│   └── strategy_store.py   # 回测策略库持久化 (JSON: 函数/参数/条件/区间 + 每标的回测档案)
└── ui/                     # 【表现层 (UI)】
    ├── main_window.py      # 总路由器 (Controller)，极简代码，只负责引入组件与事件分发
    ├── widgets/            # 【可复用图元与控件】
    │   ├── custom_widgets.py       # Hover 悬浮列表、Pokorny 风格 K线图元等
    │   ├── screenshot_gallery.py   # 复盘截图画廊 (粘贴/导入/预览/删除/序列化)
    │   └── yearly_review.py        # 年度复盘面板 (月度盈亏卡片/策略贡献/净值曲线)
    ├── dialogs/            # 【物理拆分后的独立弹窗】
    │   ├── list_manager.py     # 账户/策略管理与删除警告
    │   ├── manual_entry.py     # 手工录入交易单
    │   └── import_futures.py   # 期货 CFMMC 专属导入 (QThread 静默拆单与防重报告)
    # NOTE(v1.3): import_stocks.py 已按产品决策整体移除 —— 交易流水/深度复盘聚焦期货，
    # 股票仅保留市场行情与市场回测侧能力(扬长避短)。
    └── views/              # 页面级大型视图组件
        ├── dashboard.py    # 资金与表现面板
        ├── records.py      # 流水表格，含多维高级过滤器与全量 CSV 导出
        ├── review.py       # 深度复盘双模态工作台 (含 K线交易回放)
        ├── market.py       # 市场行情对照与沙盒视图
        ├── backtest_module.py  # 市场回测模块容器 (三子页: 单股回测/全市场筛选/广度统计)
        └── backtest.py     # 市场回测·单股回测子页 (M1: 策略工作台/折叠编辑区/净值与K线自适应)
```

## 5. AI 协作约定 (给 AI 的最高指令)
当你（AI）阅读到此文件时，请在接下来的对话中严格遵守以下规则，不容妥协：

Michael Pokorny UI 准则: 任何新增图表元素必须严格遵循“抗锯齿、去描边、单色高对比”原则。

防阻塞底线: 涉及 Parquet 读取或网络拉取时，主动封装入 QThread 异步方案。

架构纪律: 新增功能时，请主动评估应该将代码放入 core (计算), data (获取/存储), 还是 ui (展示) 层，绝不可在 ui 文件中写死 SQL 或爬虫逻辑。

代码全量输出兜底: 修改核心文件时，请尽量提供该文件的全量代码或明确的插入锚点，防止小白用户复制时引发缩进灾难。

## 6. 开发进度与 Roadmap
✅ Phase 1~3: 完成基础 MVP 闭环（UI组件物理隔离、SQLite防重持久化、CFMMC跨月交割单智能缝合）。

✅ Phase 4.1 (基础设施建设): 完成 Parquet 极速数据湖 (market_db.py)、零依赖向量化指标库 (indicators.py)、AkShare 接口封装 (akshare_feed.py)。

✅ v1.1 (数据契约重构): 交割单导入与全链路仅保留单一 `trade_time`；旧库双时间自动无损迁移；交易回放改单点锚定式；删除无法自洽的"时长分析"。

✅ v1.2 (交割单字段补全): 补入成交价 / 成交时刻 / 合约乘数 / 完整性标记；持仓明细与结算月报纳入解析；FIFO 按理论盈亏加权分配；跨月真缝合（顺序无关）；净额口径；遗留单四层防御与漏月告警。详见第 8 节。

🚀 Phase 4.2+: 前端集成与业务展现 (当前阶段)

[ ] 打通视图层: 将数据湖的数据喂给 ui/views/market.py，并利用 custom_widgets.py 中的底盘渲染出行情图表，提供输入代码查询交互。

[ ] 复盘涂鸦板: 允许在行情图表上绘制支撑/阻力线并持久化。

[ ] 热力图看板: 升级 dashboard，引入 GitHub 风格日历热力图。

## 7. v1.1 数据契约变更记录 (改前必读)

### 7.1 发生了什么
交割单 (CFMMC) 只有"交易日期"，原先 `entry_time` / `exit_time` 双时间结构会在流水表格制造虚构信息。v1.1 将二者合并为**单一 `trade_time`**：

- 数据库 `trades` 表：`entry_time`、`exit_time` 两列删除，改为 `trade_time`。
- 启动时自动迁移：`DatabaseManager` 检测到旧结构会把表改名备份后重建；
  数据归并规则 **`COALESCE(exit_time, entry_time)`**（平仓日优先，开仓日兜底），
  已有 `internal_id` 原样继承，无主键的远古库会逐行补全。
- 导出 CSV 只包含 `trade_time` 单时间列。
- `format_trade_time()`：若时间恰为当天零点只显示 `YYYY-MM-DD`，有具体时分才带 `HH:MM`，杜绝虚假 `00:00`。

### 7.2 已下线的模块与理由
| 模块 | 去处 | 理由 |
|---|---|---|
| 流水表"进场/平仓时间"列与"持仓(h)"列 | 删除 | 数据源无开平仓双时间 |
| 流水页"持仓时间"高级过滤器 | 删除 | 依赖时长，逻辑无法自洽 |
| 复盘页 "⏳ 时长分析" 页签与其散点图 | 删除 | 同上 |
| `core/utils.parse_time_gap_hours` | 删除 | 被以上功能独占 |

### 7.3 交易回放的新的表现形态 (单点锚定式)
交割单无法提供进场/离场两个坐标点，回放改为：
1. 以 `trade_time` 为圆心截取前后 ±45 天本地 K 线 (`PLAYBACK_CONTEXT_DAYS`);
2. 定位时间上最接近的一根 K 线为锚点，画垂直虚线;
3. 在该 K 线上叠加标记：**箭头方向 = 多/空**，**颜色 = 盈/亏**，并带 PnL 文字;
4. 本地缺数据或锚点误差 > 7 天 (`MAX_ANCHOR_GAP_DAYS`) 时明确提示，
   绝不硬凑坐标，并自动切回"累计盈亏"页。

### 7.4 后续使用注意事项
- **升级即迁移**：旧库首次启动自动重建为 v1.1，迁移日志走 logging；
  数据量不大，迁移在后台瞬时完成。
- **约定俗成**：新增任何按日统计/日历/回放功能一律使用 `trade_time`；
  严禁重新引入成对的进场/离场时间列。
- 手工录入与股票导入向导均已改为单"交易时间"，向导中该字段现在是必选映射项。
- 若将来真能拿到带时分秒的开/平仓逐笔流水，应新增**可选**的 `entry_time`
  作为独立附表字段，而不是推翻 `trade_time` 主锚点设计。
  （v1.2 已兑现此预留：实例交割单确实带 `成交时间`，现以 `entry_fill_time` /
  `exit_fill_time` 独立字段承载，`trade_time` 主锚点依旧是纯日期。）

## 8. v1.2 数据契约变更记录 (交割单字段补全，产品决策已定稿)

### 8.1 v1.1 的前提已被实例交割单证伪（改前必读）

v1.1 曾断言「交割单现实只有交易日期」。经对真实 CFMMC 交割单逐列实测，**该前提不成立**：
`成交明细` 页签同时提供了 `成交时间 (10:34:51)`、`成交价`、`成交额` 三列真实数据。
v1.2 在不推翻 `trade_time` 主锚点的前提下，把这些真实数据完整纳入。

### 8.2 实例交割单的标准格式（解析依据）

`成交明细` 表头位于第 9 行，数据自第 10 行起：

```
交易日期 | 合约 | 成交序号 | 成交时间 | 买/卖 | 投机（一般）/套保/套利 | 成交价 | 手数 | 成交额 | 开/平 | 手续费 | 平仓盈亏 | 实际成交日期
```

- `成交时间` 为字符串 `'10:34:51'`；`买/卖`、`开/平` 带隐藏空格（须 `clean_str` 处理）
- 开仓行的 `平仓盈亏` 为 `'--'`，须回落 0 而非报错
- **`交易月份` 元信息不可信**：实测 2025-07 的样例文件该字段写成了 `2025-09`。
  月份一律以「成交明细交易日期的众数」为准，零成交月份靠资金链反推。

### 8.3 新增字段（全部可空，加列迁移，老库零损）

| 字段 | 类型 | 语义 |
|---|---|---|
| `entry_time` | TIMESTAMP | 开仓腿真实成交时刻（完整 datetime）；孤儿单为 NULL |
| `entry_price` / `exit_price` | REAL | 开仓价 / 平仓价；待缝合单的 `entry_price` 为 NULL |
| `entry_fill_time` / `exit_fill_time` | TEXT | 真实成交时刻 `HH:MM:SS` |
| `time_source` | TEXT | `DATE_ONLY` / `STATEMENT` / `MANUAL`，时刻来源可追溯 |
| `multiplier` | REAL | 合约乘数，由 `成交额 / (成交价 × 手数)` 反推 (IF=300 / IM=200) |
| `is_orphan` | INTEGER | 1 = 开仓腿缺失、待缝合 |

新增表：`open_legs`（未平持仓腿，跨月延续）、`import_coverage`（月度资金勾稽）、`coverage_gaps`（用户确认跳过的月份）。

### 8.4 时间双轨制（产品决策）

- **`trade_time` 主锚点永远是纯日期 `00:00:00`，程序绝不自动填充时分** —— 这是"不虚构时间"的底线。
- 真实成交时刻单独存于 `*_fill_time`，**始终完整入库**（这是原始数据，不是虚构）。
- 用户的「时间精度」偏好**只控制显示与排序粒度**，切换零成本，无需重新导入。
- **首次导入必须让用户自主选择**（不设默认值），并在「交易流水 → ⏱ 时刻」保留随时修改的入口。

### 8.5 净额口径（产品决策）

- 单笔净额 = 平仓盈亏 − 手续费。
- **胜率 / 盈亏比 / 单笔极值 / 净值曲线一律改用净额**，避免"毛利为正但被手续费吃掉"的交易被错误计入胜场。
  实测该口径使胜率由 51.6%（毛利）修正为 45.2%（净额）。
- `gross_profit`（毛利）与 `total_commission`（手续费）单独保留供对照。

### 8.6 遗留单的四层防御（产品决策）

1. **持仓明细页签**：解析月末未平持仓快照（含买入价/卖出价/实际成交日期）作为下月期初持仓。
2. **open_legs 持久化 + 全局时间排序**：多文件合并后按 `(日期, 时刻, 成交序号)` 排序再跑一次 FIFO，
   使**分批导入 ≡ 一次性导入，与导入顺序完全无关**（已实测验证）。
3. **资金链对账 + 漏月告警**：利用恒等式 `客户权益 = 上月结存 + 存取 + 盈亏 − 手续费`
   与 `上月结存 ↔ 客户权益` 链条检测断层，精确报出疑似漏导的月份。
   **已导入月份中最早的那一个不算断层**（其"上月"本就不在范围内），避免误报。
4. **孤儿单显式标记 + 手工补录**：`is_orphan=1` 的记录盈亏数字依然准确（取自平仓盈亏），
   仅开仓侧信息缺失；流水页常驻黄条提醒，复盘页可手工补录（开仓日期 + 开仓价），
   补导更早月份后自动缝合。补录数据的 `entry_time` 标记为 `MANUAL` 来源。

### 8.8 交易回放双点锚定 (v1.2 形态)

完整闭环（`entry_time` / `entry_price` / `exit_price` 齐全）时，回放为**双点区间锚定**：
以开仓日与平仓日两根 K 线为双锚点，画开/平垂直虚线（蓝/橙）与价格水平点线，
两锚点之间半透明高亮带（盈绿亏红），平仓锚点叠加「多空箭头 + 点数 + 盈亏」标注。

孤儿单 / v1.1 老数据缺开仓信息时**诚实降级**为单点锚定，标题橙色标注「开仓信息缺失」，
绝不硬凑坐标；有平仓价仍画平仓价水平线作参考。

### 8.9 微观测算一致性修正 (v1.2.1)

- **盈亏分配**：一笔平仓若同时平掉多个开仓（含开仓价缺失的孤儿片），
  必须让「有开仓价的切片」分到与其 `点数 × 乘数 × 手数` 完全一致的盈亏，
  孤儿片承接「整笔平仓盈亏 − 已知片理论盈亏」的余额。
  旧实现整体按手数均分会在孤儿混合场景制造"点数与盈亏对不上"的观感。
- **孤儿语义收敛**：`is_orphan`（待缝合）是 CFMMC 开平配对的专属概念，
  手工录入 / 股票映射仅是用户填写的已平结果，不再卷入"孤儿"标记；
  缺价格只显示「—」，不进入待缝合提醒。
- **手工录入账户**：归属账户下拉只列**真实存在**的账户（来自已导入数据），
  不再预置"国内长线/国内短线"等虚构账户；支持直接输入新账户名创建。

### 8.10 范围聚焦 + 持仓时长回归 (v1.3，产品决策已定稿)

- **删除股票导入链路**：市场上股票复盘工具已足够强，Jian 不再为其预留
  `import_stocks.py` 与 UI 入口。**交易流水 / 深度复盘只做期货**；
  股票的扬长避短只保留在「市场行情」与「市场回测」侧。
- **持仓时长回归**：v1.1 因"交割单无开仓时间"而下线的时长统计，在 v1.2 补入
  `entry_time` 后重启，采取**诚实三级口径**：
    1. 开仓带时分 → 精确到秒/分/时（交割单原生即含成交时间）；
    2. 开仓仅日期 → 用本地数据湖交易日历按「交易日跨度」计（周末/节假日不虚增），
       本地无行情时回退自然日并如实标注；
    3. 无开仓时间 → 显示 `—`，绝不猜测。
  统计始终标注样本覆盖（`可计时 M/N`）。先做加法：绩效卡片 + 复盘「⏱ 持仓时长」页签，
  若拥挤再做减法。
- **手工录入补「开仓时间(选填)」**：需主动勾选并填写才入库（默认值只是预填待确认），
  保证记录真实、不拿平仓时间冒充。

> **铁律**：漏月导入 ≠ 遗留单。任何情况下都**绝不阻断**用户，数据照常入库、照常可用；
> 用户可将某月登记为"确无交易"以免重复提醒。

### 8.7 买卖分类口径（产品决策）

面向个人投资者，**不做投机 / 套保 / 套利分类**（那是机构需求）。
只保留 `买入开仓 / 卖出开仓 / 买入平仓 / 卖出平仓` 四象限，由 `direction` 完整推导，
UI 层以「买入开仓」文案 + 完整动作链 tooltip 呈现。

---

## 9. 市场回测模块规格 (v1.0，产品决策已定稿)

### 9.1 定位与铁律
- **服务对象：A 股单市场**；回测/筛选/广度统计一律不做期货，禁止股票期货混算。
- 三大模式共享一套「通达信函数 → 事件引擎 → 结果呈现」管线：
  M1 单股历史回测 / M2 单日全市场横截面筛选 / M3 全市场逐日广度家数折线(+指数叠加)。
- **信号语义 = 事件模式**：开仓条件与离场条件是两个独立表达式；仅在条件**由假变真**瞬间触发，
  绝不使用单状态持有。A 股无做空 => 引擎只支持 LONG。
- **通达信优先级最高**；Python 函数注入为远期第二作者模式，接入点预留在 `core/formula`。
- **交互铁律 (P1b 定稿)**：① 粘贴整段函数 -> ② 自动检测(语法/执行/缺参) -> ③ 基于函数**产出变量**
  用下拉+规则配置买卖条件(如：布尔变量 = 1、某信号「出现」)；条件文本在内部翻译为
  `CROSS(变量, 0.5)` 一类事件 DSL，再交给引擎。
- **隐私铁律 (用户私有公式)**: 不把任何用户私有函数写入示例/占位符/文档/代码仓库；
  示例仅允许通用的公开写法。`.gitignore` 已忽略私密函数文件，避免误提交开源仓库。

### 9.2 数据窗口与缓存铁律
- 回测数据窗口默认 **start=2018-01-01**（更早量价对回测参考意义弱，避免无谓的数据与算力开销）；
  历史行情浏览不受此限制，仍可拉取 2010 年至今。
- 全市场扫描/M3 必须先有**本地数据湖**，禁止查询时逐股实时联网；
  支持用户**预下载** + 一键**删除缓存数据**（UI 提供管理页，数据层提供原语）。
- 新增湖 zone：`market_index`（大盘/板块指数日线，供 M3 叠加）、`scan_cache`（公式哈希+日期维度的中间结果缓存）。

### 9.3 分层职责
- `core/formula/`：表达式相关纯计算，零 UI/零网络。tokens(词法)/parser(语法)/runtime(函数注册表)。
- `core/backtest.py`：事件模式回测引擎，输入 单股 df + 买/卖两个编译表达式 -> 成交清单与净值。
- `data/`：全市场增量同步与缓存清理；**UI 不得直接联网拉行情**。
- `ui/views/backtest.py`：新导航页(资金表现/流水/复盘/行情 + 📐 市场回测)。

### 9.4 Roadmap 状态
- [x] 决策定稿 (本规格)
- [x] P1 核心闭环: formula DSL(通达信子集) + 单股事件回测引擎 (core)
- [x] P1b 单股回测 UI (ui/views/backtest.py: 整段函数编辑器/检测/产出变量条件配置/净值/买卖点/明细)
- [x] P1b2 策略工作台升级: 上下区分区布局、KPI卡片、区间快捷档、策略库(保存/选用/删除)、同股策略对比
- [x] P1b3 UI精修: ①/②可折叠专注回测、净值曲线日期轴随缩放自适应、等宽去框化顶栏、三子页容器预留 M2/M3
- [x] 稳定性修复: ①函数检测缺参自动探测时占位参数改用合法默认值 5.0（窗口类周期必须≥1，旧值 0 会让 pandas 裸抛 `ValueError: span must be >= 1` 穿透 UI）；`runtime._period` 对 MA/EMA/HHV/LLV/COUNT/SUM/EVERY 周期参数统一校验，非法周期在界面显示友好错误而非终端堆栈
- [ ] M2 全市场筛选子页 / M3 广度统计子页 (模块占位已就绪)
- [ ] P1b+ 函数能力补强: 绘制输出可视化(STICKLINE/颜色/图标)、更多函数(通达信内置全量子集)、函数模板库
- [ ] P1c 单股回测进阶 (复用 TradeAnalyzer 绩效、结果长期入库+导出/删除、策略快照保存)
- [ ] P0 数据管理页: 预下载/增量/删除缓存 (含 market_index 拉取)
- [ ] P2 M2 单日全市场横截面筛选
- [ ] P3 M3 全市场广度家数折线 + 指数双轴叠加
- [ ] P5 结果长期入库(JSON 持久化) + 导出/删除 + 策略库保存
- [ ] 远期: Python 高级脚本沙箱(AST 白名单)第二作者模式