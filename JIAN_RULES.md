# Jian - 项目开发规范与上下文记忆

## 1. 项目愿景与定位
- **项目名称:** Jian (交易复盘系统)
- **项目目标:** 从 0 到 1 打造一个具有企业级地基、高扩展性的专业量化复盘与交易分析桌面端软件。
- **开发原则:** 宁可初期花费大量时间搭好地基，也绝不为了赶进度而在单文件里堆砌代码。为未来外接多元数据（如宏观数据、衍生品参数等）和多模型留出充分的接口。

## 2. 技术栈
- **UI 框架:** PyQt6 / PySide6
- **图表引擎:** PyQtGraph (深度定制 K线、盈亏曲线、时长散点图等)
- **数据处理:** Pandas, NumPy
- **运行环境:** Python 3.x 本地独立运行（不考虑云端微服务及多人协同部署）

## 3. 核心架构设计原则
本项目严格遵循 **MVC/MVP 分层与 Michael Pokorny 设计原则**，极度强调解耦：
1. **视图极其纯净 (Dumb UI):** `ui/` 目录下的代码只负责“长什么样”和“捕获用户点击”。绝对不允许在 UI 组件中写任何盈亏计算、数据清洗或数据库读取逻辑。
2. **拒绝上帝类 (No God Class):** 巨型视图必须被拆分成独立组件（如已拆分的 `dashboard.py`, `records.py`, `review.py`）。主窗口 (`main_window.py`) 只作为总调度器（Controller），负责路由和统筹。
3. **数据中心化 (Single Source of Truth):** 核心数据必须在底层集中管理，UI 组件通过事件或统一的接口刷新，避免状态同步混乱。
4. **小步快跑:** 任何重构和新功能添加，都必须保证每做完一步程序依然能完美运行，避免陷入“重构地狱”。

## 4. 目录结构 (当前最新状态)
```text
Jian/
├── main.py              # 程序唯一启动入口，负责环境与全局配置 (如图表背景色)
├── .gitignore           # Git 忽略文件，排除 __pycache__ 及本地生成数据
├── core/                # 核心引擎层
│   └── analyzer.py      # 数据分析脑，负责纯粹的数学统计与回撤计算
├── data/                # 数据层
│   └── data_feed.py     # 外部数据获取与清洗 (假数据生成、交割单解析)
├── ui/                  # 表现层
│   ├── main_window.py   # 主窗口控制器
│   ├── widgets/         # 自定义底层 UI 控件 (如带悬浮删除的 List、K线图元)
│   │   └── custom_widgets.py
│   ├── dialogs/         # 独立弹窗 (如导入向导、设置、手工录入)
│   │   └── dialogs.py
│   └── views/           # 页面级大型视图组件
│       ├── dashboard.py # 资金与表现面板
│       ├── records.py   # 流水表格面板
│       └── review.py    # 深度复盘双模态工作台
└── JIAN_RULES.md        # 本规范与记忆文件
```
## 5. AI 协作约定 (给 AI 的指令)
当你（AI）阅读到此文件时，请在接下来的对话中严格遵守以下规则：

回答风格: 保持模块化思维。在提供代码前，先明确这段代码应该放在哪个目录的哪个文件中。

全量代码: 对于初期项目，在修改某个文件时，尽可能提供该文件的全量代码，方便用户直接覆盖防止粘错，同时加上明确的注释。

保持精简: 不要擅自引入庞大的第三方重型库（如 Docker, 复杂 ORM 框架等），保持个人开源项目的轻量与专注。

警惕导包: 注意跨文件调用的 import 路径，基于当前的物理目录结构给出正确的导入方式。

## 6. 当前开发进度与下一步 Roadmap (待办事项)
当前已完成第一阶段的“单体应用大拆解与解耦”，程序可完美运行。
接下来的核心优化方向：

[ ] Phase 2-A: 配置中心化 (config/settings.py) - 剥离代码中硬编码的窗口尺寸、默认字符串等参数。

[ ] Phase 2-B: 数据模型化 (models/trade.py) - 为一笔交易建立强类型的 Class，替换裸奔的 DataFrame 行，提升数据鲁棒性。

[ ] Phase 2-C: 状态引擎分离 (core/engine.py) - 将 main_window.py 中持有的 global_df 彻底移交到底层数据引擎管理。

