# Jian - 项目开发规范与上下文记忆 (v2.0)

## 1. 项目愿景与定位
- **项目名称:** Jian (专业交易复盘系统)
- **项目目标:** 采用 MVP (最小可行性产品) 模式与敏捷开发，从 0 到 1 打造一个具有企业级地基、高扩展性的桌面端软件。
- **核心理念:** 坚持代码的高内聚、低耦合。实现程序本体与用户数据的物理隔离，保障用户的核心资产（复盘数据）绝对安全。

## 2. 技术栈
- **UI 框架:** PyQt6 / PySide6
- **图表引擎:** PyQtGraph (深度定制 K线、盈亏曲线、时长散点图等)
- **数据处理:** Pandas, NumPy
- **数据结构:** Python `dataclasses` (强类型数据模型)
- **持久化方案:** SQLite / 结构化本地文件 (Phase 3 建设中)

## 3. 核心架构设计原则 (MVC/MVP 深度解耦)
本项目严格遵循以下开发铁律：
1. **单一事实来源 (Single Source of Truth):** 所有核心数据必须交由 `core/engine.py` 中的 `DataEngine` 统一管理。
2. **面向模型编程 (Strong Typing):** 外部数据进入系统前，必须被转化为 `models/trade.py` 中的 `TradeRecord` 标准实例。严禁在业务逻辑中让原生字典裸奔。
3. **配置绝对中心化 (No Magic Strings):** 严禁在代码中硬编码任何颜色、窗口尺寸、默认字符串等。所有全局参数必须从 `config/settings.py` 中读取。
4. **视图极其纯净 (Dumb UI):** UI 组件（`views/`, `dialogs/`）只负责“长什么样”和“捕获点击”，严禁直接修改底层数据，修改数据的指令必须委托给主窗口控制器调用 Engine 的 API。
5. **用户数据隔离:** 用户产生的业务数据（截图、数据库）绝不直接存在代码目录下，需考虑未来打包分发时的 AppData 存储规范。

## 4. 目录结构 (Phase 2 完成态)
```text
Jian/
├── main.py              # 程序唯一启动入口，负责环境初始化、注入全局配置
├── .gitignore           # Git 忽略规则，保护代码库纯净
├── JIAN_RULES.md        # 本规范与记忆文件 (当前版本 v2.0)
├── config/              # 【配置中心】
│   └── settings.py      # 全局常量、颜色主题、路径管理
├── models/              # 【数据模型】
│   └── trade.py         # 核心数据契约 (TradeRecord)
├── core/                # 【核心逻辑引擎】
│   ├── engine.py        # 中央数据状态引擎 (DataEngine)
│   └── analyzer.py      # 纯粹的数学统计与回撤计算引擎
├── data/                # 【数据来源与清洗】
│   └── data_feed.py     # 假数据生成、外部 Excel (如 CFMMC) 解析映射
└── ui/                  # 【表现层 (UI)】
    ├── main_window.py   # 总路由器/控制器 (Controller)
    ├── widgets/         # 底层自定义控件 (K线图元、悬浮列表等)
    ├── dialogs/         # 全局弹窗组件 (设置、导入、录入等)
    └── views/           # 页面级大型视图组件
        ├── dashboard.py # 资金与表现面板
        ├── records.py   # 流水表格面板
        └── review.py    # 深度复盘双模态工作台
```
## 5. AI 协作约定 (给 AI 的指令)
当你（AI）阅读到此文件时，请在接下来的对话中严格遵守以下规则：

查阅配置: 任何涉及颜色、默认值、路径的修改，优先去 settings.py 寻找或添加，决不允许在 UI 文件中硬编码。

遵循契约: 涉及数据结构的增删改，必须通过 TradeRecord 实例和 DataEngine 的方法，保持数据流单向清晰。

全量输出: 在“0到1”构建期，修改核心文件时尽量提供该文件的全量代码，防止小白用户复制出错。

商业思维: 在提供技术方案时，始终以“用户不丢失数据”、“支持无痛版本更新”的商业化 MVP 标准来要求代码。

## 6. 开发进度与 Roadmap
✅ Phase 1: 单体应用拆解 - 完成 UI 与逻辑的物理文件分离。
✅ Phase 2: 深度架构重构 - 完成配置中心化、数据强类型化、引入中央数据引擎。
🚀 Phase 3: MVP 商业化闭环 (当前阶段)

[ ] 数据持久化: 接入 SQLite 或本地文件系统，实现重启后数据保留，并将数据目录移至安全路径。

[ ] 版本检测机制: 启动时静默比对云端版本，弹窗引导自主更新。

[ ] 安全兜底: 实现用户数据的 Excel/CSV 全量导出功能。