# Jian - 项目开发规范与上下文记忆 (v3.0)

## 1. 项目愿景与定位
- **项目名称:** Jian (专业交易复盘系统)
- **项目目标:** 采用 MVP 模式与敏捷开发，打造一个具有企业级地基、高扩展性、数据绝对安全的桌面端量化复盘软件。
- **核心理念:** 坚持代码的高内聚、低耦合；坚持程序本体与用户数据的物理隔离；坚持操作的幂等性（防呆防错）。

## 2. 技术栈
- **UI 框架:** PyQt6 / PySide6 (主界面与视图组件)
- **图表引擎:** PyQtGraph (深度定制高性能金融图表)
- **数据处理:** Pandas, NumPy, hashlib (确定性哈希)
- **持久化方案:** SQLite (通过 `sqlite3` 内置库实现 ACID 事务与结构化存储)
- **并发控制:** `QThread` (用于网络请求与耗时任务，绝对禁止阻塞主线程)

## 3. 核心架构设计原则 (MVC 深度解耦与数据守护)
本项目严格遵循以下开发铁律：
1. **单一事实来源 (Single Source of Truth):** 内存中的 `DataEngine` 和硬盘中的 `SQLite` 是数据的唯一合法持有者，UI 只能读取或通过 API 发送修改指令。
2. **幂等性与唯一主键:** 底层数据库绝对摒弃易重复的业务单号，统一使用 **MD5(账户名_单号)** 生成绝对唯一的 `internal_id`。确保同一份数据无论导入多少次都不会重复或覆盖错乱。
3. **数据物理隔离:** 用户产生的业务数据（`.db` 文件、`screenshots` 截图）必须强制保存在操作系统级别的用户数据目录（如 `~/.jian_data`），实现代码与数据的完全分离，保障版本覆盖时不丢数据。
4. **非阻塞 UI (Non-blocking GUI):** 任何涉及网络请求（如版本检测）或极度耗时的 I/O 操作，必须使用 `QThread` 挂入后台异步执行，确保界面永远丝滑。
5. **配置绝对中心化:** 所有尺寸、颜色（含 RGB 元组）、默认占位符、云端链接等，必须从 `config/settings.py` 中读取。

## 4. 目录结构 (Phase 3 完成态)
```text
Jian/
├── main.py              # 程序唯一启动入口，负责环境初始化、注入全局配置
├── .gitignore           # Git 忽略规则，保护代码库纯净
├── JIAN_RULES.md        # 本规范与记忆文件 (当前版本 v3.0)
├── config/              # 【配置中心】
│   └── settings.py      # 全局常量、颜色主题、用户数据物理路径、更新链接
├── models/              # 【数据模型】
│   └── trade.py         # 核心数据契约 (TradeRecord)，含 internal_id 哈希钩子
├── core/                # 【核心逻辑引擎】
│   ├── engine.py        # 中央数据状态引擎 (DataEngine)，统筹内存与 DB
│   ├── database.py      # 数据库管家 (DAO)，专职与 SQLite 进行 SQL 交互
│   ├── analyzer.py      # 纯粹的数学统计与回撤计算引擎
│   └── updater.py       # 异步侦察兵 (QThread)，后台静默检测版本更新
├── data/                # 【数据来源与清洗】
│   └── data_feed.py     # 假数据生成、外部 Excel (如 CFMMC) 解析映射
└── ui/                  # 【表现层 (UI)】
    ├── main_window.py   # 总路由器/控制器 (Controller)，处理全局弹窗与事件分配
    ├── widgets/         # 底层自定义控件 (K线图元、悬浮列表等)
    ├── dialogs/         # 全局弹窗组件 (设置、导入、录入等)
    └── views/           # 页面级大型视图组件
        ├── dashboard.py # 资金与表现面板
        ├── records.py   # 流水表格面板，含一键 CSV 全量导出
        └── review.py    # 深度复盘双模态工作台
```
## 5. AI 协作约定 (给 AI 的指令)
当你（AI）阅读到此文件时，请在接下来的对话中严格遵守以下规则：

防阻塞底线: 增加新功能时，先评估是否会卡顿 UI。如果是，主动提供 QThread 异步方案。

数据库迁移思维: 如果提出的新需求需要修改 models/trade.py 里的字段，必须同时提供 database.py 里的 SQL 迁移（Migration）方案，确保旧用户的表结构平滑升级。

业务幂等性: 新增数据导入接口时，必须遵守“多次操作等同于一次操作”的防御性编程逻辑。

全量输出: 在架构演进期，修改核心文件时尽量提供该文件的全量代码，防止小白用户复制出错。

## 6. 开发进度与 Roadmap
✅ Phase 1: 单体应用拆解 - 完成 UI 与逻辑的物理分离。
✅ Phase 2: 深度架构重构 - 配置中心化、数据强类型化、引擎接管。
✅ Phase 3: MVP 商业化闭环 - SQLite 接入、幂等性哈希、全量导出兜底、异步静默版本检测。

🚀 Phase 4: 应用层核心功能进阶 (当前阶段)
(未来探讨方向，视具体需求而定)

[ ] 宏观周期对照: 引入大盘/商品行情基准对比，探索雪球/衍生品结构。

[ ] 硬核技术分析: 集成 TA-Lib，升级 K 线图，增加 MA/MACD 及自定义画线工具。

[ ] 高级数据看板: 引入 GitHub 式日历热力图，增加多条件组合筛选器。

[ ] 沙盒回测系统: 搭建基于历史行情的回测框架骨架。

方向一：宏观周期与行情对照模块 (Macro & Market Context)
复盘不能只看自己的账户，脱离了大盘和周期的复盘往往是刻舟求剑。我们可以开发一个独立的行情模块，接入大宗商品（如金、银、铜等有色金属）的长周期历史数据，或者叠加中证 500、中证 1000 等核心宽基指数的走势曲线。在你的资金净值曲线下方，直接映照出当时的宏观大势，甚至可以为未来引入雪球结构衍生品的对冲复盘留出空间。

方向二：多维图表与硬核技术分析 (Advanced Charting)
目前的 K 线图还比较基础。我们可以集成 TA-Lib（技术分析库），在 K 线图上直接叠加 MA（移动平均线）、MACD、BOLL 等技术指标。甚至可以开发交互式的“画线工具”，允许用户在复盘图表上直接绘制支撑位、阻力位和趋势线，并保存这些绘图状态。

方向三：日历热力图与高级过滤 (Calendar & Advanced Filters)
将左上角的复盘日历升级为真正的 GitHub 风格“热力图”，一眼看出每月的交易密集度。同时在流水表格增加高级多条件筛选器（比如：“找出所有做多 RB 且持仓超过 2 天的亏损单”），帮助精准定位短板。

方向四：自定义策略回测骨架 (Strategy Backtesting Sandbox)
在现有的真实交易记录之外，开辟一个“沙盒模式”。允许用户编写简单的 Python 策略脚本（如双均线交叉），在历史行情数据上进行模拟跑盘，并将模拟结果与真实手工交易的结果进行重叠对比分析。